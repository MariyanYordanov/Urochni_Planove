
---

### **📊 КОМПОНЕНТ 4.3: SIEM И SECURITY ORCHESTRATION (4 часа)**

#### **🎯 СПЕЦИФИЧНИ ЦЕЛИ НА КОМПОНЕНТА:**

**ЗНАНИЯ:**
- Разбиране на modern SIEM architecture principles и advanced correlation techniques
- Познаване на Security Orchestration, Automation и Response (SOAR) platform capabilities
- Владеене на incident response automation workflows и escalation procedures
- Знание за SOC operations optimization и performance measurement methodologies
- Разбиране на security analytics, machine learning integration и threat detection automation

**УМЕНИЯ:**
- Проектиране и имплементиране на enterprise-grade SIEM solutions със comprehensive log management
- Конфигуриране на SOAR platforms със automated response workflows и orchestration capabilities
- Управление на complex incident response procedures със automated escalation и remediation
- Оптимизиране на SOC operations със performance metrics и continuous improvement processes
- Интегриране на AI/ML analytics във security monitoring и automated threat detection

**КОМПЕТЕНТНОСТИ:**
- Strategic thinking за enterprise security operations center design и optimization
- Technical leadership във SIEM/SOAR platform implementation и management
- Analytical mindset за complex security event correlation и investigation
- Process optimization approach към security workflow automation и efficiency
- Innovation capabilities за emerging security technologies integration и advancement

---

## 📚 **ЧАС 1: SIEM ARCHITECTURE DESIGN И LOG CORRELATION TECHNIQUES (40 мин)**

### **📊 Modern SIEM Architecture Framework (15 мин)**

**SIEM Evolution и Capabilities:**

```
SIEM Architecture Evolution:
┌─────────────────────────────────────────────────────────────┐
│                   First Generation SIEM                    │
│  ├── Log collection и storage                              │
│  ├── Basic correlation rules                               │
│  ├── Simple alerting mechanisms                            │
│  ├── Manual incident investigation                         │
│  └── Limited integration capabilities                      │
├─────────────────────────────────────────────────────────────┤
│                  Second Generation SIEM                    │
│  ├── Real-time event processing                           │
│  ├── Advanced correlation engines                         │
│  ├── Threat intelligence integration                      │
│  ├── Compliance reporting automation                      │
│  ├── Basic workflow automation                            │
│  └── Dashboard и visualization improvements               │
├─────────────────────────────────────────────────────────────┤
│                   Third Generation SIEM                    │
│  ├── Big data analytics platforms                         │
│  ├── Machine learning integration                         │
│  ├── User и entity behavior analytics                     │
│  ├── Cloud-native architectures                           │
│  ├── API-first integration approaches                     │
│  ├── Advanced threat hunting capabilities                 │
│  └── Orchestration и automation integration               │
├─────────────────────────────────────────────────────────────┤
│                 Next Generation SIEM (XDR)                │
│  ├── Extended detection и response                        │
│  ├── AI-driven autonomous investigation                   │
│  ├── Cross-platform threat correlation                    │
│  ├── Automated response orchestration                     │
│  ├── Continuous adaptive learning                         │
│  ├── Zero-trust security integration                      │
│  └── Predictive threat modeling                           │
└─────────────────────────────────────────────────────────────┘
```

**Enterprise SIEM Architecture Components:**

**Data Collection Layer:**
```
Comprehensive Data Collection Framework:
├── Log Sources
│   ├── Network Infrastructure
│   │   ├── Firewalls (Palo Alto, Cisco ASA, Fortinet)
│   │   ├── IDS/IPS (Snort, Suricata, Cisco Firepower)
│   │   ├── Switches и routers (SNMP, NetFlow, sFlow)
│   │   ├── Load balancers (F5, A10, HAProxy)
│   │   ├── VPN concentrators (Cisco AnyConnect, Pulse Secure)
│   │   └── Network access control (Cisco ISE, Aruba ClearPass)
│   ├── Endpoint Systems
│   │   ├── Windows event logs (Security, System, Application)
│   │   ├── Linux syslogs (auth.log, kern.log, messages)
│   │   ├── macOS unified logs (security, system events)
│   │   ├── EDR/EPP platforms (CrowdStrike, SentinelOne)
│   │   ├── Mobile device management (MDM) logs
│   │   └── IoT device telemetry
│   ├── Applications
│   │   ├── Web applications (Apache, Nginx, IIS access logs)
│   │   ├── Database servers (Oracle, SQL Server, MySQL)
│   │   ├── Application servers (Tomcat, WebSphere, WebLogic)
│   │   ├── Email systems (Exchange, Office 365, Gmail)
│   │   ├── Collaboration tools (Teams, Slack, Zoom)
│   │   └── Business applications (SAP, Oracle ERP, CRM)
│   ├── Cloud Platforms
│   │   ├── AWS (CloudTrail, VPC Flow Logs, GuardDuty)
│   │   ├── Azure (Activity Logs, Security Center, Sentinel)
│   │   ├── Google Cloud (Cloud Logging, Security Command Center)
│   │   ├── Office 365 (Audit logs, Advanced Threat Protection)
│   │   └── SaaS applications (Salesforce, Workday, ServiceNow)
│   └── Security Tools
│       ├── Vulnerability scanners (Nessus, Qualys, Rapid7)
│       ├── Threat intelligence platforms (MISP, ThreatConnect)
│       ├── Email security gateways (Proofpoint, Mimecast)
│       ├── Web security gateways (Zscaler, BlueCoat, Forcepoint)
│       └── Data loss prevention (Symantec DLP, Forcepoint DLP)
├── Collection Methods
│   ├── Agent-Based Collection
│   │   ├── Universal forwarders (Splunk UF)
│   │   ├── Beats agents (Filebeat, Winlogbeat, Metricbeat)
│   │   ├── FluentD и Logstash agents
│   │   ├── OSSEC/Wazuh agents
│   │   └── Custom collection scripts
│   ├── Agentless Collection
│   │   ├── Syslog forwarding (TCP/UDP 514, TLS)
│   │   ├── SNMP polling (v2c, v3 secure)
│   │   ├── WMI/WinRM collection
│   │   ├── API-based collection (REST, GraphQL)
│   │   └── File share monitoring (SMB, NFS)
│   ├── Stream Processing
│   │   ├── Apache Kafka message streaming
│   │   ├── AWS Kinesis data streams
│   │   ├── Azure Event Hubs
│   │   ├── Google Cloud Pub/Sub
│   │   └── Real-time protocol analysis
│   └── Batch Processing
│       ├── Scheduled file transfers (SFTP, SCP)
│       ├── Database direct connects (JDBC, ODBC)
│       ├── Cloud storage imports (S3, Blob, GCS)
│       ├── Email-based log delivery
│       └── Archival system integration
└── Data Normalization
    ├── Common Event Format (CEF)
    ├── Log Event Extended Format (LEEF)
    ├── JSON structured logging
    ├── Syslog RFC standards (3164, 5424)
    ├── OCSF (Open Cybersecurity Schema Framework)
    └── Custom parsing rules и field mapping
```

**SIEM Data Architecture:**
```python
# Enterprise SIEM Data Architecture Implementation
class SIEMDataArchitecture:
    def __init__(self):
        self.ingestion_layer = DataIngestionLayer()
        self.processing_layer = EventProcessingLayer()
        self.storage_layer = DataStorageLayer()
        self.analytics_layer = SecurityAnalyticsLayer()
        self.presentation_layer = PresentationLayer()
        
    def design_ingestion_architecture(self, data_sources, throughput_requirements):
        """Design data ingestion architecture based on requirements"""
        architecture = IngestionArchitecture()
        
        # Calculate data volume requirements
        total_eps = sum([source.events_per_second for source in data_sources])
        peak_eps = total_eps * 3  # Account for peak traffic
        
        # Design collection infrastructure
        if peak_eps > 100000:  # High-volume environment
            architecture.collection_method = 'distributed_kafka_cluster'
            architecture.kafka_config = {
                'brokers': self.calculate_kafka_brokers(peak_eps),
                'partitions': self.calculate_partitions(data_sources),
                'replication_factor': 3,
                'retention_hours': 24
            }
        elif peak_eps > 10000:  # Medium-volume environment
            architecture.collection_method = 'centralized_logstash'
            architecture.logstash_config = {
                'instances': self.calculate_logstash_instances(peak_eps),
                'memory_per_instance': '4GB',
                'pipeline_workers': 8
            }
        else:  # Low-volume environment
            architecture.collection_method = 'single_collector'
            architecture.collector_config = {
                'type': 'rsyslog_ng',
                'memory': '2GB',
                'storage': '100GB'
            }
        
        # Design parsing и normalization
        architecture.parsing_rules = self.generate_parsing_rules(data_sources)
        architecture.normalization_schema = self.design_normalization_schema(data_sources)
        
        return architecture
    
    def implement_log_parsing(self, log_sources):
        """Implement comprehensive log parsing и normalization"""
        parsing_engine = LogParsingEngine()
        
        for source in log_sources:
            # Create source-specific parser
            parser = self.create_source_parser(source)
            
            # Define field extraction rules
            extraction_rules = self.define_field_extraction(source)
            
            # Configure enrichment rules
            enrichment_rules = self.define_enrichment_rules(source)
            
            # Register parser with engine
            parsing_engine.register_parser(source.name, parser, extraction_rules, enrichment_rules)
        
        return parsing_engine
    
    def create_source_parser(self, source):
        """Create parser for specific log source"""
        if source.type == 'windows_eventlog':
            return WindowsEventLogParser(
                event_id_mapping=self.load_windows_event_mappings(),
                field_mappings=self.load_windows_field_mappings(),
                severity_mapping=self.load_windows_severity_mappings()
            )
        elif source.type == 'syslog':
            return SyslogParser(
                facility_mapping=self.load_syslog_facilities(),
                severity_mapping=self.load_syslog_severities(),
                custom_patterns=self.load_custom_syslog_patterns()
            )
        elif source.type == 'cisco_asa':
            return CiscoASAParser(
                message_id_mapping=self.load_cisco_message_mappings(),
                action_mapping=self.load_cisco_action_mappings(),
                protocol_mapping=self.load_protocol_mappings()
            )
        elif source.type == 'json':
            return JSONParser(
                schema_validation=True,
                field_flattening=True,
                timestamp_parsing=True
            )
        else:
            return GenericLogParser(
                regex_patterns=self.load_generic_patterns(source),
                field_mappings=self.load_generic_mappings(source)
            )
    
    def design_correlation_engine(self, use_cases):
        """Design correlation engine for security use cases"""
        correlation_engine = CorrelationEngine()
        
        # Real-time correlation rules
        for use_case in use_cases:
            correlation_rule = self.create_correlation_rule(use_case)
            correlation_engine.add_rule(correlation_rule)
        
        # Statistical correlation
        statistical_rules = self.create_statistical_rules(use_cases)
        correlation_engine.add_statistical_rules(statistical_rules)
        
        # Machine learning correlation
        ml_models = self.create_ml_correlation_models(use_cases)
        correlation_engine.add_ml_models(ml_models)
        
        return correlation_engine
    
    def create_correlation_rule(self, use_case):
        """Create correlation rule for specific use case"""
        if use_case.name == 'brute_force_detection':
            return CorrelationRule(
                name='SSH_Brute_Force_Detection',
                description='Detect SSH brute force attacks',
                conditions=[
                    Condition('event_type', 'equals', 'ssh_login_failed'),
                    Condition('source_ip', 'same_value', True),
                    Condition('count', 'greater_than', 5),
                    Condition('timeframe', 'within', '5 minutes')
                ],
                actions=[
                    Action('create_alert', {'severity': 'medium', 'category': 'brute_force'}),
                    Action('enrich_with_geoip', {'field': 'source_ip'}),
                    Action('lookup_threat_intelligence', {'field': 'source_ip'})
                ],
                suppression_rules=[
                    SuppressionRule('same_source_ip', duration='10 minutes')
                ]
            )
        elif use_case.name == 'lateral_movement_detection':
            return CorrelationRule(
                name='Lateral_Movement_Detection',
                description='Detect lateral movement via RDP/SMB',
                conditions=[
                    Condition('event_type', 'in', ['rdp_login', 'smb_access']),
                    Condition('user', 'same_value', True),
                    Condition('destination_hosts', 'unique_count', 'greater_than', 3),
                    Condition('timeframe', 'within', '30 minutes')
                ],
                actions=[
                    Action('create_alert', {'severity': 'high', 'category': 'lateral_movement'}),
                    Action('enrich_with_user_context', {'field': 'user'}),
                    Action('create_investigation_case', {'priority': 'high'})
                ]
            )
        
        return correlation_rule
```

### **🔗 Advanced Correlation Techniques (12 мин)**

**Multi-Dimensional Correlation Strategies:**

**Temporal Correlation Patterns:**
```python
# Advanced Temporal Correlation Engine
class TemporalCorrelationEngine:
    def __init__(self):
        self.time_windows = {}
        self.sequence_detectors = {}
        self.anomaly_detectors = {}
        
    def implement_sliding_window_correlation(self, events, window_size, overlap):
        """Implement sliding window correlation analysis"""
        correlation_results = []
        
        # Create sliding windows
        windows = self.create_sliding_windows(events, window_size, overlap)
        
        for window in windows:
            # Analyze event patterns within window
            patterns = self.analyze_event_patterns(window)
            
            # Detect sequence anomalies
            sequence_anomalies = self.detect_sequence_anomalies(window)
            
            # Calculate temporal correlations
            temporal_correlations = self.calculate_temporal_correlations(window)
            
            # Score correlation strength
            correlation_score = self.score_correlation_strength(patterns, sequence_anomalies, temporal_correlations)
            
            if correlation_score > 0.7:  # High correlation threshold
                correlation_result = TemporalCorrelationResult(
                    window=window,
                    patterns=patterns,
                    anomalies=sequence_anomalies,
                    correlations=temporal_correlations,
                    score=correlation_score,
                    recommended_actions=self.recommend_actions(correlation_score)
                )
                correlation_results.append(correlation_result)
        
        return correlation_results
    
    def detect_attack_sequence_patterns(self, events):
        """Detect multi-stage attack sequence patterns"""
        attack_sequences = [
            AttackSequence('credential_theft_and_lateral_movement', [
                SequenceStep('credential_access', ['lsass_access', 'credential_dumping']),
                SequenceStep('lateral_movement', ['rdp_login', 'smb_access', 'psexec']),
                SequenceStep('persistence', ['registry_modification', 'service_creation'])
            ]),
            AttackSequence('data_exfiltration_sequence', [
                SequenceStep('collection', ['file_enumeration', 'data_archiving']),
                SequenceStep('staging', ['temp_file_creation', 'encryption']),
                SequenceStep('exfiltration', ['large_upload', 'external_connection'])
            ])
        ]
        
        detected_sequences = []
        
        for sequence in attack_sequences:
            sequence_match = self.match_sequence_pattern(events, sequence)
            if sequence_match.confidence > 0.8:
                detected_sequences.append(sequence_match)
        
        return detected_sequences
    
    def implement_behavioral_correlation(self, user_events, baseline_behavior):
        """Implement user behavior correlation analysis"""
        behavioral_correlations = []
        
        # Group events by user
        user_grouped_events = self.group_events_by_user(user_events)
        
        for user, events in user_grouped_events.items():
            # Get user baseline behavior
            user_baseline = baseline_behavior.get(user)
            if not user_baseline:
                continue
            
            # Analyze current behavior patterns
            current_behavior = self.analyze_user_behavior(events)
            
            # Calculate behavioral deviations
            deviations = self.calculate_behavioral_deviations(current_behavior, user_baseline)
            
            # Identify significant anomalies
            significant_anomalies = [d for d in deviations if d.significance > 0.7]
            
            if significant_anomalies:
                behavioral_correlation = BehavioralCorrelation(
                    user=user,
                    baseline=user_baseline,
                    current_behavior=current_behavior,
                    deviations=deviations,
                    anomalies=significant_anomalies,
                    risk_score=self.calculate_user_risk_score(significant_anomalies)
                )
                behavioral_correlations.append(behavioral_correlation)
        
        return behavioral_correlations
```

### **📈 SIEM Performance Optimization (13 мин)**

**Enterprise SIEM Performance Framework:**

**Optimization Strategies:**
```python
# SIEM Performance Optimization Engine
class SIEMPerformanceOptimizer:
    def __init__(self):
        self.performance_monitor = PerformanceMonitor()
        self.resource_optimizer = ResourceOptimizer()
        self.query_optimizer = QueryOptimizer()
        self.index_optimizer = IndexOptimizer()
        
    def optimize_siem_performance(self, siem_instance):
        """Comprehensive SIEM performance optimization"""
        optimization_results = {}
        
        # Data ingestion optimization
        ingestion_optimization = self.optimize_data_ingestion(siem_instance)
        optimization_results['ingestion'] = ingestion_optimization
        
        # Storage optimization
        storage_optimization = self.optimize_data_storage(siem_instance)
        optimization_results['storage'] = storage_optimization
        
        # Query performance optimization
        query_optimization = self.optimize_query_performance(siem_instance)
        optimization_results['queries'] = query_optimization
        
        # Correlation engine optimization
        correlation_optimization = self.optimize_correlation_engine(siem_instance)
        optimization_results['correlation'] = correlation_optimization
        
        # Resource utilization optimization
        resource_optimization = self.optimize_resource_utilization(siem_instance)
        optimization_results['resources'] = resource_optimization
        
        return SIEMOptimizationResult(
            optimizations=optimization_results,
            performance_improvement=self.calculate_performance_improvement(optimization_results),
            recommendations=self.generate_optimization_recommendations(optimization_results)
        )
    
    def optimize_data_ingestion(self, siem_instance):
        """Optimize data ingestion performance"""
        current_throughput = self.measure_ingestion_throughput(siem_instance)
        
        optimizations = []
        
        # Parallel ingestion optimization
        if current_throughput.bottleneck == 'single_threaded_ingestion':
            parallel_config = ParallelIngestionConfig(
                worker_threads=min(cpu_count() * 2, 32),
                queue_size=10000,
                batch_size=1000
            )
            optimizations.append(('parallel_ingestion', parallel_config))
        
        # Buffer optimization
        if current_throughput.buffer_utilization > 0.8:
            buffer_optimization = BufferOptimization(
                increase_buffer_size=True,
                new_buffer_size=current_throughput.buffer_size * 2,
                enable_compression=True,
                compression_algorithm='zstd'
            )
            optimizations.append(('buffer_optimization', buffer_optimization))
        
        # Network optimization
        if current_throughput.network_latency > 100:  # ms
            network_optimization = NetworkOptimization(
                enable_tcp_no_delay=True,
                increase_receive_buffer=True,
                enable_connection_pooling=True,
                connection_pool_size=50
            )
            optimizations.append(('network_optimization', network_optimization))
        
        # Parsing optimization
        if current_throughput.parsing_cpu_usage > 0.7:
            parsing_optimization = ParsingOptimization(
                precompile_regex_patterns=True,
                enable_parsing_cache=True,
                optimize_field_extraction=True,
                parallel_parsing=True
            )
            optimizations.append(('parsing_optimization', parsing_optimization))
        
        return IngestionOptimizationResult(
            current_performance=current_throughput,
            optimizations=optimizations,
            projected_improvement=self.calculate_projected_improvement(optimizations)
        )
    
    def optimize_query_performance(self, siem_instance):
        """Optimize search и query performance"""
        query_metrics = self.analyze_query_performance(siem_instance)
        
        optimizations = []
        
        # Index optimization
        if query_metrics.avg_query_time > 30:  # seconds
            index_optimization = IndexOptimization(
                create_accelerated_data_models=True,
                optimize_field_indexing=True,
                create_summary_indexes=True,
                enable_bloom_filters=True
            )
            optimizations.append(('index_optimization', index_optimization))
        
        # Query optimization
        slow_queries = [q for q in query_metrics.queries if q.execution_time > 60]
        if slow_queries:
            query_optimization = QueryOptimization(
                rewrite_slow_queries=True,
                add_time_range_filters=True,
                optimize_join_operations=True,
                enable_query_caching=True
            )
            optimizations.append(('query_optimization', query_optimization))
        
        # Resource allocation optimization
        if query_metrics.concurrent_query_conflicts > 10:  # per hour
            resource_optimization = ResourceAllocationOptimization(
                increase_search_concurrency=True,
                max_concurrent_searches=query_metrics.max_concurrent_searches * 1.5,
                dedicated_search_heads=True,
                search_head_clustering=True
            )
            optimizations.append(('resource_optimization', resource_optimization))
        
        return QueryOptimizationResult(
            current_metrics=query_metrics,
            optimizations=optimizations,
            projected_improvement=self.calculate_query_improvement(optimizations)
        )
    
    def implement_auto_scaling(self, siem_instance):
        """Implement auto-scaling for SIEM infrastructure"""
        auto_scaling_config = AutoScalingConfig()
        
        # Define scaling triggers
        auto_scaling_config.scaling_triggers = [
            ScalingTrigger(
                metric='cpu_utilization',
                threshold=80,
                duration=300,  # seconds
                action='scale_up',
                scaling_factor=1.5
            ),
            ScalingTrigger(
                metric='memory_utilization',
                threshold=85,
                duration=300,
                action='scale_up',
                scaling_factor=1.3
            ),
            ScalingTrigger(
                metric='ingestion_queue_depth',
                threshold=10000,
                duration=60,
                action='scale_up_ingestion',
                scaling_factor=2.0
            ),
            ScalingTrigger(
                metric='query_response_time',
                threshold=45,  # seconds
                duration=600,
                action='scale_up_search',
                scaling_factor=1.2
            )
        ]
        
        # Define scaling limits
        auto_scaling_config.scaling_limits = ScalingLimits(
            min_instances=2,
            max_instances=20,
            min_cpu_cores=4,
            max_cpu_cores=64,
            min_memory_gb=8,
            max_memory_gb=256
        )
        
        # Define cost optimization rules
        auto_scaling_config.cost_optimization = CostOptimization(
            enable_spot_instances=True,
            preferred_instance_types=['r5.xlarge', 'r5.2xlarge', 'r5.4xlarge'],
            scale_down_after_idle=1800,  # seconds
            prefer_regional_resources=True
        )
        
        return auto_scaling_config
    
    def monitor_siem_health(self, siem_instance):
        """Monitor comprehensive SIEM health metrics"""
        health_metrics = SIEMHealthMetrics()
        
        # Ingestion health
        health_metrics.ingestion = IngestionHealthMetrics(
            events_per_second=self.measure_current_eps(siem_instance),
            ingestion_lag=self.measure_ingestion_lag(siem_instance),
            failed_ingestion_rate=self.measure_failed_ingestion_rate(siem_instance),
            parser_error_rate=self.measure_parser_error_rate(siem_instance)
        )
        
        # Storage health
        health_metrics.storage = StorageHealthMetrics(
            disk_utilization=self.measure_disk_utilization(siem_instance),
            hot_storage_ratio=self.measure_hot_storage_ratio(siem_instance),
            cold_storage_ratio=self.measure_cold_storage_ratio(siem_instance),
            retention_compliance=self.check_retention_compliance(siem_instance)
        )
        
        # Query performance health
        health_metrics.query_performance = QueryHealthMetrics(
            average_query_time=self.measure_avg_query_time(siem_instance),
            query_success_rate=self.measure_query_success_rate(siem_instance),
            concurrent_query_capacity=self.measure_concurrent_capacity(siem_instance),
            slow_query_count=self.count_slow_queries(siem_instance)
        )
        
        # Correlation engine health
        health_metrics.correlation = CorrelationHealthMetrics(
            correlation_processing_lag=self.measure_correlation_lag(siem_instance),
            rule_efficiency=self.measure_rule_efficiency(siem_instance),
            false_positive_rate=self.measure_false_positive_rate(siem_instance),
            alert_processing_time=self.measure_alert_processing_time(siem_instance)
        )
        
        # Calculate overall health score
        health_metrics.overall_health_score = self.calculate_overall_health_score(health_metrics)
        
        return health_metrics
```

---

## 📚 **ЧАС 2: SECURITY ORCHESTRATION, AUTOMATION И RESPONSE (SOAR) IMPLEMENTATION (40 мин)**

### **⚙️ SOAR Platform Architecture (15 мин)**

**SOAR Framework Fundamentals:**

```
SOAR Platform Architecture:
┌─────────────────────────────────────────────────────────────┐
│                    Orchestration Layer                     │
│  ├── Workflow Engine                                       │
│  │   ├── Visual workflow designer                         │
│  │   ├── Business process management (BPM)                │
│  │   ├── Decision tree automation                         │
│  │   ├── Conditional logic processing                     │
│  │   └── Parallel task execution                          │
│  ├── Playbook Management                                   │
│  │   ├── Incident response playbooks                      │
│  │   ├── Threat hunting playbooks                         │
│  │   ├── Compliance automation playbooks                  │
│  │   ├── Version control и testing                        │
│  │   └── Playbook performance analytics                   │
│  └── Case Management                                       │
│      ├── Incident tracking и lifecycle                    │
│      ├── Evidence collection и preservation               │
│      ├── Task assignment и escalation                     │
│      ├── Collaboration и communication                    │
│      └── Reporting и metrics dashboard                    │
├─────────────────────────────────────────────────────────────┤
│                     Integration Layer                      │
│  ├── Security Tool Integration                             │
│  │   ├── SIEM platforms (Splunk, QRadar, ArcSight)       │
│  │   ├── Endpoint protection (CrowdStrike, SentinelOne)  │
│  │   ├── Network security (Palo Alto, Cisco, Fortinet)   │
│  │   ├── Threat intelligence (Recorded Future, ThreatQ)  │
│  │   ├── Vulnerability management (Nessus, Qualys)       │
│  │   └── Identity management (Active Directory, Okta)    │
│  ├── IT Service Management                                 │
│  │   ├── Ticketing systems (ServiceNow, Jira, Remedy)    │
│  │   ├── Asset management (Lansweeper, Device42)         │
│  │   ├── Configuration management (Ansible, Puppet)      │
│  │   ├── Monitoring systems (Nagios, PRTG, SolarWinds)   │
│  │   └── Communication platforms (Slack, Teams, Email)   │
│  └── Cloud и DevOps Integration                           │
│      ├── Cloud platforms (AWS, Azure, GCP)               │
│      ├── Container orchestration (Kubernetes, Docker)    │
│      ├── CI/CD pipelines (Jenkins, GitLab, Azure DevOps) │
│      ├── Infrastructure as Code (Terraform, CloudFormation)│
│      └── Monitoring (Prometheus, Grafana, DataDog)       │
├─────────────────────────────────────────────────────────────┤
│                      Automation Layer                      │
│  ├── Response Actions                                      │
│  │   ├── Automated containment (isolation, blocking)     │
│  │   ├── Evidence collection (logs, memory, disk)        │
│  │   ├── Threat hunting automation                        │
│  │   ├── Remediation actions (patching, cleaning)        │
│  │   └── Recovery procedures (restoration, validation)   │
│  ├── Enrichment и Analysis                                │
│  │   ├── Threat intelligence lookup                       │
│  │   ├── Geolocation и reputation analysis               │
│  │   ├── Malware analysis automation                     │
│  │   ├── User и entity behavior analysis                 │
│  │   └── Risk scoring и prioritization                   │
│  └── Communication и Notification                         │
│      ├── Stakeholder notification automation             │
│      ├── Escalation procedures                           │
│      ├── Status updates и reporting                      │
│      ├── Compliance notification                         │
│      └── External communication (customers, partners)    │
└─────────────────────────────────────────────────────────────┘
```

**SOAR Implementation Strategy:**
```python
# Enterprise SOAR Implementation Framework
class SOARImplementationFramework:
    def __init__(self):
        self.workflow_engine = WorkflowEngine()
        self.integration_manager = IntegrationManager()
        self.playbook_library = PlaybookLibrary()
        self.automation_engine = AutomationEngine()
        
    def design_soar_architecture(self, organization_requirements):
        """Design SOAR architecture based on organizational requirements"""
        architecture = SOARArchitecture()
        
        # Assess current environment
        current_tools = organization_requirements.existing_security_tools
        integration_complexity = self.assess_integration_complexity(current_tools)
        
        # Design integration strategy
        if integration_complexity == 'high':
            architecture.integration_approach = 'phased_integration'
            architecture.integration_phases = self.design_phased_integration(current_tools)
        else:
            architecture.integration_approach = 'comprehensive_integration'
            architecture.integrations = self.design_comprehensive_integration(current_tools)
        
        # Design workflow architecture
        architecture.workflow_design = self.design_workflow_architecture(organization_requirements)
        
        # Design automation capabilities
        architecture.automation_capabilities = self.design_automation_capabilities(organization_requirements)
        
        # Design scalability framework
        architecture.scalability_design = self.design_scalability_framework(organization_requirements)
        
        return architecture
    
    def implement_core_playbooks(self, incident_types):
        """Implement core incident response playbooks"""
        core_playbooks = {}
        
        for incident_type in incident_types:
            playbook = self.create_incident_playbook(incident_type)
            core_playbooks[incident_type] = playbook
        
        # Add cross-cutting playbooks
        core_playbooks['threat_enrichment'] = self.create_threat_enrichment_playbook()
        core_playbooks['evidence_collection'] = self.create_evidence_collection_playbook()
        core_playbooks['communication'] = self.create_communication_playbook()
        
        return core_playbooks
    
    def create_incident_playbook(self, incident_type):
        """Create incident-specific response playbook"""
        if incident_type == 'malware_infection':
            return self.create_malware_response_playbook()
        elif incident_type == 'phishing_attack':
            return self.create_phishing_response_playbook()
        elif incident_type == 'data_breach':
            return self.create_data_breach_playbook()
        elif incident_type == 'ddos_attack':
            return self.create_ddos_response_playbook()
        elif incident_type == 'insider_threat':
            return self.create_insider_threat_playbook()
        else:
            return self.create_generic_incident_playbook(incident_type)
    
    def create_malware_response_playbook(self):
        """Create comprehensive malware response playbook"""
        playbook = SOARPlaybook(
            name='Malware_Incident_Response',
            version='2.1',
            description='Automated response to malware infections',
            trigger_conditions=['malware_detected', 'suspicious_file_execution']
        )
        
        # Phase 1: Initial Assessment
        playbook.add_phase(PlaybookPhase(
            name='initial_assessment',
            description='Assess malware incident scope и impact',
            tasks=[
                PlaybookTask(
                    name='collect_initial_indicators',
                    action='enrichment.collect_file_indicators',
                    inputs=['file_hash', 'file_path', 'process_name'],
                    outputs=['file_reputation', 'malware_family', 'threat_score']
                ),
                PlaybookTask(
                    name='assess_system_impact',
                    action='endpoint.assess_infection_scope',
                    inputs=['infected_hosts', 'malware_indicators'],
                    outputs=['affected_systems', 'lateral_movement_risk']
                ),
                PlaybookTask(
                    name='determine_response_priority',
                    action='risk.calculate_incident_priority',
                    inputs=['threat_score', 'affected_systems', 'business_impact'],
                    outputs=['incident_priority', 'response_urgency']
                )
            ]
        ))
        
        # Phase 2: Containment
        playbook.add_phase(PlaybookPhase(
            name='containment',
            description='Contain malware spread и isolate affected systems',
            tasks=[
                PlaybookTask(
                    name='isolate_infected_hosts',
                    action='endpoint.isolate_hosts',
                    inputs=['affected_systems'],
                    outputs=['isolation_status', 'isolation_errors'],
                    conditions=['incident_priority >= high']
                ),
                PlaybookTask(
                    name='block_malicious_indicators',
                    action='network.block_indicators',
                    inputs=['malware_indicators', 'iocs'],
                    outputs=['blocking_status', 'blocked_indicators']
                ),
                PlaybookTask(
                    name='disable_user_accounts',
                    action='identity.disable_accounts',
                    inputs=['compromised_accounts'],
                    outputs=['disabled_accounts'],
                    conditions=['lateral_movement_risk == high']
                )
            ]
        ))
        
        # Phase 3: Investigation
        playbook.add_phase(PlaybookPhase(
            name='investigation',
            description='Investigate malware infection details',
            tasks=[
                PlaybookTask(
                    name='collect_forensic_evidence',
                    action='forensics.collect_evidence',
                    inputs=['affected_systems', 'evidence_types'],
                    outputs=['forensic_artifacts', 'timeline_data']
                ),
                PlaybookTask(
                    name='analyze_malware_sample',
                    action='malware_analysis.submit_sample',
                    inputs=['malware_sample', 'analysis_type'],
                    outputs=['malware_capabilities', 'attack_vectors', 'attribution']
                ),
                PlaybookTask(
                    name='trace_attack_timeline',
                    action='investigation.build_timeline',
                    inputs=['timeline_data', 'log_sources'],
                    outputs=['attack_timeline', 'entry_vector', 'persistence_mechanisms']
                )
            ]
        ))
        
        # Phase 4: Eradication
        playbook.add_phase(PlaybookPhase(
            name='eradication',
            description='Remove malware и close attack vectors',
            tasks=[
                PlaybookTask(
                    name='remove_malware_artifacts',
                    action='endpoint.remove_malware',
                    inputs=['malware_artifacts', 'affected_systems'],
                    outputs=['removal_status', 'persistent_artifacts']
                ),
                PlaybookTask(
                    name='patch_vulnerabilities',
                    action='vulnerability.apply_patches',
                    inputs=['exploited_vulnerabilities', 'affected_systems'],
                    outputs=['patching_status', 'patch_errors']
                ),
                PlaybookTask(
                    name='update_security_controls',
                    action='security.update_controls',
                    inputs=['attack_vectors', 'security_gaps'],
                    outputs=['updated_controls', 'new_detection_rules']
                )
            ]
        ))
        
        # Phase 5: Recovery
        playbook.add_phase(PlaybookPhase(
            name='recovery',
            description='Restore systems и validate security',
            tasks=[
                PlaybookTask(
                    name='restore_system_functionality',
                    action='system.restore_services',
                    inputs=['affected_systems', 'service_dependencies'],
                    outputs=['restoration_status', 'service_health']
                ),
                PlaybookTask(
                    name='validate_system_integrity',
                    action='validation.check_system_integrity',
                    inputs=['restored_systems', 'integrity_baselines'],
                    outputs=['integrity_status', 'validation_results']
                ),
                PlaybookTask(
                    name='monitor_for_reinfection',
                    action='monitoring.enhanced_monitoring',
                    inputs=['recovered_systems', 'malware_indicators'],
                    outputs=['monitoring_status', 'detection_rules']
                )
            ]
        ))
        
        # Phase 6: Lessons Learned
        playbook.add_phase(PlaybookPhase(
            name='lessons_learned',
            description='Document lessons learned и improve defenses',
            tasks=[
                PlaybookTask(
                    name='document_incident',
                    action='documentation.create_incident_report',
                    inputs=['incident_data', 'investigation_results', 'response_actions'],
                    outputs=['incident_report', 'documentation_status']
                ),
                PlaybookTask(
                    name='update_detection_capabilities',
                    action='detection.improve_rules',
                    inputs=['new_indicators', 'attack_patterns'],
                    outputs=['updated_detection_rules', 'enhanced_monitoring']
                ),
                PlaybookTask(
                    name='conduct_post_incident_review',
                    action='review.conduct_pir',
                    inputs=['incident_response_timeline', 'effectiveness_metrics'],
                    outputs=['improvement_recommendations', 'process_updates']
                )
            ]
        ))
        
        return playbook
```

### **🔄 Workflow Automation Design (12 мин)**

**Advanced Workflow Patterns:**

**Decision-Based Automation:**
```python
# Advanced Workflow Automation Engine
class WorkflowAutomationEngine:
    def __init__(self):
        self.decision_engine = DecisionEngine()
        self.execution_engine = ExecutionEngine()
        self.condition_evaluator = ConditionEvaluator()
        self.parallel_executor = ParallelExecutor()
        
    def implement_conditional_workflows(self, workflow_definitions):
        """Implement conditional workflow logic"""
        conditional_workflows = {}
        
        for workflow_def in workflow_definitions:
            workflow = ConditionalWorkflow(workflow_def.name)
            
            # Add decision points
            for decision_point in workflow_def.decision_points:
                decision_node = self.create_decision_node(decision_point)
                workflow.add_decision_node(decision_node)
            
            # Add conditional branches
            for branch in workflow_def.conditional_branches:
                branch_workflow = self.create_conditional_branch(branch)
                workflow.add_branch(branch.condition, branch_workflow)
            
            # Add exception handling
            exception_handlers = self.create_exception_handlers(workflow_def.exception_scenarios)
            workflow.add_exception_handlers(exception_handlers)
            
            conditional_workflows[workflow_def.name] = workflow
        
        return conditional_workflows
    
    def create_decision_node(self, decision_point):
        """Create intelligent decision node"""
        decision_node = DecisionNode(decision_point.name)
        
        # Define decision criteria
        for criterion in decision_point.criteria:
            decision_rule = DecisionRule(
                condition=criterion.condition,
                weight=criterion.weight,
                evaluation_method=criterion.evaluation_method
            )
            decision_node.add_rule(decision_rule)
        
        # Define decision outcomes
        for outcome in decision_point.outcomes:
            decision_outcome = DecisionOutcome(
                name=outcome.name,
                actions=outcome.actions,
                confidence_threshold=outcome.confidence_threshold
            )
            decision_node.add_outcome(decision_outcome)
        
        # Configure machine learning decision support
        if decision_point.enable_ml_support:
            ml_model = self.load_decision_ml_model(decision_point.ml_model_type)
            decision_node.set_ml_model(ml_model)
        
        return decision_node
    
    def implement_parallel_execution(self, workflow_tasks):
        """Implement parallel task execution"""
        parallel_groups = self.identify_parallel_groups(workflow_tasks)
        
        execution_plan = ParallelExecutionPlan()
        
        for group in parallel_groups:
            if group.can_execute_parallel:
                parallel_task_group = ParallelTaskGroup(
                    tasks=group.tasks,
                    max_concurrency=group.max_concurrency,
                    timeout=group.timeout,
                    failure_handling=group.failure_handling
                )
                execution_plan.add_parallel_group(parallel_task_group)
            else:
                sequential_task_group = SequentialTaskGroup(
                    tasks=group.tasks,
                    stop_on_failure=group.stop_on_failure
                )
                execution_plan.add_sequential_group(sequential_task_group)
        
        return execution_plan
    
    def implement_dynamic_workflows(self, base_workflow, dynamic_conditions):
        """Implement dynamic workflow adaptation"""
        dynamic_workflow = DynamicWorkflow(base_workflow)
        
        # Add dynamic adaptation rules
        for condition in dynamic_conditions:
            adaptation_rule = AdaptationRule(
                trigger_condition=condition.trigger,
                adaptation_action=condition.adaptation,
                confidence_threshold=condition.threshold
            )
            dynamic_workflow.add_adaptation_rule(adaptation_rule)
        
        # Configure real-time adaptation
        dynamic_workflow.enable_real_time_adaptation(
            monitoring_interval=30,  # seconds
            adaptation_cooldown=300,  # seconds
            max_adaptations_per_hour=10
        )
        
        return dynamic_workflow
    
    def create_workflow_templates(self, incident_categories):
        """Create reusable workflow templates"""
        templates = {}
        
        for category in incident_categories:
            template = WorkflowTemplate(
                name=f"{category}_response_template",
                category=category,
                base_phases=self.get_standard_phases(category),
                customizable_parameters=self.get_customizable_parameters(category)
            )
            
            # Add template variations
            for severity in ['low', 'medium', 'high', 'critical']:
                variation = self.create_severity_variation(template, severity)
                template.add_variation(severity, variation)
            
            # Add organizational customizations
            customizations = self.get_organizational_customizations(category)
            template.add_customizations(customizations)
            
            templates[category] = template
        
        return templates
```

### **📊 SOAR Performance Metrics (13 мин)**

**Comprehensive SOAR Analytics:**

**Performance Measurement Framework:**
```python
# SOAR Performance Analytics Engine
class SOARPerformanceAnalytics:
    def __init__(self):
        self.metrics_collector = MetricsCollector()
        self.performance_analyzer = PerformanceAnalyzer()
        self.efficiency_calculator = EfficiencyCalculator()
        self.roi_analyzer = ROIAnalyzer()
        
    def calculate_automation_metrics(self, time_period):
        """Calculate comprehensive automation metrics"""
        automation_metrics = AutomationMetrics()
        
        # Automation coverage metrics
        automation_metrics.coverage = self.calculate_automation_coverage(time_period)
        
        # Automation effectiveness metrics
        automation_metrics.effectiveness = self.calculate_automation_effectiveness(time_period)
        
        # Time savings metrics
        automation_metrics.time_savings = self.calculate_time_savings(time_period)
        
        # Error reduction metrics
        automation_metrics.error_reduction = self.calculate_error_reduction(time_period)
        
        # Cost savings metrics
        automation_metrics.cost_savings = self.calculate_cost_savings(time_period)
        
        return automation_metrics
    
    def calculate_automation_coverage(self, time_period):
        """Calculate automation coverage across different areas"""
        total_incidents = self.get_total_incidents(time_period)
        automated_incidents = self.get_automated_incidents(time_period)
        
        coverage_metrics = AutomationCoverageMetrics()
        
        # Overall automation coverage
        coverage_metrics.overall_coverage = len(automated_incidents) / len(total_incidents) if total_incidents else 0
        
        # Coverage by incident type
        incident_types = set([inc.type for inc in total_incidents])
        for inc_type in incident_types:
            type_total = len([inc for inc in total_incidents if inc.type == inc_type])
            type_automated = len([inc for inc in automated_incidents if inc.type == inc_type])
            coverage_metrics.coverage_by_type[inc_type] = type_automated / type_total if type_total else 0
        
        # Coverage by severity
        severities = ['low', 'medium', 'high', 'critical']
        for severity in severities:
            sev_total = len([inc for inc in total_incidents if inc.severity == severity])
            sev_automated = len([inc for inc in automated_incidents if inc.severity == severity])
            coverage_metrics.coverage_by_severity[severity] = sev_automated / sev_total if sev_total else 0
        
        # Automation depth analysis
        coverage_metrics.automation_depth = self.calculate_automation_depth(automated_incidents)
        
        return coverage_metrics
    
    def calculate_response_time_improvements(self, time_period):
        """Calculate response time improvements from automation"""
        manual_responses = self.get_manual_responses(time_period)
        automated_responses = self.get_automated_responses(time_period)
        
        time_improvements = ResponseTimeImprovements()
        
        # Mean time to detection (MTTD) improvement
        manual_mttd = np.mean([resp.time_to_detection for resp in manual_responses])
        automated_mttd = np.mean([resp.time_to_detection for resp in automated_responses])
        time_improvements.mttd_improvement = (manual_mttd - automated_mttd) / manual_mttd if manual_mttd else 0
        
        # Mean time to response (MTTR) improvement
        manual_mttr = np.mean([resp.time_to_response for resp in manual_responses])
        automated_mttr = np.mean([resp.time_to_response for resp in automated_responses])
        time_improvements.mttr_improvement = (manual_mttr - automated_mttr) / manual_mttr if manual_mttr else 0
        
        # Mean time to containment (MTTC) improvement
        manual_mttc = np.mean([resp.time_to_containment for resp in manual_responses])
        automated_mttc = np.mean([resp.time_to_containment for resp in automated_responses])
        time_improvements.mttc_improvement = (manual_mttc - automated_mttc) / manual_mttc if manual_mttc else 0
        
        # Mean time to resolution (MTTR) improvement
        manual_mttr_resolution = np.mean([resp.time_to_resolution for resp in manual_responses])
        automated_mttr_resolution = np.mean([resp.time_to_resolution for resp in automated_responses])
        time_improvements.resolution_improvement = (manual_mttr_resolution - automated_mttr_resolution) / manual_mttr_resolution if manual_mttr_resolution else 0
        
        return time_improvements
    
    def calculate_soar_roi(self, implementation_costs, operational_period):
        """Calculate comprehensive SOAR return on investment"""
        roi_analysis = SOARROIAnalysis()
        
        # Implementation costs
        roi_analysis.implementation_costs = ImplementationCosts(
            software_licensing=implementation_costs.software_licensing,
            professional_services=implementation_costs.professional_services,
            hardware_infrastructure=implementation_costs.hardware_infrastructure,
            training_costs=implementation_costs.training_costs,
            internal_resources=implementation_costs.internal_resources
        )
        
        # Operational benefits
        operational_benefits = self.calculate_operational_benefits(operational_period)
        roi_analysis.operational_benefits = operational_benefits
        
        # Cost savings
        cost_savings = self.calculate_cost_savings(operational_period)
        roi_analysis.cost_savings = cost_savings
        
        # Risk reduction value
        risk_reduction_value = self.calculate_risk_reduction_value(operational_period)
        roi_analysis.risk_reduction_value = risk_reduction_value
        
        # Calculate ROI metrics
        total_benefits = (operational_benefits.total_value + 
                         cost_savings.total_savings + 
                         risk_reduction_value.total_value)
        total_costs = sum(implementation_costs.__dict__.values())
        
        roi_analysis.roi_percentage = ((total_benefits - total_costs) / total_costs) * 100
        roi_analysis.payback_period_months = total_costs / (total_benefits / 12) if total_benefits > 0 else float('inf')
        roi_analysis.net_present_value = self.calculate_npv(total_benefits, total_costs, operational_period)
        
        return roi_analysis
    
    def generate_soar_dashboard(self, time_period):
        """Generate comprehensive SOAR performance dashboard"""
        dashboard_data = SOARDashboardData()
        
        # Key performance indicators
        dashboard_data.kpis = {
            'automation_coverage': self.calculate_automation_coverage(time_period).overall_coverage,
            'average_response_time': self.calculate_average_response_time(time_period),
            'false_positive_rate': self.calculate_false_positive_rate(time_period),
            'playbook_success_rate': self.calculate_playbook_success_rate(time_period),
            'analyst_productivity_improvement': self.calculate_productivity_improvement(time_period)
        }
        
        # Automation effectiveness charts
        dashboard_data.charts = {
            'automation_trends': self.generate_automation_trend_chart(time_period),
            'response_time_comparison': self.generate_response_time_chart(time_period),
            'incident_volume_automation': self.generate_incident_volume_chart(time_period),
            'cost_savings_timeline': self.generate_cost_savings_chart(time_period),
            'playbook_performance': self.generate_playbook_performance_chart(time_period)
        }
        
        # Executive summary
        dashboard_data.executive_summary = self.generate_executive_summary(time_period)
        
        # Recommendations
        dashboard_data.recommendations = self.generate_improvement_recommendations(time_period)
        
        return dashboard_data
    
    def optimize_soar_performance(self, performance_data):
        """Optimize SOAR performance based on analytics"""
        optimization_recommendations = []
        
        # Analyze playbook performance
        underperforming_playbooks = [pb for pb in performance_data.playbooks if pb.success_rate < 0.8]
        for playbook in underperforming_playbooks:
            optimization = PlaybookOptimization(
                playbook_name=playbook.name,
                current_success_rate=playbook.success_rate,
                identified_issues=self.analyze_playbook_issues(playbook),
                recommended_improvements=self.recommend_playbook_improvements(playbook)
            )
            optimization_recommendations.append(optimization)
        
        # Analyze integration performance
        slow_integrations = [integ for integ in performance_data.integrations if integ.avg_response_time > 30]
        for integration in slow_integrations:
            optimization = IntegrationOptimization(
                integration_name=integration.name,
                current_performance=integration.avg_response_time,
                bottleneck_analysis=self.analyze_integration_bottlenecks(integration),
                optimization_suggestions=self.suggest_integration_optimizations(integration)
            )
            optimization_recommendations.append(optimization)
        
        # Analyze resource utilization
        resource_optimization = self.analyze_resource_utilization(performance_data)
        optimization_recommendations.append(resource_optimization)
        
        return SOAROptimizationReport(
            recommendations=optimization_recommendations,
            expected_improvements=self.calculate_expected_improvements(optimization_recommendations),
            implementation_priority=self.prioritize_optimizations(optimization_recommendations)
        )
```

---

## 📚 **ЧАС 3: INCIDENT RESPONSE WORKFLOWS И AUTOMATED REMEDIATION (40 мин)**

### **🎯 Automated Incident Response Framework (18 мин)**

**Comprehensive IR Automation:**

**Incident Classification и Routing:**
```python
# Automated Incident Response System
class AutomatedIncidentResponse:
    def __init__(self):
        self.classification_engine = IncidentClassificationEngine()
        self.routing_engine = IncidentRoutingEngine()
        self.escalation_manager = EscalationManager()
        self.remediation_engine = RemediationEngine()
        
    def implement_automated_classification(self, incident_data):
        """Implement automated incident classification"""
        classification_result = IncidentClassificationResult()
        
        # Multi-stage classification process
        # Stage 1: Basic categorization
        basic_category = self.classify_basic_category(incident_data)
        classification_result.basic_category = basic_category
        
        # Stage 2: Severity assessment
        severity_assessment = self.assess_incident_severity(incident_data, basic_category)
        classification_result.severity = severity_assessment.severity
        classification_result.severity_confidence = severity_assessment.confidence
        
        # Stage 3: Attack vector identification
        attack_vectors = self.identify_attack_vectors(incident_data)
        classification_result.attack_vectors = attack_vectors
        
        # Stage 4: Asset impact assessment
        asset_impact = self.assess_asset_impact(incident_data)
        classification_result.asset_impact = asset_impact
        
        # Stage 5: Business impact assessment
        business_impact = self.assess_business_impact(incident_data, asset_impact)
        classification_result.business_impact = business_impact
        
        # Stage 6: Threat actor attribution
        attribution = self.perform_threat_attribution(incident_data)
        classification_result.attribution = attribution
        
        # Calculate overall classification confidence
        classification_result.overall_confidence = self.calculate_classification_confidence(classification_result)
        
        return classification_result
    
    def classify_basic_category(self, incident_data):
        """Classify incident into basic category"""
        classification_models = {
            'malware': MalwareClassificationModel(),
            'phishing': PhishingClassificationModel(),
            'data_breach': DataBreachClassificationModel(),
            'ddos': DDoSClassificationModel(),
            'insider_threat': InsiderThreatClassificationModel(),
            'system_compromise': SystemCompromiseClassificationModel()
        }
        
        category_scores = {}
        
        for category, model in classification_models.items():
            score = model.calculate_probability(incident_data)
            category_scores[category] = score
        
        # Select highest scoring category
        best_category = max(category_scores, key=category_scores.get)
        confidence = category_scores[best_category]
        
        return BasicCategoryResult(
            category=best_category,
            confidence=confidence,
            all_scores=category_scores
        )
    
    def assess_incident_severity(self, incident_data, category):
        """Assess incident severity using multiple factors"""
        severity_factors = SeverityFactors()
        
        # Technical impact factors
        severity_factors.technical_impact = self.assess_technical_impact(incident_data)
        
        # Business impact factors
        severity_factors.business_impact = self.assess_business_impact_factors(incident_data)
        
        # Asset criticality factors
        severity_factors.asset_criticality = self.assess_asset_criticality(incident_data)
        
        # Threat sophistication factors
        severity_factors.threat_sophistication = self.assess_threat_sophistication(incident_data)
        
        # Data sensitivity factors
        severity_factors.data_sensitivity = self.assess_data_sensitivity(incident_data)
        
        # Calculate weighted severity score
        severity_weights = {
            'technical_impact': 0.25,
            'business_impact': 0.30,
            'asset_criticality': 0.20,
            'threat_sophistication': 0.15,
            'data_sensitivity': 0.10
        }
        
        weighted_score = sum([
            getattr(severity_factors, factor) * weight
            for factor, weight in severity_weights.items()
        ])
        
        # Map score to severity level
        if weighted_score >= 0.8:
            severity_level = 'critical'
        elif weighted_score >= 0.6:
            severity_level = 'high'
        elif weighted_score >= 0.4:
            severity_level = 'medium'
        else:
            severity_level = 'low'
        
        return SeverityAssessment(
            severity=severity_level,
            score=weighted_score,
            factors=severity_factors,
            confidence=self.calculate_severity_confidence(severity_factors)
        )
    
    def implement_automated_routing(self, classified_incident):
        """Implement automated incident routing"""
        routing_decision = IncidentRoutingDecision()
        
        # Determine primary response team
        primary_team = self.determine_primary_team(classified_incident)
        routing_decision.primary_team = primary_team
        
        # Determine supporting teams
        supporting_teams = self.determine_supporting_teams(classified_incident)
        routing_decision.supporting_teams = supporting_teams
        
        # Determine escalation path
        escalation_path = self.determine_escalation_path(classified_incident)
        routing_decision.escalation_path = escalation_path
        
        # Determine response priority
        response_priority = self.determine_response_priority(classified_incident)
        routing_decision.response_priority = response_priority
        
        # Determine SLA requirements
        sla_requirements = self.determine_sla_requirements(classified_incident)
        routing_decision.sla_requirements = sla_requirements
        
        # Create incident ticket
        incident_ticket = self.create_incident_ticket(classified_incident, routing_decision)
        routing_decision.incident_ticket = incident_ticket
        
        # Notify assigned teams
        notification_result = self.notify_assigned_teams(routing_decision)
        routing_decision.notification_result = notification_result
        
        return routing_decision
    
    def implement_automated_remediation(self, incident):
        """Implement automated remediation actions"""
        remediation_plan = RemediationPlan(incident)
        
        # Assess remediation safety
        safety_assessment = self.assess_remediation_safety(incident)
        
        if safety_assessment.is_safe_for_automation:
            # Execute safe automated actions
            safe_actions = self.get_safe_automation_actions(incident)
            
            for action in safe_actions:
                execution_result = self.execute_remediation_action(action)
                remediation_plan.add_execution_result(action, execution_result)
                
                # Verify action success
                verification_result = self.verify_action_success(action, execution_result)
                remediation_plan.add_verification_result(action, verification_result)
        
        # Prepare manual remediation recommendations
        manual_actions = self.get_manual_remediation_actions(incident)
        remediation_plan.manual_actions = manual_actions
        
        # Create remediation documentation
        remediation_documentation = self.create_remediation_documentation(remediation_plan)

### **📊 Advanced IOC Development и Validation (20 мин)**

**IOC Lifecycle Management:**

**Comprehensive IOC Framework:**
```python
# Advanced IOC Development and Validation System
class AdvancedIOCManager:
    def __init__(self):
        self.ioc_repository = IOCRepository()
        self.validation_engine = IOCValidationEngine()
        self.scoring_system = IOCScoringSystem()
        self.distribution_manager = IOCDistributionManager()
        
    def develop_iocs_from_hunt(self, hunt_findings):
        """Develop high-quality IOCs from hunting findings"""
        candidate_iocs = self.extract_candidate_iocs(hunt_findings)
        validated_iocs = []
        
        for candidate in candidate_iocs:
            # Multi-stage validation
            validation_result = self.comprehensive_ioc_validation(candidate)
            
            if validation_result.is_valid:
                # Score the IOC
                ioc_score = self.score_ioc(candidate, validation_result)
                
                # Create enriched IOC
                enriched_ioc = self.create_enriched_ioc(candidate, validation_result, ioc_score)
                
                # Store in repository
                self.ioc_repository.store_ioc(enriched_ioc)
                
                validated_iocs.append(enriched_ioc)
        
        return validated_iocs
    
    def comprehensive_ioc_validation(self, candidate_ioc):
        """Perform comprehensive IOC validation"""
        validation_result = IOCValidationResult(ioc=candidate_ioc)
        
        # Technical validation
        technical_validation = self.perform_technical_validation(candidate_ioc)
        validation_result.add_validation('technical', technical_validation)
        
        # Context validation
        context_validation = self.perform_context_validation(candidate_ioc)
        validation_result.add_validation('context', context_validation)
        
        # Intelligence validation
        intelligence_validation = self.perform_intelligence_validation(candidate_ioc)
        validation_result.add_validation('intelligence', intelligence_validation)
        
        # False positive assessment
        false_positive_risk = self.assess_false_positive_risk(candidate_ioc)
        validation_result.add_validation('false_positive', false_positive_risk)
        
        # Temporal validation
        temporal_validation = self.perform_temporal_validation(candidate_ioc)
        validation_result.add_validation('temporal', temporal_validation)
        
        # Calculate overall validation score
        validation_result.overall_score = self.calculate_validation_score(validation_result)
        validation_result.is_valid = validation_result.overall_score > 0.7
        
        return validation_result
    
    def perform_technical_validation(self, ioc):
        """Perform technical validation of IOC"""
        validation = TechnicalValidation()
        
        if ioc.type == 'ip_address':
            validation = self.validate_ip_address(ioc)
        elif ioc.type == 'domain':
            validation = self.validate_domain(ioc)
        elif ioc.type == 'url':
            validation = self.validate_url(ioc)
        elif ioc.type == 'file_hash':
            validation = self.validate_file_hash(ioc)
        elif ioc.type == 'email':
            validation = self.validate_email(ioc)
        
        return validation
    
    def validate_ip_address(self, ioc):
        """Validate IP address IOC"""
        validation = TechnicalValidation()
        ip = ioc.value
        
        # Format validation
        validation.format_valid = self.is_valid_ip_format(ip)
        
        # Public/private address check
        validation.is_public = not self.is_private_ip(ip)
        
        # Geolocation validation
        geo_info = self.get_geolocation(ip)
        validation.geolocation_available = geo_info is not None
        
        # ASN validation
        asn_info = self.get_asn_info(ip)
        validation.asn_available = asn_info is not None
        validation.hosting_provider = asn_info.organization if asn_info else None
        
        # Active/inactive check
        validation.is_active = self.check_ip_activity(ip)
        
        # Reputation check
        reputation_scores = self.get_ip_reputation_scores(ip)
        validation.reputation_scores = reputation_scores
        validation.has_bad_reputation = any(score < 0.3 for score in reputation_scores.values())
        
        # Calculate technical score
        validation.score = self.calculate_ip_technical_score(validation)
        
        return validation
    
    def validate_domain(self, ioc):
        """Validate domain IOC"""
        validation = TechnicalValidation()
        domain = ioc.value
        
        # Format validation
        validation.format_valid = self.is_valid_domain_format(domain)
        
        # DNS resolution
        dns_info = self.resolve_domain(domain)
        validation.resolves = dns_info is not None
        validation.ip_addresses = dns_info.ip_addresses if dns_info else []
        
        # WHOIS information
        whois_info = self.get_whois_info(domain)
        validation.whois_available = whois_info is not None
        validation.domain_age = whois_info.creation_date if whois_info else None
        validation.registrar = whois_info.registrar if whois_info else None
        
        # Subdomain analysis
        validation.is_subdomain = '.' in domain and domain.count('.') > 1
        validation.parent_domain = self.get_parent_domain(domain) if validation.is_subdomain else None
        
        # DGA detection
        validation.dga_score = self.calculate_dga_score(domain)
        validation.likely_dga = validation.dga_score > 0.7
        
        # Certificate information
        cert_info = self.get_certificate_info(domain)
        validation.has_valid_certificate = cert_info is not None and cert_info.is_valid
        
        # Calculate technical score
        validation.score = self.calculate_domain_technical_score(validation)
        
        return validation
    
    def assess_false_positive_risk(self, ioc):
        """Assess false positive risk for IOC"""
        risk_assessment = FalsePositiveRisk()
        
        # Check against whitelist databases
        whitelist_matches = self.check_whitelist_databases(ioc)
        risk_assessment.whitelist_matches = whitelist_matches
        
        # Legitimate service check
        is_legitimate = self.check_legitimate_services(ioc)
        risk_assessment.is_legitimate_service = is_legitimate
        
        # Popularity assessment
        popularity_score = self.assess_popularity(ioc)
        risk_assessment.popularity_score = popularity_score
        
        # CDN/Cloud service check
        is_cloud_service = self.check_cloud_services(ioc)
        risk_assessment.is_cloud_service = is_cloud_service
        
        # Historical false positive rate
        historical_fp_rate = self.get_historical_fp_rate(ioc)
        risk_assessment.historical_fp_rate = historical_fp_rate
        
        # Calculate overall false positive risk
        risk_assessment.overall_risk = self.calculate_fp_risk_score(risk_assessment)
        
        return risk_assessment
    
    def score_ioc(self, ioc, validation_result):
        """Score IOC based on multiple factors"""
        scoring_factors = {
            'technical_validity': validation_result.validations['technical'].score * 0.25,
            'intelligence_correlation': validation_result.validations['intelligence'].score * 0.30,
            'context_relevance': validation_result.validations['context'].score * 0.20,
            'false_positive_risk': (1 - validation_result.validations['false_positive'].overall_risk) * 0.15,
            'temporal_relevance': validation_result.validations['temporal'].score * 0.10
        }
        
        # Calculate weighted score
        total_score = sum(scoring_factors.values())
        
        # Apply bonus factors
        bonus_factors = self.calculate_bonus_factors(ioc, validation_result)
        total_score += bonus_factors
        
        # Apply penalty factors
        penalty_factors = self.calculate_penalty_factors(ioc, validation_result)
        total_score -= penalty_factors
        
        # Normalize to 0-1 range
        final_score = max(0, min(1, total_score))
        
        return IOCScore(
            overall_score=final_score,
            component_scores=scoring_factors,
            bonus_factors=bonus_factors,
            penalty_factors=penalty_factors,
            scoring_explanation=self.generate_scoring_explanation(scoring_factors, bonus_factors, penalty_factors)
        )
    
    def create_enriched_ioc(self, candidate_ioc, validation_result, ioc_score):
        """Create enriched IOC with all metadata"""
        enriched_ioc = EnrichedIOC(
            value=candidate_ioc.value,
            type=candidate_ioc.type,
            source_hunt=candidate_ioc.source_hunt,
            discovery_timestamp=candidate_ioc.discovery_timestamp
        )
        
        # Add validation metadata
        enriched_ioc.validation_result = validation_result
        enriched_ioc.score = ioc_score
        
        # Add threat intelligence context
        ti_context = self.get_threat_intelligence_context(candidate_ioc)
        enriched_ioc.threat_intelligence = ti_context
        
        # Add attribution information
        attribution = self.perform_ioc_attribution(candidate_ioc)
        enriched_ioc.attribution = attribution
        
        # Add relationships
        related_iocs = self.find_related_iocs(candidate_ioc)
        enriched_ioc.related_iocs = related_iocs
        
        # Add deployment recommendations
        deployment_recommendations = self.generate_deployment_recommendations(enriched_ioc)
        enriched_ioc.deployment_recommendations = deployment_recommendations
        
        # Add expiration information
        expiration_info = self.calculate_ioc_expiration(enriched_ioc)
        enriched_ioc.expiration_info = expiration_info
        
        return enriched_ioc
    
    def generate_deployment_recommendations(self, enriched_ioc):
        """Generate deployment recommendations for IOC"""
        recommendations = DeploymentRecommendations()
        
        # Blocking recommendations
        if enriched_ioc.score.overall_score > 0.8:
            recommendations.blocking_action = 'immediate_block'
            recommendations.blocking_confidence = 'high'
        elif enriched_ioc.score.overall_score > 0.6:
            recommendations.blocking_action = 'monitor_and_alert'
            recommendations.blocking_confidence = 'medium'
        else:
            recommendations.blocking_action = 'monitor_only'
            recommendations.blocking_confidence = 'low'
        
        # Deployment targets
        if enriched_ioc.type in ['ip_address', 'domain']:
            recommendations.deployment_targets = ['firewall', 'dns_sinkhole', 'proxy', 'siem']
        elif enriched_ioc.type == 'file_hash':
            recommendations.deployment_targets = ['endpoint_protection', 'email_gateway', 'siem']
        elif enriched_ioc.type == 'url':
            recommendations.deployment_targets = ['web_gateway', 'proxy', 'email_gateway', 'siem']
        
        # Monitoring recommendations
        recommendations.monitoring_duration = self.calculate_monitoring_duration(enriched_ioc)
        recommendations.alert_severity = self.determine_alert_severity(enriched_ioc)
        
        # Integration recommendations
        recommendations.siem_integration = True
        recommendations.threat_hunting_priority = self.calculate_hunting_priority(enriched_ioc)
        recommendations.sharing_classification = self.determine_sharing_classification(enriched_ioc)
        
        return recommendations
    
    def manage_ioc_lifecycle(self):
        """Manage IOC lifecycle including aging and retirement"""
        all_iocs = self.ioc_repository.get_all_active_iocs()
        
        for ioc in all_iocs:
            # Check for aging
            age_assessment = self.assess_ioc_age(ioc)
            
            # Check for continued relevance
            relevance_assessment = self.assess_ioc_relevance(ioc)
            
            # Check for false positive reports
            fp_reports = self.get_false_positive_reports(ioc)
            
            # Update IOC status
            if age_assessment.is_expired or relevance_assessment.is_irrelevant:
                self.retire_ioc(ioc, reason='expired_or_irrelevant')
            elif len(fp_reports) > 5:  # Too many false positive reports
                self.review_ioc(ioc, reason='high_false_positive_rate')
            elif relevance_assessment.confidence_degraded:
                self.downgrade_ioc(ioc, new_score=relevance_assessment.adjusted_score)
            
            # Log lifecycle action
            self.log_lifecycle_action(ioc, action_taken)
    
    def generate_ioc_intelligence_report(self, time_period):
        """Generate comprehensive IOC intelligence report"""
        report_data = {
            'new_iocs': self.ioc_repository.get_new_iocs(time_period),
            'high_confidence_iocs': self.ioc_repository.get_high_confidence_iocs(time_period),
            'trending_iocs': self.ioc_repository.get_trending_iocs(time_period),
            'retired_iocs': self.ioc_repository.get_retired_iocs(time_period),
            'false_positive_analysis': self.analyze_false_positives(time_period),
            'effectiveness_metrics': self.calculate_ioc_effectiveness(time_period)
        }
        
        # Generate insights
        insights = self.generate_ioc_insights(report_data)
        
        # Generate recommendations
        recommendations = self.generate_ioc_program_recommendations(report_data)
        
        return IOCIntelligenceReport(
            time_period=time_period,
            summary_statistics=self.calculate_summary_statistics(report_data),
            detailed_data=report_data,
            insights=insights,
            recommendations=recommendations,
            trending_analysis=self.perform_trending_analysis(report_data),
            quality_metrics=self.calculate_quality_metrics(report_data)
        )
```

---

## 🛠️ **ПРАКТИЧЕСКИ УПРАЖНЕНИЯ КОМПОНЕНТ 4.2**

### **🕵️ Упражнение 4.2.1: Hypothesis-Driven Threat Hunt (30 мин)**

**Scenario:** Enterprise network compromise investigation

**Hunt Hypothesis:** "Adversaries may be using living-off-the-land techniques to perform credential dumping and lateral movement in our Windows environment"

**Investigation Framework:**

**Phase 1: Hypothesis Development (8 мин)**
- Define testable predictions
- Identify required data sources
- Establish success criteria
- Create investigation timeline

**Phase 2: Data Collection и Analysis (15 мин)**
- Execute SIEM queries for suspicious activities
- Analyze endpoint telemetry for credential access
- Investigate network lateral movement patterns
- Correlate findings across data sources

**Phase 3: Results Validation (7 мин)**
- Validate findings against hypothesis
- Assess confidence levels
- Document evidence chain
- Generate actionable intelligence

**Deliverable:** Complete hunt report със findings, evidence, и recommendations

### **🎯 Упражнение 4.2.2: MITRE ATT&CK Campaign Tracking (25 мин)**

**Objective:** Track и analyze advanced persistent threat campaign

**Campaign Scenario:** Multi-stage APT campaign targeting financial sector

**Tracking Process:**

**Phase 1: Technique Identification (8 мин)**
- Map observed activities to MITRE ATT&CK
- Identify attack progression patterns
- Correlate techniques across timeline
- Build attack chain visualization

**Phase 2: Campaign Attribution (10 мин)**
- Compare TTPs to known threat actors
- Analyze infrastructure overlaps
- Assess tool и technique similarities
- Calculate attribution confidence

**Phase 3: Predictive Analysis (7 мин)**
- Predict next likely attack stages
- Identify potential targets
- Recommend defensive measures
- Create hunting priorities

**Deliverable:** Comprehensive campaign analysis със attribution assessment и predictive intelligence

### **🧠 Упражнение 4.2.3: Threat Intelligence Integration Workshop (20 мин)**

**Challenge:** Integrate multiple TI sources за enhanced hunting

**Intelligence Sources:**
- Commercial threat feeds
- Open source intelligence
- Government advisories
- Industry sharing groups
- Internal incident data

**Integration Tasks:**

**Phase 1: Intelligence Consumption (7 мин)**
- Configure automated feed ingestion
- Normalize data formats
- Resolve conflicts между sources
- Validate intelligence quality

**Phase 2: Correlation и Enrichment (8 мин)**
- Correlate indicators across sources
- Enrich IOCs със contextual data
- Score intelligence reliability
- Generate hunting priorities

**Phase 3: Operationalization (5 мин)**
- Create automated hunting rules
- Configure alert thresholds
- Set up distribution workflows
- Test integration effectiveness

**Deliverable:** Operational TI integration platform със automated hunting capabilities

### **📊 Упражнение 4.2.4: Advanced IOC Development и Validation (25 мин)**

**Project:** Develop high-quality IOCs from hunting findings

**IOC Development Requirements:**
- Multi-stage validation process
- Automated scoring system
- False positive mitigation
- Lifecycle management
- Distribution automation

**Development Process:**

**Phase 1: IOC Extraction (8 мин)**
- Extract candidate IOCs from hunt data
- Categorize by type и source
- Perform initial filtering
- Document extraction context

**Phase 2: Validation и Scoring (10 мин)**
- Execute technical validation
- Assess false positive risk
- Score reliability и relevance
- Add threat intelligence context

**Phase 3: Operationalization (7 мин)**
- Generate deployment recommendations
- Configure automated distribution
- Set up lifecycle management
- Test operational effectiveness

**Deliverable:** Production-ready IOC management system със comprehensive validation framework

---

## 📊 **ОЦЕНЯВАНЕ КОМПОНЕНТ 4.2**

### **📝 Формативно оценяване:**

**🧪 Тест 4.2: Threat Hunting и Intelligence (15 мин, 25 точки)**

**Практически задачи (15 точки):**
1. Develop hypothesis-driven hunting methodology (6 точки)
2. Map threat activity to MITRE ATT&CK framework (5 точки)
3. Integrate threat intelligence feeds за hunting enhancement (4 точки)

**Теоретични въпроси (10 точки):**
1. Explain threat hunting maturity model progression (3 точки)
2. Compare proactive hunting vs. reactive detection approaches (4 точки)
3. Describe IOC validation и scoring methodologies (3 точки)

### **🎯 Практическо оценяване:**

**💼 Проект 4.2: Advanced Threat Hunting Program (30 точки)**

**Рубрика за оценяване:**
- **Hunting Methodology (10 точки):** Hypothesis development, investigation framework, validation process
- **MITRE ATT&CK Integration (8 точки):** Technique mapping, campaign tracking, attribution analysis
- **Threat Intelligence Utilization (7 точки):** Multi-source integration, correlation, operationalization
- **IOC Management (5 точки):** Development process, validation framework, lifecycle management

**Критерии за успех:**
- **Отличен (27-30):** Industry-leading threat hunting program със innovation
- **Много добър (22-26):** Strong hunting capabilities с minor optimization opportunities
- **Добър (16-21):** Functional hunting approach с improvement potential
- **Среден (9-15):** Basic understanding requiring significant development

### **🏠 Домашна работа:**

**📋 Assignment 4.2: "Enterprise Threat Hunting Strategy" (Due next week)**

**Task:** Design comprehensive threat hunting program за large organization

**Requirements:**
1. **Program Development (40%)**
   - Threat hunting maturity roadmap
   - Hypothesis development methodology
   - Investigation framework design
   - Success metrics definition

2. **Technical Implementation (35%)**
   - MITRE ATT&CK integration strategy
   - Multi-source threat intelligence platform
   - Automated hunting capabilities
   - IOC management system

3. **Operational Framework (25%)**
   - Team structure и skills development
   - Hunting workflow automation
   - Reporting и communication strategy
   - Continuous improvement process

**Format:** Strategic program document (8-12 pages) със technical specifications, implementation timeline, и ROI analysis

---

---

### **🔍 КОМПОНЕНТ 4.2: THREAT HUNTING И PROACTIVE DETECTION (3 часа)**

#### **🎯 СПЕЦИФИЧНИ ЦЕЛИ НА КОМПОНЕНТА:**

**ЗНАНИЯ:**
- Разбиране на threat hunting methodologies и hypothesis-driven investigation approaches
- Познаване на MITRE ATT&CK framework integration и tactical threat analysis techniques
- Владеене на advanced threat intelligence utilization и IOC management strategies
- Знание за proactive detection techniques и behavioral hunting methodologies
- Разбиране на threat hunting tools ecosystem и investigation automation capabilities

**УМЕНИЯ:**
- Провеждане на comprehensive threat hunting sessions със systematic investigation approaches
- Имплементиране на MITRE ATT&CK-based hunting strategies и technique-focused searches
- Управление на threat intelligence feeds и automated IOC correlation
- Изпълнение на behavioral anomaly hunting и advanced pattern recognition
- Интегриране на threat hunting findings със incident response workflows

**КОМПЕТЕНТНОСТИ:**
- Analytical mindset за complex threat pattern identification и investigation
- Strategic thinking за proactive threat detection program development
- Technical expertise в cutting-edge hunting tools и methodologies
- Collaborative approach към cross-functional threat intelligence sharing
- Continuous learning commitment за emerging threat landscape adaptation

---

## 📚 **ЧАС 1: THREAT HUNTING METHODOLOGIES И HYPOTHESIS-DRIVEN INVESTIGATION (40 мин)**

### **🕵️ Threat Hunting Philosophy и Framework (15 мин)**

**Threat Hunting Definition и Purpose:**

Threat hunting е proactive approach към cybersecurity, където security analysts actively search за threats във network environment преди automated detection systems identify them. За разлика от reactive incident response, threat hunting е hypothesis-driven investigative process.

**Traditional Detection vs. Threat Hunting:**
```
Detection Approach Comparison:
┌─────────────────────────────────────────────────────────────┐
│                   Traditional Detection                    │
│  ├── Reactive approach - wait for alerts                  │
│  ├── Signature-based detection                            │
│  ├── Known threat patterns                                │
│  ├── Automated rule triggering                            │
│  └── Limited false positive tolerance                     │
├─────────────────────────────────────────────────────────────┤
│                    Threat Hunting                         │
│  ├── Proactive approach - actively search                 │
│  ├── Behavioral analysis focus                            │
│  ├── Unknown threat discovery                             │
│  ├── Human-driven investigation                           │
│  ├── Hypothesis-based exploration                         │
│  └── High tolerance for false positives                   │
└─────────────────────────────────────────────────────────────┘
```

**Threat Hunting Maturity Model:**
```
Hunting Maturity Progression:
├── Level 0: No Hunting Capability
│   ├── Purely reactive security operations
│   ├── Dependency on automated alerts only
│   ├── No proactive threat investigation
│   └── Limited threat awareness
├── Level 1: Initial Hunting (HMM1)
│   ├── Primarily data-driven searches
│   ├── Routine hunting activities
│   ├── Basic IOC searches
│   ├── Limited hypothesis development
│   └── Minimal threat intelligence integration
├── Level 2: Procedural Hunting (HMM2)
│   ├── Procedure-driven hunting activities
│   ├── Regular hunting schedules
│   ├── Standard hunting playbooks
│   ├── Threat intelligence consumption
│   └── Basic analytics utilization
├── Level 3: Innovative Hunting (HMM3)
│   ├── Creative hypothesis development
│   ├── Advanced analytics utilization
│   ├── Machine learning integration
│   ├── Threat intelligence production
│   └── Custom tool development
├── Level 4: Leading Hunting (HMM4)
│   ├── Automated hypothesis generation
│   ├── AI-assisted hunting operations
│   ├── Real-time adaptive hunting
│   ├── Industry threat intelligence sharing
│   └── Continuous hunting innovation
└── Level 5: Optimized Hunting (HMM5)
    ├── Fully integrated hunting ecosystem
    ├── Predictive threat modeling
    ├── Autonomous threat discovery
    ├── Global threat intelligence leadership
    └── Revolutionary hunting methodologies
```

**Hypothesis-Driven Hunting Process:**

**The Scientific Method Applied:**
```
Threat Hunting Scientific Method:
1. Observation
   ├── Environmental monitoring
   ├── Threat landscape awareness
   ├── Intelligence consumption
   └── Anomaly identification

2. Question Formation
   ├── "What if..." scenarios
   ├── Threat-based questions
   ├── Environmental assumptions
   └── Knowledge gap identification

3. Hypothesis Development
   ├── Testable predictions
   ├── Observable indicators
   ├── Data requirements
   └── Success criteria

4. Prediction
   ├── Expected findings
   ├── Indicator patterns
   ├── Behavioral expectations
   └── Timeline predictions

5. Testing
   ├── Data collection
   ├── Analysis execution
   ├── Pattern identification
   └── Evidence gathering

6. Analysis
   ├── Results evaluation
   ├── Hypothesis validation
   ├── False positive assessment
   └── Confidence determination

7. Conclusion
   ├── Findings documentation
   ├── Threat confirmation/denial
   ├── Lessons learned
   └── New hypothesis generation
```

**Practical Hypothesis Examples:**
```python
# Threat Hunting Hypothesis Framework
class ThreatHuntingHypothesis:
    def __init__(self, hypothesis_id, title, description):
        self.id = hypothesis_id
        self.title = title
        self.description = description
        self.threat_actors = []
        self.ttps = []  # Tactics, Techniques, Procedures
        self.data_sources = []
        self.expected_indicators = []
        self.testing_methods = []
        self.confidence_level = 0.0
        
    def add_threat_context(self, threat_actor, motivation, capability):
        """Add threat actor context to hypothesis"""
        self.threat_actors.append({
            'actor': threat_actor,
            'motivation': motivation,
            'capability': capability
        })
    
    def add_ttp(self, tactic, technique, procedure):
        """Add TTP to hypothesis"""
        self.ttps.append({
            'tactic': tactic,
            'technique': technique,
            'procedure': procedure,
            'mitre_id': self.get_mitre_id(technique)
        })
    
    def define_data_requirements(self, data_source, retention_period, query_method):
        """Define data requirements for hypothesis testing"""
        self.data_sources.append({
            'source': data_source,
            'retention': retention_period,
            'query_method': query_method,
            'criticality': 'high'
        })

# Example Hypotheses
hypothesis_lateral_movement = ThreatHuntingHypothesis(
    hypothesis_id="HUNT-001",
    title="Credential Dumping and Lateral Movement",
    description="Adversaries may be using credential dumping tools to harvest credentials and perform lateral movement via RDP or SMB"
)

hypothesis_lateral_movement.add_threat_context(
    threat_actor="APT29",
    motivation="Espionage and data theft",
    capability="Advanced persistent threat with sophisticated tools"
)

hypothesis_lateral_movement.add_ttp(
    tactic="Credential Access",
    technique="OS Credential Dumping",
    procedure="LSASS memory dumping using MimiKatz or similar tools"
)

hypothesis_lateral_movement.add_ttp(
    tactic="Lateral Movement", 
    technique="Remote Desktop Protocol",
    procedure="RDP connections to multiple systems using compromised credentials"
)

hypothesis_lateral_movement.define_data_requirements(
    data_source="Windows Event Logs",
    retention_period="90 days",
    query_method="SIEM query language"
)

hypothesis_lateral_movement.expected_indicators = [
    "Process creation events for credential dumping tools",
    "LSASS process access attempts",
    "Multiple RDP logons from single source",
    "SMB traffic to administrative shares",
    "Unusual authentication patterns"
]
```

### **🔬 Hunting Methodologies и Frameworks (12 мин)**

**NIST Cybersecurity Framework Hunting Integration:**

**Framework Alignment:**
```
NIST Framework Hunting Integration:
├── Identify (ID)
│   ├── Asset Discovery Hunting
│   │   ├── Unknown device identification
│   │   ├── Shadow IT discovery
│   │   ├── Unauthorized software detection
│   │   └── Network topology mapping
│   ├── Vulnerability Hunting
│   │   ├── Zero-day indicator searches
│   │   ├── Exploitation attempt detection
│   │   ├── Configuration weakness identification
│   │   └── Patch verification hunting
│   └── Risk Assessment Hunting
│       ├── Threat exposure analysis
│       ├── Attack surface evaluation
│       ├── Business impact assessment
│       └── Threat actor targeting analysis
├── Protect (PR)
│   ├── Control Effectiveness Hunting
│   │   ├── Security control bypass detection
│   │   ├── Policy violation identification
│   │   ├── Access control verification
│   │   └── Encryption implementation validation
│   └── Baseline Deviation Hunting
│       ├── Configuration drift detection
│       ├── Unauthorized changes identification
│       ├── Service degradation analysis
│       └── Performance anomaly investigation
├── Detect (DE)
│   ├── Threat Discovery Hunting
│   │   ├── Advanced persistent threat identification
│   │   ├── Living-off-the-land technique detection
│   │   ├── Insider threat behavioral analysis
│   │   └── Supply chain compromise investigation
│   └── Indicator Development Hunting
│       ├── IOC generation from findings
│       ├── Behavioral signature creation
│       ├── Pattern recognition improvement
│       └── Detection rule enhancement
├── Respond (RS)
│   ├── Incident Expansion Hunting
│   │   ├── Attack scope determination
│   │   ├── Additional victim identification
│   │   ├── Attribution evidence gathering
│   │   └── Timeline reconstruction
│   └── Threat Intelligence Hunting
│       ├── Campaign correlation analysis
│       ├── Actor attribution investigation
│       ├── TTP evolution tracking
│       └── Future threat prediction
└── Recover (RC)
    ├── Persistence Hunting
    │   ├── Backdoor identification
    │   ├── Dormant threat detection
    │   ├── Incomplete remediation validation
    │   └── Reinfection prevention verification
    └── Lessons Learned Hunting
        ├── Attack vector analysis
        ├── Detection gap identification
        ├── Response improvement opportunities
        └── Prevention strategy enhancement
```

**SQRRL Threat Hunting Framework:**

**Structured Hunting Approach:**
```python
# SQRRL Framework Implementation
class SQRRLHuntingFramework:
    def __init__(self):
        self.current_phase = None
        self.hunting_session = None
        
    def start_hunting_session(self, hypothesis):
        """Initialize hunting session using SQRRL framework"""
        self.hunting_session = HuntingSession(hypothesis)
        return self.execute_sqrrl_process()
    
    def execute_sqrrl_process(self):
        """Execute SQRRL hunting framework phases"""
        
        # Phase 1: Create Hypothesis
        hypothesis_phase = self.create_hypothesis_phase()
        
        # Phase 2: Investigate via Tools and Techniques
        investigation_phase = self.investigate_phase()
        
        # Phase 3: Uncover New Patterns and TTPs
        discovery_phase = self.uncover_patterns_phase()
        
        # Phase 4: Inform and Enrich Analytics
        analytics_phase = self.inform_analytics_phase()
        
        return HuntingResult(
            hypothesis=self.hunting_session.hypothesis,
            phases=[hypothesis_phase, investigation_phase, discovery_phase, analytics_phase],
            findings=self.hunting_session.findings,
            new_detections=self.hunting_session.new_detections,
            lessons_learned=self.hunting_session.lessons_learned
        )
    
    def create_hypothesis_phase(self):
        """SQRRL Phase 1: Create Hypothesis"""
        hypothesis_data = {
            'threat_intelligence': self.gather_threat_intelligence(),
            'environmental_factors': self.assess_environment(),
            'attack_surface': self.map_attack_surface(),
            'historical_incidents': self.review_historical_incidents()
        }
        
        # Develop testable hypothesis
        hypothesis = self.formulate_hypothesis(hypothesis_data)
        
        # Define success criteria
        success_criteria = self.define_success_criteria(hypothesis)
        
        return HuntingPhase(
            phase_name="Create Hypothesis",
            activities=["Threat Intelligence Gathering", "Environmental Assessment", 
                       "Attack Surface Mapping", "Historical Analysis"],
            outputs={"hypothesis": hypothesis, "success_criteria": success_criteria},
            duration=self.calculate_phase_duration("hypothesis")
        )
    
    def investigate_phase(self):
        """SQRRL Phase 2: Investigate via Tools and Techniques"""
        investigation_tools = {
            'siem_queries': self.prepare_siem_queries(),
            'endpoint_searches': self.prepare_endpoint_searches(),
            'network_analysis': self.prepare_network_analysis(),
            'threat_intelligence_lookups': self.prepare_ti_lookups()
        }
        
        # Execute investigations
        investigation_results = []
        for tool_type, queries in investigation_tools.items():
            results = self.execute_investigation_tool(tool_type, queries)
            investigation_results.extend(results)
        
        # Analyze findings
        analyzed_findings = self.analyze_investigation_results(investigation_results)
        
        return HuntingPhase(
            phase_name="Investigate",
            activities=["SIEM Analysis", "Endpoint Investigation", 
                       "Network Traffic Analysis", "Threat Intelligence Correlation"],
            outputs={"raw_findings": investigation_results, 
                    "analyzed_findings": analyzed_findings},
            duration=self.calculate_phase_duration("investigation")
        )
    
    def uncover_patterns_phase(self):
        """SQRRL Phase 3: Uncover New Patterns and TTPs"""
        pattern_analysis = {
            'temporal_patterns': self.analyze_temporal_patterns(),
            'behavioral_patterns': self.analyze_behavioral_patterns(),
            'network_patterns': self.analyze_network_patterns(),
            'file_patterns': self.analyze_file_patterns()
        }
        
        # Identify new TTPs
        new_ttps = self.identify_new_ttps(pattern_analysis)
        
        # Map to MITRE ATT&CK
        mitre_mappings = self.map_to_mitre_attack(new_ttps)
        
        # Generate IOCs
        generated_iocs = self.generate_iocs(pattern_analysis)
        
        return HuntingPhase(
            phase_name="Uncover Patterns",
            activities=["Pattern Analysis", "TTP Identification", 
                       "MITRE Mapping", "IOC Generation"],
            outputs={"patterns": pattern_analysis, "new_ttps": new_ttps,
                    "mitre_mappings": mitre_mappings, "iocs": generated_iocs},
            duration=self.calculate_phase_duration("pattern_discovery")
        )
    
    def inform_analytics_phase(self):
        """SQRRL Phase 4: Inform and Enrich Analytics"""
        analytics_improvements = {
            'detection_rules': self.create_detection_rules(),
            'hunting_queries': self.optimize_hunting_queries(),
            'threat_intelligence': self.enrich_threat_intelligence(),
            'behavioral_analytics': self.enhance_behavioral_analytics()
        }
        
        # Update detection systems
        detection_updates = self.update_detection_systems(analytics_improvements)
        
        # Document lessons learned
        lessons_learned = self.document_lessons_learned()
        
        # Plan future hunts
        future_hunt_recommendations = self.plan_future_hunts()
        
        return HuntingPhase(
            phase_name="Inform Analytics",
            activities=["Detection Rule Creation", "Query Optimization",
                       "Intelligence Enrichment", "System Updates"],
            outputs={"analytics_improvements": analytics_improvements,
                    "detection_updates": detection_updates,
                    "lessons_learned": lessons_learned,
                    "future_hunts": future_hunt_recommendations},
            duration=self.calculate_phase_duration("analytics_improvement")
        )
```

### **📊 Hunting Data Sources и Collection (13 мин)**

**Comprehensive Data Source Matrix:**

**Endpoint Data Sources:**
```
Endpoint Telemetry Collection:
├── Process Execution Data
│   ├── Process creation/termination events
│   ├── Command line arguments
│   ├── Parent-child process relationships
│   ├── Process injection detection
│   ├── Memory allocation patterns
│   ├── DLL loading activities
│   └── Service installation/modification
├── File System Activity
│   ├── File creation/modification/deletion
│   ├── Directory traversal patterns
│   ├── Temporary file usage
│   ├── Executable file analysis
│   ├── Document access patterns
│   ├── Hidden file detection
│   └── File attribute modifications
├── Registry Operations
│   ├── Registry key creation/modification
│   ├── Autorun registry entries
│   ├── Service registry modifications
│   ├── Policy registry changes
│   ├── User profile modifications
│   ├── Application-specific registry activity
│   └── System configuration changes
├── Network Connections
│   ├── Outbound connection attempts
│   ├── Listening port activities
│   ├── DNS query patterns
│   ├── HTTP/HTTPS traffic analysis
│   ├── Protocol usage statistics
│   ├── Data transfer volumes
│   └── Connection timing analysis
└── Authentication Events
    ├── Logon/logoff activities
    ├── Failed authentication attempts
    ├── Privilege escalation events
    ├── Account usage patterns
    ├── Remote authentication activities
    ├── Service account activities
    └── Interactive vs. non-interactive sessions
```

**Network Data Sources:**
```python
# Network Data Collection Framework
class NetworkDataCollector:
    def __init__(self):
        self.collection_points = {
            'network_flows': NetFlowCollector(),
            'dns_logs': DNSLogCollector(), 
            'proxy_logs': ProxyLogCollector(),
            'firewall_logs': FirewallLogCollector(),
            'packet_captures': PacketCaptureCollector()
        }
        self.enrichment_services = {
            'geolocation': GeolocationService(),
            'threat_intelligence': ThreatIntelligenceService(),
            'domain_reputation': DomainReputationService(),
            'ip_reputation': IPReputationService()
        }
    
    def collect_network_telemetry(self, time_range):
        """Collect comprehensive network telemetry"""
        network_data = {}
        
        # Collect flow data
        network_data['flows'] = self.collection_points['network_flows'].collect(
            time_range=time_range,
            include_metadata=True,
            enrich_with_reputation=True
        )
        
        # Collect DNS data
        network_data['dns'] = self.collection_points['dns_logs'].collect(
            time_range=time_range,
            include_response_codes=True,
            track_query_patterns=True
        )
        
        # Collect proxy data
        network_data['proxy'] = self.collection_points['proxy_logs'].collect(
            time_range=time_range,
            include_user_agent=True,
            track_download_patterns=True
        )
        
        # Enrich with threat intelligence
        enriched_data = self.enrich_network_data(network_data)
        
        return enriched_data
    
    def enrich_network_data(self, raw_data):
        """Enrich network data with threat intelligence"""
        enriched_data = raw_data.copy()
        
        # Enrich IP addresses
        for flow in enriched_data['flows']:
            flow['src_reputation'] = self.enrichment_services['ip_reputation'].lookup(flow['src_ip'])
            flow['dst_reputation'] = self.enrichment_services['ip_reputation'].lookup(flow['dst_ip'])
            flow['src_geolocation'] = self.enrichment_services['geolocation'].lookup(flow['src_ip'])
            flow['dst_geolocation'] = self.enrichment_services['geolocation'].lookup(flow['dst_ip'])
        
        # Enrich DNS queries
        for dns_query in enriched_data['dns']:
            dns_query['domain_reputation'] = self.enrichment_services['domain_reputation'].lookup(dns_query['query'])
            dns_query['threat_classification'] = self.enrichment_services['threat_intelligence'].classify_domain(dns_query['query'])
        
        return enriched_data
    
    def analyze_network_patterns(self, enriched_data):
        """Analyze network patterns for hunting"""
        patterns = {
            'beaconing': self.detect_beaconing_patterns(enriched_data['flows']),
            'dns_tunneling': self.detect_dns_tunneling(enriched_data['dns']),
            'data_exfiltration': self.detect_data_exfiltration(enriched_data['flows']),
            'lateral_movement': self.detect_lateral_movement(enriched_data['flows']),
            'command_control': self.detect_c2_communication(enriched_data)
        }
        
        return patterns
    
    def detect_beaconing_patterns(self, flow_data):
        """Detect beaconing communication patterns"""
        beaconing_candidates = []
        
        # Group flows by source-destination pairs
        flow_groups = self.group_flows_by_endpoints(flow_data)
        
        for endpoints, flows in flow_groups.items():
            if len(flows) < 10:  # Need minimum flows for pattern analysis
                continue
            
            # Analyze timing patterns
            intervals = [flows[i].timestamp - flows[i-1].timestamp 
                        for i in range(1, len(flows))]
            
            # Calculate interval statistics
            mean_interval = np.mean(intervals)
            std_interval = np.std(intervals)
            coefficient_of_variation = std_interval / mean_interval if mean_interval > 0 else 0
            
            # Beaconing criteria
            if (coefficient_of_variation < 0.3 and  # Regular intervals
                mean_interval > 30 and             # At least 30 seconds apart
                mean_interval < 3600 and           # Less than 1 hour apart
                len(flows) > 20):                  # Sufficient occurrences
                
                beaconing_candidates.append({
                    'src_ip': endpoints[0],
                    'dst_ip': endpoints[1],
                    'dst_port': flows[0].dst_port,
                    'flow_count': len(flows),
                    'mean_interval': mean_interval,
                    'regularity_score': 1 - coefficient_of_variation,
                    'duration': flows[-1].timestamp - flows[0].timestamp,
                    'threat_score': self.calculate_beaconing_threat_score(flows)
                })
        
        return sorted(beaconing_candidates, key=lambda x: x['threat_score'], reverse=True)
```

**Cloud и Application Data Sources:**
```
Cloud and Application Telemetry:
├── Cloud Infrastructure Logs
│   ├── AWS CloudTrail events
│   ├── Azure Activity logs
│   ├── Google Cloud Audit logs
│   ├── Identity and access management
│   ├── Resource configuration changes
│   ├── API call patterns
│   └── Service usage analytics
├── Application Logs
│   ├── Web application access logs
│   ├── Database query logs
│   ├── Authentication system logs
│   ├── Business application events
│   ├── API gateway logs
│   ├── Microservices communication
│   └── Container orchestration logs
├── Email Security Data
│   ├── Email gateway logs
│   ├── Phishing detection events
│   ├── Attachment analysis results
│   ├── URL reputation checks
│   ├── Email authentication results
│   ├── User reported phishing
│   └── Email flow patterns
└── Third-Party Security Data
    ├── Threat intelligence feeds
    ├── Vulnerability scanner results
    ├── Reputation services data
    ├── Sandbox analysis reports
    ├── Certificate transparency logs
    ├── Dark web monitoring
    └── Industry threat sharing
```

---

## 📚 **ЧАС 2: MITRE ATT&CK FRAMEWORK INTEGRATION И TACTICAL ANALYSIS (40 мин)**

### **🎯 MITRE ATT&CK Hunting Implementation (18 мин)**

**ATT&CK Framework Hunting Strategy:**

**Technique-Based Hunting Approach:**
```
ATT&CK Hunting Matrix:
┌─────────────────┬─────────────────┬─────────────────┬─────────────────┐
│ Tactic          │ Primary Tech.   │ Hunting Focus   │ Data Sources    │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Initial Access  │ T1566 Phishing  │ Email patterns  │ Email gateway   │
│                 │ T1190 Exploit   │ Vuln scanning   │ Network logs    │
│                 │ T1078 Valid Acc │ Auth anomalies  │ Auth logs       │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Execution       │ T1059 CLI       │ Command analysis│ Process logs    │
│                 │ T1047 WMI       │ WMI activities  │ WMI logs        │
│                 │ T1053 Scheduled │ Task creation   │ Task scheduler  │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Persistence     │ T1547 Boot/Auto │ Startup changes │ Registry logs   │
│                 │ T1543 Services  │ Service install │ Service logs    │
│                 │ T1136 Account   │ Account creation│ User mgmt logs  │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Priv Escalation │ T1055 Proc Inj  │ Process inject  │ Memory analysis │
│                 │ T1068 Exploits  │ Exploit usage   │ Vuln logs       │
│                 │ T1134 Tokens    │ Token manipul   │ Auth logs       │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Defense Evasion │ T1027 Obfuscat  │ Code obfuscate  │ File analysis   │
│                 │ T1070 Indic Rem │ Log clearing    │ Event logs      │
│                 │ T1562 Disable   │ Security bypass │ Security logs   │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Cred Access     │ T1003 Dump      │ Credential dump │ Process logs    │
│                 │ T1110 Brute     │ Password attack │ Auth logs       │
│                 │ T1555 Passwords │ Password theft  │ Browser logs    │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Discovery       │ T1083 File Disc │ File enumerat   │ File access     │
│                 │ T1018 Remote    │ Network scanning│ Network logs    │
│                 │ T1087 Account   │ Account enum    │ LDAP logs       │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Lateral Move    │ T1021 Remote    │ Remote access   │ Network logs    │
│                 │ T1210 Exploits  │ Network exploits│ IDS/IPS logs    │
│                 │ T1550 Use Alt   │ Token reuse     │ Auth logs       │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Collection      │ T1560 Archive   │ Data archiving  │ File logs       │
│                 │ T1113 Screen    │ Screenshots     │ Process logs    │
│                 │ T1005 Local     │ Local data      │ File access     │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Command Control │ T1071 App Layer │ Protocol abuse  │ Network logs    │
│                 │ T1001 Exfil     │ Data encoding   │ Traffic analysis│
│                 │ T1095 Non-App   │ Non-standard    │ Network flows   │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ Exfiltration    │ T1041 C2 Exfil  │ C2 channels     │ Network logs    │
│                 │ T1048 Alt Prot  │ Alternative     │ Protocol logs   │
│                 │ T1052 Physical  │ Physical media  │ Device logs     │
└─────────────────┴─────────────────┴─────────────────┴─────────────────┘
```

**ATT&CK-Based Hunt Development:**
```python
# MITRE ATT&CK Hunting Framework
class MitreAttackHunter:
    def __init__(self):
        self.attack_data = self.load_attack_framework()
        self.hunt_library = self.initialize_hunt_library()
        self.technique_mappings = self.load_technique_mappings()
        
    def generate_technique_hunt(self, technique_id):
        """Generate hunting query for specific ATT&CK technique"""
        technique = self.attack_data.get_technique(technique_id)
        if not technique:
            raise ValueError(f"Technique {technique_id} not found")
        
        hunt_config = HuntConfiguration(
            technique_id=technique_id,
            technique_name=technique.name,
            tactic=technique.tactic,
            data_sources=technique.data_sources,
            detection_methods=self.get_detection_methods(technique_id)
        )
        
        # Generate queries for each data source
        hunt_queries = {}
        for data_source in technique.data_sources:
            query_generator = self.get_query_generator(data_source)
            hunt_queries[data_source] = query_generator.generate_hunt_query(technique)
        
        return TechniqueHunt(
            configuration=hunt_config,
            queries=hunt_queries,
            expected_indicators=self.get_expected_indicators(technique_id),
            false_positive_filters=self.get_false_positive_filters(technique_id)
        )
    
    def hunt_t1059_command_line_interface(self):
        """Hunt for T1059 - Command and Scripting Interpreter"""
        hunt_queries = {
            'windows_events': """
                // PowerShell suspicious usage
                DeviceProcessEvents
                | where Timestamp > ago(24h)
                | where ProcessCommandLine contains "powershell" or ProcessCommandLine contains "pwsh"
                | where ProcessCommandLine has_any(
                    "-encodedcommand", "-enc", "-e", "invoke-expression", "iex",
                    "downloadstring", "downloadfile", "bypass", "unrestricted",
                    "hidden", "-windowstyle", "noprofile"
                )
                | extend SuspiciousIndicators = pack_array(
                    iff(ProcessCommandLine contains "-encodedcommand", "Encoded Command", ""),
                    iff(ProcessCommandLine contains "invoke-expression", "Code Execution", ""),
                    iff(ProcessCommandLine contains "downloadstring", "Remote Download", ""),
                    iff(ProcessCommandLine contains "bypass", "Execution Policy Bypass", "")
                )
                | where array_length(SuspiciousIndicators) > 0
                | project Timestamp, DeviceName, ProcessCommandLine, InitiatingProcessCommandLine, 
                         AccountName, SuspiciousIndicators
            """,
            
            'process_logs': """
                // Command line obfuscation detection
                SELECT 
                    timestamp, hostname, process_name, command_line, parent_process,
                    CASE 
                        WHEN command_line REGEXP '\\^' THEN 'Caret Obfuscation'
                        WHEN command_line REGEXP '\\+' THEN 'Plus Obfuscation'  
                        WHEN command_line REGEXP '%[0-9a-fA-F]{2}' THEN 'URL Encoding'
                        WHEN LENGTH(command_line) > 1000 THEN 'Excessive Length'
                        WHEN command_line REGEXP '[A-Za-z0-9+/]{20,}={0,2}' THEN 'Base64 Encoding'
                        ELSE 'Other'
                    END as obfuscation_type
                FROM process_events 
                WHERE timestamp > NOW() - INTERVAL 24 HOUR
                    AND (command_line REGEXP '\\^|\\+|%[0-9a-fA-F]{2}'
                         OR LENGTH(command_line) > 1000
                         OR command_line REGEXP '[A-Za-z0-9+/]{20,}={0,2}')
                ORDER BY timestamp DESC
            """,
            
            'sysmon_logs': """
                // Sysmon Event ID 1 - Process Creation
                index=sysmon EventCode=1
                | where _time > relative_time(now(), "-24h")
                | where (CommandLine="*powershell*" OR CommandLine="*cmd*" OR CommandLine="*wscript*" OR CommandLine="*cscript*")
                | where (CommandLine="*-enc*" OR CommandLine="*-encodedcommand*" OR 
                         CommandLine="*invoke-expression*" OR CommandLine="*downloadstring*" OR
                         CommandLine="*bypass*" OR CommandLine="*unrestricted*")
                | eval suspicion_score = 0
                | eval suspicion_score = if(match(CommandLine, "(?i)-enc"), suspicion_score + 2, suspicion_score)
                | eval suspicion_score = if(match(CommandLine, "(?i)invoke-expression"), suspicion_score + 2, suspicion_score)
                | eval suspicion_score = if(match(CommandLine, "(?i)downloadstring"), suspicion_score + 3, suspicion_score)
                | eval suspicion_score = if(match(CommandLine, "(?i)bypass"), suspicion_score + 1, suspicion_score)
                | where suspicion_score >= 2
                | table _time, Computer, User, Image, CommandLine, ParentImage, ParentCommandLine, suspicion_score
                | sort -_time
            """
        }
        
        return TechniqueHuntResult(
            technique_id="T1059",
            hunt_queries=hunt_queries,
            execution_time=datetime.now(),
            confidence_threshold=0.7
        )
    
    def hunt_t1003_credential_dumping(self):
        """Hunt for T1003 - OS Credential Dumping"""
        hunt_queries = {
            'security_events': """
                // LSASS access detection
                SecurityEvent
                | where TimeGenerated > ago(24h)
                | where EventID == 4656  // Handle to Object
                | where ObjectName contains "lsass.exe"
                | where AccessMask in ("0x1010", "0x1410", "0x1038", "0x40")  // Suspicious access rights
                | extend ProcessName = extract(@"([^\\\\]+)$", 1, ProcessName)
                | where ProcessName !in ("wmiprvse.exe", "winlogon.exe", "csrss.exe", "wininit.exe")
                | summarize count() by bin(TimeGenerated, 5m), Computer, ProcessName, SubjectUserName
                | where count_ > 1
            """,
            
            'process_events': """
                // Credential dumping tool detection
                SELECT timestamp, hostname, process_name, command_line, file_path, hash_sha256
                FROM process_events 
                WHERE timestamp > NOW() - INTERVAL 24 HOUR
                    AND (process_name IN ('mimikatz.exe', 'procdump.exe', 'comsvcs.dll')
                         OR command_line LIKE '%sekurlsa%'
                         OR command_line LIKE '%lsadump%'
                         OR command_line LIKE '%dump%lsass%'
                         OR command_line LIKE '%MiniDump%'
                         OR file_path LIKE '%temp%' AND hash_sha256 IN (
                            'known_mimikatz_hashes'  -- Replace with actual hash list
                         ))
                ORDER BY timestamp DESC
            """,
            
            'file_events': """
                // Suspicious file creation in temp directories
                index=windows source="WinEventLog:Microsoft-Windows-Sysmon/Operational" EventCode=11
                | where _time > relative_time(now(), "-24h")
                | where (TargetFilename="*\\temp\\*" OR TargetFilename="*\\tmp\\*")
                | where (TargetFilename="*.dmp" OR TargetFilename="*.dump" OR TargetFilename="*.kirbi")
                | eval file_size_mb = round(FileSize/1024/1024, 2)
                | where file_size_mb > 10  // Large files that might contain credential data
                | table _time, Computer, ProcessGuid, Image, TargetFilename, file_size_mb
                | sort -_time
            """
        }
        
        return TechniqueHuntResult(
            technique_id="T1003",
            hunt_queries=hunt_queries,
            execution_time=datetime.now(),
            confidence_threshold=0.8
        )
    
    def execute_campaign_hunt(self, threat_actor):
        """Hunt for specific threat actor campaign"""
        actor_ttps = self.get_actor_ttps(threat_actor)
        campaign_hunt = CampaignHunt(threat_actor)
        
        for ttp in actor_ttps:
            technique_hunt = self.generate_technique_hunt(ttp.technique_id)
            campaign_hunt.add_technique_hunt(technique_hunt)
        
        # Execute all hunts in parallel
        hunt_results = campaign_hunt.execute_parallel_hunts()
        
        # Correlate findings across techniques
        correlated_results = self.correlate_campaign_findings(hunt_results)
        
        return CampaignHuntResult(
            threat_actor=threat_actor,
            technique_results=hunt_results,
            correlated_findings=correlated_results,
            campaign_confidence=self.calculate_campaign_confidence(correlated_results)
        )
```

### **📈 Campaign Tracking и Attribution (12 мин)**

**Threat Actor Campaign Analysis:**

**Campaign Correlation Framework:**
```python
# Campaign Tracking and Attribution System
class ThreatCampaignTracker:
    def __init__(self):
        self.actor_profiles = self.load_actor_profiles()
        self.campaign_database = CampaignDatabase()
        self.ttp_correlator = TTPCorrelator()
        self.timeline_builder = TimelineBuilder()
        
    def analyze_campaign_indicators(self, hunt_findings):
        """Analyze findings for campaign attribution"""
        campaign_analysis = CampaignAnalysis()
        
        # Extract TTPs from findings
        observed_ttps = self.extract_ttps_from_findings(hunt_findings)
        
        # Match against known actor profiles
        actor_matches = self.match_against_actor_profiles(observed_ttps)
        
        # Analyze temporal patterns
        temporal_patterns = self.analyze_temporal_patterns(hunt_findings)
        
        # Infrastructure correlation
        infrastructure_links = self.correlate_infrastructure(hunt_findings)
        
        # Tool and technique correlation
        tool_correlations = self.correlate_tools_and_techniques(observed_ttps)
        
        campaign_analysis.actor_matches = actor_matches
        campaign_analysis.temporal_patterns = temporal_patterns
        campaign_analysis.infrastructure_links = infrastructure_links
        campaign_analysis.tool_correlations = tool_correlations
        campaign_analysis.confidence_score = self.calculate_attribution_confidence(campaign_analysis)
        
        return campaign_analysis
    
    def match_against_actor_profiles(self, observed_ttps):
        """Match observed TTPs against known threat actor profiles"""
        actor_matches = []
        
        for actor_name, actor_profile in self.actor_profiles.items():
            match_score = self.calculate_ttp_match_score(observed_ttps, actor_profile.ttps)
            
            if match_score > 0.3:  # Minimum threshold for consideration
                actor_match = ActorMatch(
                    actor_name=actor_name,
                    match_score=match_score,
                    matching_ttps=self.get_matching_ttps(observed_ttps, actor_profile.ttps),
                    unique_ttps=self.get_unique_ttps(observed_ttps, actor_profile.ttps),
                    profile_coverage=len(self.get_matching_ttps(observed_ttps, actor_profile.ttps)) / len(actor_profile.ttps)
                )
                actor_matches.append(actor_match)
        
        # Sort by match score
        return sorted(actor_matches, key=lambda x: x.match_score, reverse=True)
    
    def calculate_ttp_match_score(self, observed_ttps, profile_ttps):
        """Calculate TTP matching score between observations and profile"""
        if not observed_ttps or not profile_ttps:
            return 0.0
        
        observed_set = set([ttp.technique_id for ttp in observed_ttps])
        profile_set = set([ttp.technique_id for ttp in profile_ttps])
        
        # Jaccard similarity with weighted scoring
        intersection = observed_set.intersection(profile_set)
        union = observed_set.union(profile_set)
        
        # Base Jaccard similarity
        jaccard_score = len(intersection) / len(union) if union else 0
        
        # Weighted by technique rarity (rare techniques get higher weight)
        weighted_score = 0
        for technique_id in intersection:
            rarity_weight = self.get_technique_rarity_weight(technique_id)
            weighted_score += rarity_weight
        
        # Normalize weighted score
        max_possible_weight = sum([self.get_technique_rarity_weight(t) for t in intersection])
        normalized_weighted_score = weighted_score / max_possible_weight if max_possible_weight > 0 else 0
        
        # Combine Jaccard and weighted scores
        final_score = (jaccard_score * 0.4) + (normalized_weighted_score * 0.6)
        
        return min(final_score, 1.0)
    
    def build_campaign_timeline(self, hunt_findings):
        """Build temporal timeline of campaign activities"""
        timeline_events = []
        
        for finding in hunt_findings:
            event = TimelineEvent(
                timestamp=finding.timestamp,
                event_type=finding.event_type,
                technique_id=finding.technique_id,
                severity=finding.severity,
                affected_assets=finding.affected_assets,
                indicators=finding.indicators,
                description=finding.description
            )
            timeline_events.append(event)
        
        # Sort by timestamp
        timeline_events.sort(key=lambda x: x.timestamp)
        
        # Identify phases and patterns
        campaign_phases = self.identify_campaign_phases(timeline_events)
        
        # Calculate duration and intensity
        campaign_duration = self.calculate_campaign_duration(timeline_events)
        activity_intensity = self.calculate_activity_intensity(timeline_events)
        
        return CampaignTimeline(
            events=timeline_events,
            phases=campaign_phases,
            duration=campaign_duration,
            intensity=activity_intensity,
            peak_activity_periods=self.identify_peak_activity(timeline_events)
        )
    
    def identify_campaign_phases(self, timeline_events):
        """Identify distinct phases in campaign timeline"""
        phases = []
        
        # Group events by MITRE ATT&CK tactics
        tactic_groups = {}
        for event in timeline_events:
            tactic = self.get_tactic_for_technique(event.technique_id)
            if tactic not in tactic_groups:
                tactic_groups[tactic] = []
            tactic_groups[tactic].append(event)
        
        # Determine phase progression
        tactic_order = ['Initial Access', 'Execution', 'Persistence', 'Privilege Escalation',
                       'Defense Evasion', 'Credential Access', 'Discovery', 'Lateral Movement',
                       'Collection', 'Command and Control', 'Exfiltration']
        
        for i, tactic in enumerate(tactic_order):
            if tactic in tactic_groups:
                events = tactic_groups[tactic]
                start_time = min([e.timestamp for e in events])
                end_time = max([e.timestamp for e in events])
                
                phase = CampaignPhase(
                    phase_number=i + 1,
                    tactic=tactic,
                    start_time=start_time,
                    end_time=end_time,
                    events=events,
                    duration=end_time - start_time,
                    event_count=len(events)
                )
                phases.append(phase)
        
        return phases
```

### **🔗 IOC Management и Enrichment (10 мин)**

**Comprehensive IOC Framework:**
```python
# IOC Management and Enrichment System
class IOCManagementSystem:
    def __init__(self):
        self.ioc_database = IOCDatabase()
        self.enrichment_services = EnrichmentServices()
        self.threat_intelligence = ThreatIntelligenceAggregator()
        self.automated_hunting = AutomatedHuntingEngine()
        
    def process_hunt_iocs(self, hunt_findings):
        """Process and enrich IOCs from hunting findings"""
        extracted_iocs = self.extract_iocs_from_findings(hunt_findings)
        enriched_iocs = []
        
        for ioc in extracted_iocs:
            # Enrich with threat intelligence
            enriched_ioc = self.enrich_ioc(ioc)
            
            # Validate and score
            enriched_ioc.confidence_score = self.calculate_ioc_confidence(enriched_ioc)
            enriched_ioc.threat_score = self.calculate_threat_score(enriched_ioc)
            
            # Add to database
            self.ioc_database.store_ioc(enriched_ioc)
            
            # Trigger automated hunting
            if enriched_ioc.confidence_score > 0.7:
                self.automated_hunting.schedule_ioc_hunt(enriched_ioc)
            
            enriched_iocs.append(enriched_ioc)
        
        return enriched_iocs
    
    def enrich_ioc(self, ioc):
        """Enrich IOC with multiple intelligence sources"""
        enriched_ioc = ioc.copy()
        
        # Enrich based on IOC type
        if ioc.type == 'ip_address':
            enriched_ioc = self.enrich_ip_address(enriched_ioc)
        elif ioc.type == 'domain':
            enriched_ioc = self.enrich_domain(enriched_ioc)
        elif ioc.type == 'file_hash':
            enriched_ioc = self.enrich_file_hash(enriched_ioc)
        elif ioc.type == 'url':
            enriched_ioc = self.enrich_url(enriched_ioc)
        elif ioc.type == 'email':
            enriched_ioc = self.enrich_email(enriched_ioc)
        
        # Cross-reference with historical data
        enriched_ioc.historical_sightings = self.get_historical_sightings(ioc)
        
        # Campaign correlation
        enriched_ioc.related_campaigns = self.correlate_with_campaigns(ioc)
        
        return enriched_ioc
    
    def enrich_ip_address(self, ioc):
        """Enrich IP address IOC"""
        ip_address = ioc.value
        
        # Geolocation enrichment
        ioc.geolocation = self.enrichment_services.geolocate_ip(ip_address)
        
        # ASN information
        ioc.asn_info = self.enrichment_services.get_asn_info(ip_address)
        
        # Threat intelligence lookups
        ioc.threat_feeds = self.threat_intelligence.lookup_ip(ip_address)
        
        # Passive DNS
        ioc.passive_dns = self.enrichment_services.get_passive_dns(ip_address)
        
        # Port scan history
        ioc.port_scan_history = self.enrichment_services.get_port_scan_history(ip_address)
        
        # Reputation scores
        ioc.reputation_scores = self.enrichment_services.get_ip_reputation(ip_address)
        
        return ioc
    
    def create_automated_hunts(self, enriched_iocs):
        """Create automated hunts based on enriched IOCs"""
        automated_hunts = []
        
        for ioc in enriched_iocs:
            if ioc.confidence_score > 0.8:  # High confidence IOCs
                hunt_config = AutomatedHuntConfig(
                    ioc=ioc,
                    hunt_frequency='hourly',
                    data_sources=['network_logs', 'endpoint_logs', 'proxy_logs'],
                    alert_threshold=1,  # Alert on any match
                    auto_response=True
                )
                automated_hunts.append(hunt_config)
            
            elif ioc.confidence_score > 0.6:  # Medium confidence IOCs
                hunt_config = AutomatedHuntConfig(
                    ioc=ioc,
                    hunt_frequency='daily',
                    data_sources=['network_logs', 'proxy_logs'],
                    alert_threshold=3,  # Alert on multiple matches
                    auto_response=False
                )
                automated_hunts.append(hunt_config)
        
        return automated_hunts
    
    def generate_ioc_reports(self, time_period):
        """Generate comprehensive IOC intelligence reports"""
        report_data = {
            'new_iocs': self.ioc_database.get_new_iocs(time_period),
            'trending_iocs': self.ioc_database.get_trending_iocs(time_period),
            'campaign_correlations': self.get_campaign_correlations(time_period),
            'threat_actor_activity': self.get_threat_actor_activity(time_period),
            'industry_targeting': self.analyze_industry_targeting(time_period)
        }
        
        return IOCIntelligenceReport(
            time_period=time_period,
            data=report_data,
            key_findings=self.extract_key_findings(report_data),
            recommendations=self.generate_hunting_recommendations(report_data)
        )
```

---

## 📚 **ЧАС 3: ADVANCED THREAT INTELLIGENCE UTILIZATION И IOC MANAGEMENT (40 мин)**

### **🧠 Threat Intelligence Integration (20 мин)**

**Strategic Threat Intelligence Framework:**

**Intelligence-Driven Hunting Model:**
```
Threat Intelligence Pyramid:
┌─────────────────────────────────────────────────────────────┐
│                    Strategic Intelligence                   │
│  ├── Threat actor motivations и capabilities               │
│  ├── Geopolitical threat landscape                         │
│  ├── Industry-specific targeting trends                    │
│  ├── Long-term threat evolution patterns                   │
│  └── Executive decision support intelligence               │
├─────────────────────────────────────────────────────────────┤
│                    Operational Intelligence                │
│  ├── Campaign attribution и tracking                      │
│  ├── Attack methodology analysis                          │
│  ├── TTP evolution monitoring                             │
│  ├── Infrastructure overlap identification                │
│  └── Hunting priority recommendations                     │
├─────────────────────────────────────────────────────────────┤
│                     Tactical Intelligence                  │
│  ├── Specific technique detection methods                 │
│  ├── Tool и malware analysis                              │
│  ├── IOC validation и contextualization                   │
│  ├── Detection rule optimization                          │
│  └── Immediate hunting guidance                           │
├─────────────────────────────────────────────────────────────┤
│                   Technical Intelligence                   │
│  ├── Raw indicators of compromise (IOCs)                  │
│  ├── Signatures и detection rules                         │
│  ├── Network и host-based artifacts                       │
│  ├── Malware samples и analysis                           │
│  └── Automated feed consumption                           │
└─────────────────────────────────────────────────────────────┘
```

**Advanced TI Integration Implementation:**
```python
# Advanced Threat Intelligence Integration System
class AdvancedThreatIntelligence:
    def __init__(self):
        self.ti_sources = self.initialize_ti_sources()
        self.correlation_engine = TICorrelationEngine()
        self.confidence_calculator = ConfidenceCalculator()
        self.hunting_prioritizer = HuntingPrioritizer()
        
    def initialize_ti_sources(self):
        """Initialize multiple threat intelligence sources"""
        return {
            'commercial_feeds': {
                'recorded_future': RecordedFutureAPI(),
                'crowdstrike': CrowdStrikeAPI(),
                'mandiant': MandiantThreatIntelAPI(),
                'anomali': AnomaliThreatStreamAPI()
            },
            'open_source': {
                'misp': MISPPlatform(),
                'otx': AlienVaultOTX(),
                'threatfox': ThreatFoxAPI(),
                'urlhaus': URLHausAPI()
            },
            'government': {
                'us_cert': USCERTFeeds(),
                'eu_cert': EUCERTFeeds(),
                'national_feeds': NationalThreatFeeds()
            },
            'industry_sharing': {
                'isac_feeds': ISACThreatFeeds(),
                'sharing_groups': ThreatSharingGroups(),
                'sector_specific': SectorSpecificFeeds()
            }
        }
    
    def consume_and_correlate_intelligence(self):
        """Consume intelligence from all sources and correlate"""
        all_intelligence = {}
        
        # Consume from all sources
        for category, sources in self.ti_sources.items():
            all_intelligence[category] = {}
            for source_name, source_api in sources.items():
                try:
                    intelligence_data = source_api.fetch_latest_intelligence()
                    all_intelligence[category][source_name] = intelligence_data
                except Exception as e:
                    logging.error(f"Failed to fetch from {source_name}: {e}")
        
        # Correlate intelligence across sources
        correlated_intelligence = self.correlation_engine.correlate_across_sources(all_intelligence)
        
        # Calculate confidence scores
        scored_intelligence = self.confidence_calculator.score_intelligence(correlated_intelligence)
        
        # Prioritize for hunting
        hunting_priorities = self.hunting_prioritizer.prioritize_for_hunting(scored_intelligence)
        
        return IntelligenceConsumptionResult(
            raw_intelligence=all_intelligence,
            correlated_intelligence=correlated_intelligence,
            scored_intelligence=scored_intelligence,
            hunting_priorities=hunting_priorities
        )
    
    def generate_hunting_hypotheses_from_ti(self, threat_intelligence):
        """Generate hunting hypotheses based on threat intelligence"""
        hypotheses = []
        
        # Campaign-based hypotheses
        for campaign in threat_intelligence.active_campaigns:
            hypothesis = self.create_campaign_hypothesis(campaign)
            hypotheses.append(hypothesis)
        
        # Technique-based hypotheses
        for trending_technique in threat_intelligence.trending_techniques:
            hypothesis = self.create_technique_hypothesis(trending_technique)
            hypotheses.append(hypothesis)
        
        # Actor-based hypotheses
        for active_actor in threat_intelligence.active_threat_actors:
            hypothesis = self.create_actor_hypothesis(active_actor)
            hypotheses.append(hypothesis)
        
        # Infrastructure-based hypotheses
        for suspicious_infrastructure in threat_intelligence.suspicious_infrastructure:
            hypothesis = self.create_infrastructure_hypothesis(suspicious_infrastructure)
            hypotheses.append(hypothesis)
        
        # Prioritize hypotheses
        prioritized_hypotheses = self.prioritize_hypotheses(hypotheses)
        
        return prioritized_hypotheses
    
    def create_campaign_hypothesis(self, campaign):
        """Create hunting hypothesis based on campaign intelligence"""
        return HuntingHypothesis(
            id=f"CAMP-{campaign.id}",
            title=f"Hunt for {campaign.name} Campaign Activity",
            description=f"Search for indicators related to {campaign.name} campaign targeting {campaign.targets}",
            threat_context={
                'campaign': campaign.name,
                'threat_actor': campaign.attributed_actor,
                'targets': campaign.targets,
                'motivation': campaign.motivation
            },
            ttps=campaign.observed_ttps,
            iocs=campaign.associated_iocs,
            data_sources=['endpoint_logs', 'network_logs', 'email_logs'],
            confidence_level=campaign.attribution_confidence,
            priority_score=self.calculate_campaign_priority(campaign),
            expected_indicators=[
                f"Network communication to {campaign.c2_infrastructure}",
                f"File hashes matching {campaign.malware_families}",
                f"Domain resolutions to {campaign.domains}"
            ],
            hunting_queries=self.generate_campaign_hunting_queries(campaign)
        )
    
    def enrich_hunt_findings_with_ti(self, hunt_findings):
        """Enrich hunting findings with threat intelligence context"""
        enriched_findings = []
        
        for finding in hunt_findings:
            enriched_finding = finding.copy()
            
            # IOC enrichment
            for ioc in finding.iocs:
                ioc_context = self.get_ioc_context(ioc)
                enriched_finding.add_ioc_context(ioc, ioc_context)
            
            # TTP enrichment
            if finding.mitre_techniques:
                ttp_context = self.get_ttp_context(finding.mitre_techniques)
                enriched_finding.add_ttp_context(ttp_context)
            
            # Attribution enrichment
            attribution = self.perform_attribution_analysis(finding)
            enriched_finding.add_attribution(attribution)
            
            # Campaign correlation
            related_campaigns = self.correlate_with_known_campaigns(finding)
            enriched_finding.add_campaign_correlations(related_campaigns)
            
            # Threat scoring
            threat_score = self.calculate_finding_threat_score(enriched_finding)
            enriched_finding.threat_score = threat_score
            
            enriched_findings.append(enriched_finding)
        
        return enriched_findings
    
    def get_ioc_context(self, ioc):
        """Get comprehensive context for an IOC"""
        context = IOCContext(ioc=ioc.value, ioc_type=ioc.type)
        
        # Search across all TI sources
        for category, sources in self.ti_sources.items():
            for source_name, source_api in sources.items():
                try:
                    source_context = source_api.lookup_ioc(ioc.value)
                    if source_context:
                        context.add_source_context(source_name, source_context)
                except Exception as e:
                    logging.warning(f"IOC lookup failed for {source_name}: {e}")
        
        # Aggregate confidence и threat scores
        context.aggregate_confidence = self.calculate_aggregate_confidence(context.source_contexts)
        context.threat_level = self.calculate_aggregate_threat_level(context.source_contexts)
        
        # Generate context summary
        context.summary = self.generate_ioc_context_summary(context)
        
        return context
    
    def perform_attribution_analysis(self, finding):
        """Perform attribution analysis on hunting finding"""
        attribution_candidates = []
        
        # TTP-based attribution
        if finding.mitre_techniques:
            ttp_attribution = self.attribute_by_ttps(finding.mitre_techniques)
            attribution_candidates.extend(ttp_attribution)
        
        # Infrastructure-based attribution
        if finding.network_indicators:
            infra_attribution = self.attribute_by_infrastructure(finding.network_indicators)
            attribution_candidates.extend(infra_attribution)
        
        # Malware-based attribution
        if finding.malware_indicators:
            malware_attribution = self.attribute_by_malware(finding.malware_indicators)
            attribution_candidates.extend(malware_attribution)
        
        # Temporal pattern attribution
        temporal_attribution = self.attribute_by_temporal_patterns(finding)
        attribution_candidates.extend(temporal_attribution)
        
        # Score and rank attribution candidates
        scored_attributions = self.score_attribution_candidates(attribution_candidates)
        
        return AttributionAnalysis(
            candidates=scored_attributions,
            confidence_level=self.calculate_attribution_confidence(scored_attributions),
            primary_attribution=scored_attributions[0] if scored_attributions else None,
            attribution_reasoning=self.generate_attribution_reasoning(scored_attributions)
        )
```

---

# 🛡️ МОДУЛ 4: "ENDPOINT PROTECTION И MONITORING"

## 🎯 ЗАГЛАВНА ЧАСТ

**Предмет:** Технически методи за защита  
**Клас:** 11  
**Модул:** 4 - Endpoint Protection и Monitoring (ФИНАЛЕН МОДУЛ)  
**Продължителност:** 14 учебни часа (14 x 40 минути)  
**Тип урок:** Комбинирани уроци за усвояване на нови знания с практически endpoint security implementations и advanced threat hunting exercises

---

## 🎓 МОДУЛНИ УЧЕБНИ ЦЕЛИ

### **ГЛАВНА ЦЕЛ:**
Учениците да овладеят comprehensive endpoint protection strategies, advanced threat detection methodologies, и professional security monitoring techniques за осигуряване на enterprise-level endpoint security и proactive threat response.

### **СПЕЦИФИЧНИ ЦЕЛИ:**

**ЗНАНИЯ:**
- Разбиране на modern endpoint protection platforms (EPP/EDR/XDR) и behavioral analysis techniques
- Познаване на threat hunting methodologies и proactive security investigation approaches
- Владеене на Security Information and Event Management (SIEM) systems и automation capabilities
- Знание за malware analysis techniques и reverse engineering methodologies
- Разбиране на network security monitoring approaches и forensic investigation procedures

**УМЕНИЯ:**
- Конфигуриране и управление на advanced endpoint protection solutions
- Провеждане на professional threat hunting sessions и security investigations
- Имплементиране на comprehensive SIEM solutions със automated response capabilities
- Изпълнение на malware analysis и threat intelligence correlation
- Управление на security incident response workflows и forensic procedures

**КОМПЕТЕНТНОСТИ:**
- Analytical mindset за complex threat pattern recognition и investigation
- Strategic thinking за enterprise endpoint security architecture design
- Leadership capabilities в security operations center (SOC) coordination
- Innovation approach към emerging threat detection и response techniques
- Continuous learning commitment за rapidly evolving threat landscape adaptation

---

## 📋 МОДУЛНА СТРУКТУРА

### **🛡️ КОМПОНЕНТ 4.1: ADVANCED ENDPOINT PROTECTION PLATFORMS (4 часа)**
- Час 1: EPP vs EDR vs XDR - Modern Endpoint Security Evolution
- Час 2: Behavioral Analysis и Machine Learning Detection Techniques  
- Час 3: Zero-Day Protection и Advanced Persistent Threat (APT) Defense
- Час 4: Endpoint Security Architecture Design и Implementation

### **🔍 КОМПОНЕНТ 4.2: THREAT HUNTING И PROACTIVE DETECTION (3 часа)**
- Час 1: Threat Hunting Methodologies и Hypothesis-Driven Investigation
- Час 2: MITRE ATT&CK Framework Integration и Tactical Analysis
- Час 3: Advanced Threat Intelligence Utilization и IOC Management

### **📊 КОМПОНЕНТ 4.3: SIEM И SECURITY ORCHESTRATION (4 часа)**
- Час 1: SIEM Architecture Design и Log Correlation Techniques
- Час 2: Security Orchestration, Automation и Response (SOAR) Implementation
- Час 3: Incident Response Workflows и Automated Remediation
- Час 4: Security Metrics и SOC Performance Optimization

### **🎯 КОМПОНЕНТ 4.4: MALWARE ANALYSIS И THREAT INTELLIGENCE (2 часа)**
- Час 1: Static и Dynamic Malware Analysis Techniques
- Час 2: Threat Intelligence Integration и Attribution Analysis

### **🌐 КОМПОНЕНТ 4.5: NETWORK SECURITY MONITORING И FORENSICS (1 час)**
- Час 1: Network Traffic Analysis и Digital Forensics Fundamentals

---

## 🎯 **ЗАПОЧВАМЕ С КОМПОНЕНТ 4.1!**

### **🛡️ КОМПОНЕНТ 4.1: ADVANCED ENDPOINT PROTECTION PLATFORMS (4 часа)**

#### **🎯 СПЕЦИФИЧНИ ЦЕЛИ НА КОМПОНЕНТА:**

**ЗНАНИЯ:**
- Разбиране на endpoint protection evolution от traditional antivirus към modern XDR platforms
- Познаване на behavioral analysis techniques и machine learning detection algorithms
- Владеене на zero-day protection mechanisms и advanced persistent threat defense strategies
- Знание за endpoint security architecture design principles и enterprise deployment considerations
- Разбиране на endpoint security integration със broader security ecosystem

**УМЕНИЯ:**
- Конфигуриране на modern EPP/EDR/XDR solutions със advanced detection capabilities
- Имплементиране на behavioral analysis rules и custom detection signatures
- Управление на zero-day protection systems и threat response workflows
- Проектиране на comprehensive endpoint security architectures
- Интегриране на endpoint security със enterprise security infrastructure

**КОМПЕТЕНТНОСТИ:**
- Technical expertise в cutting-edge endpoint protection technologies
- Analytical thinking за complex threat pattern recognition
- Strategic planning за enterprise security architecture design
- Innovation mindset за emerging threat adaptation
- Collaborative approach към cross-functional security team coordination

---

## 📚 **ЧАС 1: EPP VS EDR VS XDR - MODERN ENDPOINT SECURITY EVOLUTION (40 мин)**

### **🛡️ Endpoint Security Technology Evolution (15 мин)**

**Traditional Antivirus → Modern Endpoint Protection Evolution:**

```
Endpoint Security Evolution Timeline:
┌─────────────────────────────────────────────────────────────┐
│              1990s-2000s: Signature-Based AV               │
│  ├── Static signature matching                             │
│  ├── Regular pattern updates                               │
│  ├── Reactive threat response                              │
│  └── Limited zero-day protection                           │
├─────────────────────────────────────────────────────────────┤
│              2000s-2010s: Heuristic Analysis               │
│  ├── Behavioral pattern detection                          │
│  ├── Sandboxing technologies                               │
│  ├── Real-time threat analysis                             │
│  └── Improved zero-day detection                           │
├─────────────────────────────────────────────────────────────┤
│              2010s: Endpoint Protection Platforms (EPP)    │
│  ├── Multi-layered protection                              │
│  ├── Machine learning integration                          │
│  ├── Cloud-based threat intelligence                       │
│  ├── Application control & device management               │
│  └── Centralized policy management                         │
├─────────────────────────────────────────────────────────────┤
│              2015+: Endpoint Detection & Response (EDR)    │
│  ├── Continuous monitoring & logging                       │
│  ├── Advanced threat hunting capabilities                  │
│  ├── Incident investigation & forensics                    │
│  ├── Automated response & remediation                      │
│  └── Behavioral analytics & AI/ML                          │
├─────────────────────────────────────────────────────────────┤
│              2020+: Extended Detection & Response (XDR)    │
│  ├── Cross-platform threat correlation                     │
│  ├── Network, email, cloud integration                     │
│  ├── Unified security operations                           │
│  ├── Advanced AI/ML threat detection                       │
│  └── Comprehensive attack chain visibility                 │
└─────────────────────────────────────────────────────────────┘
```

**EPP (Endpoint Protection Platform) Core Capabilities:**

**Prevention-Focused Approach:**
- **Next-Generation Antivirus (NGAV)** - machine learning-based threat detection
- **Exploit Protection** - memory protection и exploitation technique blocking
- **Application Control** - whitelist/blacklist application execution policies  
- **Device Control** - USB и peripheral device access management
- **Web Protection** - malicious URL blocking и phishing prevention
- **Firewall Integration** - host-based firewall management
- **Vulnerability Assessment** - endpoint vulnerability scanning и patching

**Real-World EPP Example - CrowdStrike Falcon Prevent:**
```
CrowdStrike Falcon Prevent Capabilities:
├── Machine Learning Prevention
│   ├── Static analysis of files before execution
│   ├── Dynamic behavioral analysis during execution
│   ├── Cloud-based threat intelligence correlation
│   └── Real-time verdict determination
├── Exploit Blocking
│   ├── Memory protection techniques
│   ├── Return-oriented programming (ROP) prevention
│   ├── Heap spray detection
│   └── PowerShell и script-based attack blocking
├── IOA (Indicators of Attack) Detection
│   ├── MITRE ATT&CK technique mapping
│   ├── Behavioral pattern recognition
│   ├── Attack chain reconstruction
│   └── Lateral movement detection
└── Cloud-Native Architecture
    ├── Lightweight agent deployment
    ├── Real-time cloud processing
    ├── Minimal endpoint resource usage
    └── Instant threat intelligence updates
```

**EDR (Endpoint Detection & Response) Advanced Capabilities:**

**Detection & Investigation Focus:**
```
EDR Core Functionality Matrix:
┌─────────────────┬──────────────────┬─────────────────┬──────────────────┐
│ Capability      │ Data Collection  │ Analysis Method │ Response Action  │
├─────────────────┼──────────────────┼─────────────────┼──────────────────┤
│ Process Mon.    │ Process creation │ Behavioral ML   │ Process kill     │
│ File Activity   │ File operations  │ Hash analysis   │ File quarantine  │
│ Network Conn.   │ Traffic metadata │ Comm patterns   │ Connection block │
│ Registry Mon.   │ Registry changes │ Baseline comp.  │ Registry restore │
│ Memory Analysis │ Memory dumps     │ Injection det.  │ Memory cleaning  │
│ User Behavior   │ Login patterns   │ Anomaly detect. │ Account disable  │
│ Script Exec.    │ PowerShell logs  │ Script analysis │ Script blocking  │
└─────────────────┴──────────────────┴─────────────────┴──────────────────┘
```

**EDR Investigation Workflow:**
1. **Continuous Data Collection** - comprehensive endpoint telemetry gathering
2. **Real-Time Analysis** - behavioral analytics и anomaly detection  
3. **Alert Generation** - high-fidelity threat notifications
4. **Investigation Tools** - forensic timeline и threat hunting capabilities
5. **Response Actions** - automated isolation, remediation, и recovery

### **🔍 XDR Platform Integration Architecture (12 мин)**

**XDR (Extended Detection & Response) Ecosystem:**

**Multi-Vector Threat Correlation:**
```
XDR Integration Architecture:
┌─────────────────────────────────────────────────────────────┐
│                    XDR Correlation Engine                   │
│  ├── Machine Learning Threat Analytics                     │
│  ├── Cross-Platform Attack Chain Reconstruction            │
│  ├── Unified Threat Intelligence Integration                │
│  └── Comprehensive Security Orchestration                  │
├─────────────────────────────────────────────────────────────┤
│                     Data Source Integration                 │
│  ├── Endpoint Telemetry (EDR)                             │
│  │   ├── Process execution logs                           │
│  │   ├── File system activities                           │
│  │   ├── Network connections                              │
│  │   └── Registry modifications                           │
│  ├── Network Security (NDR)                               │
│  │   ├── Traffic flow analysis                            │
│  │   ├── DNS query monitoring                             │
│  │   ├── Protocol anomaly detection                       │
│  │   └── Lateral movement identification                  │
│  ├── Email Security (Email Gateway)                       │
│  │   ├── Phishing attempt detection                       │
│  │   ├── Malicious attachment analysis                    │
│  │   ├── Business email compromise                        │
│  │   └── Social engineering identification                │
│  ├── Cloud Security (CASB/CWPP)                          │
│  │   ├── Cloud workload protection                        │
│  │   ├── Data loss prevention                            │
│  │   ├── Identity และ access monitoring                    │
│  │   └── Configuration drift detection                    │
│  └── Identity Systems (IAM/AD)                           │
│      ├── Authentication anomalies                         │
│      ├── Privilege escalation attempts                    │
│      ├── Account compromise indicators                    │
│      └── Identity-based attack patterns                   │
├─────────────────────────────────────────────────────────────┤
│                    Response Orchestration                  │
│  ├── Automated Investigation Workflows                     │
│  ├── Cross-Platform Threat Containment                     │
│  ├── Coordinated Response Actions                          │
│  └── Comprehensive Incident Documentation                  │
└─────────────────────────────────────────────────────────────┘
```

**XDR Attack Chain Visibility Example:**
```
Multi-Stage Attack Detection:
1. Initial Access (Email Security Detection)
   ├── Spear-phishing email detected
   ├── Malicious attachment quarantined
   ├── User education triggered
   └── Email sender reputation updated

2. Execution (Endpoint Security Detection)  
   ├── Suspicious process execution identified
   ├── PowerShell obfuscation detected
   ├── Memory injection attempt blocked
   └── Process genealogy analyzed

3. Persistence (Endpoint + Network Detection)
   ├── Registry modification detected
   ├── Scheduled task creation identified
   ├── Service installation monitored
   └── Network callback analysis

4. Lateral Movement (Network Security Detection)
   ├── Abnormal SMB traffic detected
   ├── Credential harvesting identified
   ├── Internal reconnaissance observed
   └── Privilege escalation attempted

5. Data Exfiltration (Cloud + Network Detection)
   ├── Large data transfer identified
   ├── Cloud storage upload detected
   ├── Encryption key generation observed
   └── Command and control communication
```

### **🚀 Modern Endpoint Security Technologies (13 мин)**

**Artificial Intelligence и Machine Learning Integration:**

**AI/ML Endpoint Security Applications:**
```
AI/ML Security Technology Stack:
├── Static Analysis AI
│   ├── File structure analysis
│   ├── Code pattern recognition
│   ├── Entropy calculation
│   ├── Signature generation
│   └── Malware family classification
├── Dynamic Behavioral AI
│   ├── Process behavior modeling
│   ├── System call analysis
│   ├── Network communication patterns
│   ├── User behavior profiling
│   └── Anomaly score calculation
├── Deep Learning Models
│   ├── Convolutional Neural Networks (CNN)
│   ├── Recurrent Neural Networks (RNN)
│   ├── Transformer architectures
│   ├── Generative Adversarial Networks (GAN)
│   └── Ensemble model combinations
└── Threat Intelligence AI
    ├── IOC pattern recognition
    ├── Attribution analysis
    ├── Campaign correlation
    ├── Threat actor profiling
    └── Predictive threat modeling
```

**Advanced Behavioral Analysis Techniques:**

**Process Behavior Monitoring:**
```python
# Behavioral Analysis Example - Process Monitoring
class ProcessBehaviorAnalyzer:
    def __init__(self):
        self.baseline_behaviors = {}
        self.anomaly_threshold = 2.5  # Standard deviations
        self.ml_model = load_pretrained_model('process_behavior_v2.model')
    
    def analyze_process_behavior(self, process_data):
        """
        Analyze process behavior using machine learning
        """
        behavior_features = self.extract_behavior_features(process_data)
        
        # Calculate anomaly score
        anomaly_score = self.ml_model.predict_anomaly(behavior_features)
        
        # Check against established baselines
        baseline_deviation = self.compare_to_baseline(process_data)
        
        # Combine ML and statistical analysis
        risk_score = self.calculate_risk_score(anomaly_score, baseline_deviation)
        
        return SecurityAssessment(
            process_id=process_data.pid,
            risk_score=risk_score,
            anomaly_indicators=self.identify_anomalies(behavior_features),
            recommended_actions=self.suggest_actions(risk_score),
            confidence_level=self.calculate_confidence(behavior_features)
        )
    
    def extract_behavior_features(self, process_data):
        """
        Extract relevant behavioral features for ML analysis
        """
        return BehaviorFeatures(
            # Process execution patterns
            parent_child_relationships=process_data.get_process_tree(),
            execution_frequency=process_data.get_execution_frequency(),
            resource_consumption=process_data.get_resource_usage(),
            
            # File system behavior
            file_operations=process_data.get_file_activities(),
            directory_traversal=process_data.get_directory_access(),
            file_modifications=process_data.get_file_changes(),
            
            # Network behavior
            network_connections=process_data.get_network_activity(),
            dns_queries=process_data.get_dns_requests(),
            data_transfer_patterns=process_data.get_transfer_volumes(),
            
            # System interaction
            registry_operations=process_data.get_registry_activities(),
            service_interactions=process_data.get_service_calls(),
            privilege_operations=process_data.get_privilege_usage(),
            
            # Temporal patterns
            execution_timing=process_data.get_execution_times(),
            activity_duration=process_data.get_activity_duration(),
            idle_periods=process_data.get_idle_intervals()
        )
    
    def calculate_risk_score(self, anomaly_score, baseline_deviation):
        """
        Calculate comprehensive risk score
        """
        # Weighted combination of different factors
        weights = {
            'anomaly_score': 0.4,
            'baseline_deviation': 0.3,
            'threat_intelligence': 0.2,
            'historical_context': 0.1
        }
        
        # Normalize scores to 0-100 range
        normalized_anomaly = min(anomaly_score * 10, 100)
        normalized_deviation = min(baseline_deviation * 20, 100)
        
        # Get threat intelligence score
        ti_score = self.get_threat_intelligence_score(process_data)
        
        # Get historical context score
        historical_score = self.get_historical_context_score(process_data)
        
        # Calculate weighted risk score
        risk_score = (
            weights['anomaly_score'] * normalized_anomaly +
            weights['baseline_deviation'] * normalized_deviation +
            weights['threat_intelligence'] * ti_score +
            weights['historical_context'] * historical_score
        )
        
        return min(risk_score, 100)  # Cap at 100
```

**Zero-Day Protection Mechanisms:**
```
Zero-Day Protection Layer Stack:
├── Static Heuristic Analysis
│   ├── Code structure analysis
│   ├── Packer detection
│   ├── Entropy calculations
│   ├── Import table analysis
│   └── String pattern matching
├── Sandboxing Technology
│   ├── Virtual machine isolation
│   ├── Behavioral observation
│   ├── API call monitoring
│   ├── Network traffic analysis
│   └── File system monitoring
├── Memory Protection
│   ├── DEP (Data Execution Prevention)
│   ├── ASLR (Address Space Layout Randomization)
│   ├── Control Flow Guard (CFG)
│   ├── Return Flow Guard (RFG)
│   └── Intel CET (Control-flow Enforcement Technology)
├── Application Whitelisting
│   ├── Digital signature validation
│   ├── Publisher reputation checking
│   ├── Application behavior profiling
│   ├── Hash-based verification
│   └── Machine learning classification
└── Cloud-Based Analysis
    ├── Real-time file submission
    ├── Global threat intelligence
    ├── Crowd-sourced detection
    ├── Rapid signature deployment
    └── Collective defense benefits
```

**Enterprise Endpoint Security Architecture:**
```
Enterprise Endpoint Security Architecture:
┌─────────────────────────────────────────────────────────────┐
│                     Management Layer                       │
│  ├── Centralized Policy Management                         │
│  ├── Threat Intelligence Integration                       │
│  ├── Compliance Reporting                                  │
│  └── Security Operations Dashboard                         │
├─────────────────────────────────────────────────────────────┤
│                    Orchestration Layer                     │
│  ├── SOAR Integration                                      │
│  ├── SIEM Correlation                                      │
│  ├── Incident Response Automation                          │
│  └── Threat Hunting Platforms                              │
├─────────────────────────────────────────────────────────────┤
│                     Detection Layer                        │
│  ├── Endpoint Detection & Response (EDR)                  │
│  ├── Network Detection & Response (NDR)                   │
│  ├── User & Entity Behavior Analytics (UEBA)              │
│  └── Deception Technology                                  │
├─────────────────────────────────────────────────────────────┤
│                    Prevention Layer                        │
│  ├── Next-Generation Antivirus (NGAV)                     │
│  ├── Application Control & Whitelisting                   │
│  ├── Exploit Protection                                    │
│  └── Device Control                                        │
├─────────────────────────────────────────────────────────────┤
│                     Endpoint Layer                         │
│  ├── Windows Endpoints                                     │
│  ├── macOS Endpoints                                       │
│  ├── Linux Endpoints                                       │
│  ├── Mobile Devices                                        │
│  └── IoT Devices                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 📚 **ЧАС 2: BEHAVIORAL ANALYSIS И MACHINE LEARNING DETECTION TECHNIQUES (40 мин)**

### **🧠 Machine Learning в Endpoint Security (15 мин)**

**ML Algorithm Applications в Threat Detection:**

**Supervised Learning Models:**
```
Supervised Learning Applications:
├── Classification Models
│   ├── Malware Family Classification
│   │   ├── Random Forest: 95% accuracy
│   │   ├── Support Vector Machine: 92% accuracy
│   │   ├── Neural Networks: 97% accuracy
│   │   └── Ensemble Methods: 98% accuracy
│   ├── Phishing Detection
│   │   ├── URL feature analysis
│   │   ├── Content analysis
│   │   ├── Sender reputation
│   │   └── User behavior patterns
│   └── Network Intrusion Detection
│       ├── Packet analysis
│       ├── Flow-based detection
│       ├── Protocol anomalies
│       └── Behavioral signatures
├── Regression Models
│   ├── Risk Score Prediction
│   ├── Attack Progression Modeling
│   ├── Resource Consumption Forecasting
│   └── Performance Impact Assessment
└── Training Data Sources
    ├── Historical malware samples
    ├── Benign software datasets
    ├── Threat intelligence feeds
    └── Security incident databases
```

**Unsupervised Learning για Anomaly Detection:**
```python
# Advanced Anomaly Detection Implementation
import numpy as np
from sklearn.ensemble import IsolationForest
from sklearn.cluster import DBSCAN
from sklearn.preprocessing import StandardScaler

class EndpointAnomalyDetector:
    def __init__(self):
        self.isolation_forest = IsolationForest(
            contamination=0.1,  # Expect 10% anomalies
            random_state=42,
            n_estimators=200
        )
        self.dbscan = DBSCAN(eps=0.5, min_samples=5)
        self.scaler = StandardScaler()
        self.baseline_models = {}
        
    def train_baseline_models(self, historical_data):
        """
        Train baseline models using historical endpoint data
        """
        # Process different data types
        for endpoint_type in ['workstation', 'server', 'mobile']:
            type_data = historical_data[endpoint_type]
            
            # Feature extraction
            features = self.extract_behavioral_features(type_data)
            
            # Normalize features
            normalized_features = self.scaler.fit_transform(features)
            
            # Train isolation forest for this endpoint type
            if_model = IsolationForest(contamination=0.05)
            if_model.fit(normalized_features)
            
            # Train clustering model
            clusters = self.dbscan.fit_predict(normalized_features)
            
            self.baseline_models[endpoint_type] = {
                'isolation_forest': if_model,
                'clusters': clusters,
                'feature_means': np.mean(normalized_features, axis=0),
                'feature_stds': np.std(normalized_features, axis=0)
            }
    
    def detect_anomalies(self, endpoint_data):
        """
        Detect anomalies in real-time endpoint data
        """
        endpoint_type = endpoint_data.get('type', 'workstation')
        model = self.baseline_models.get(endpoint_type)
        
        if not model:
            return AnomalyResult(score=0.5, confidence=0.3, 
                               reason="No baseline model available")
        
        # Extract features from current data
        features = self.extract_behavioral_features([endpoint_data])
        normalized_features = self.scaler.transform(features)
        
        # Isolation Forest anomaly detection
        if_score = model['isolation_forest'].decision_function(normalized_features)[0]
        if_prediction = model['isolation_forest'].predict(normalized_features)[0]
        
        # Statistical anomaly detection
        stat_score = self.calculate_statistical_anomaly(
            normalized_features[0], 
            model['feature_means'], 
            model['feature_stds']
        )
        
        # Behavioral pattern analysis
        behavior_score = self.analyze_behavioral_patterns(endpoint_data)
        
        # Combine scores
        combined_score = self.combine_anomaly_scores(
            if_score, stat_score, behavior_score
        )
        
        return AnomalyResult(
            score=combined_score,
            confidence=self.calculate_confidence(if_score, stat_score),
            anomaly_type=self.classify_anomaly_type(endpoint_data),
            recommendations=self.generate_recommendations(combined_score)
        )
    
    def extract_behavioral_features(self, endpoint_data_list):
        """
        Extract behavioral features for ML analysis
        """
        features = []
        
        for data in endpoint_data_list:
            feature_vector = [
                # Process behavior features
                data.get('process_count', 0),
                data.get('unique_processes', 0),
                data.get('process_creation_rate', 0),
                data.get('process_termination_rate', 0),
                
                # Network behavior features
                data.get('network_connections', 0),
                data.get('bytes_sent', 0),
                data.get('bytes_received', 0),
                data.get('unique_destinations', 0),
                
                # File system behavior features
                data.get('files_created', 0),
                data.get('files_modified', 0),
                data.get('files_deleted', 0),
                data.get('registry_changes', 0),
                
                # Temporal features
                data.get('activity_duration', 0),
                data.get('peak_activity_time', 0),
                data.get('activity_variance', 0),
                
                # User behavior features
                data.get('login_frequency', 0),
                data.get('application_usage_diversity', 0),
                data.get('working_hours_activity', 0)
            ]
            
            features.append(feature_vector)
        
        return np.array(features)
    
    def analyze_behavioral_patterns(self, endpoint_data):
        """
        Analyze specific behavioral patterns
        """
        risk_indicators = 0
        
        # Suspicious process behaviors
        if endpoint_data.get('powershell_executions', 0) > 10:
            risk_indicators += 0.3
        
        if endpoint_data.get('cmd_executions', 0) > 20:
            risk_indicators += 0.2
        
        # Network behavior anomalies
        if endpoint_data.get('external_connections', 0) > 50:
            risk_indicators += 0.4
        
        # File system anomalies
        if endpoint_data.get('temp_file_creations', 0) > 100:
            risk_indicators += 0.3
        
        # Time-based anomalies
        current_hour = datetime.now().hour
        if current_hour < 6 or current_hour > 22:  # Outside business hours
            if endpoint_data.get('activity_level', 0) > 0.7:
                risk_indicators += 0.5
        
        return min(risk_indicators, 1.0)  # Cap at 1.0
```

**Deep Learning для Advanced Threat Detection:**
```python
# Deep Learning Threat Detection Model
import tensorflow as tf
from tensorflow.keras.models import Sequential, Model
from tensorflow.keras.layers import LSTM, Dense, Dropout, Attention

class DeepThreatDetectionModel:
    def __init__(self):
        self.sequence_length = 100  # Time series length
        self.feature_dim = 50       # Feature dimensions
        self.model = self.build_model()
    
    def build_model(self):
        """
        Build deep learning model for threat detection
        """
        # Input layer
        inputs = tf.keras.Input(shape=(self.sequence_length, self.feature_dim))
        
        # LSTM layers for temporal pattern recognition
        lstm1 = LSTM(128, return_sequences=True)(inputs)
        lstm1_dropout = Dropout(0.3)(lstm1)
        
        lstm2 = LSTM(64, return_sequences=True)(lstm1_dropout)
        lstm2_dropout = Dropout(0.3)(lstm2)
        
        # Attention mechanism for important feature focus
        attention = Attention()([lstm2_dropout, lstm2_dropout])
        
        # Dense layers for classification
        dense1 = Dense(32, activation='relu')(attention)
        dense1_dropout = Dropout(0.5)(dense1)
        
        # Output layers
        malware_output = Dense(1, activation='sigmoid', name='malware_detection')(dense1_dropout)
        attack_type_output = Dense(10, activation='softmax', name='attack_classification')(dense1_dropout)
        severity_output = Dense(1, activation='linear', name='severity_prediction')(dense1_dropout)
        
        model = Model(
            inputs=inputs,
            outputs=[malware_output, attack_type_output, severity_output]
        )
        
        # Compile с multiple loss functions
        model.compile(
            optimizer='adam',
            loss={
                'malware_detection': 'binary_crossentropy',
                'attack_classification': 'categorical_crossentropy',
                'severity_prediction': 'mse'
            },
            loss_weights={
                'malware_detection': 1.0,
                'attack_classification': 0.8,
                'severity_prediction': 0.5
            },
            metrics=['accuracy']
        )
        
        return model
    
    def preprocess_endpoint_data(self, endpoint_telemetry):
        """
        Preprocess endpoint telemetry for deep learning
        """
        # Convert telemetry to time series format
        time_series_data = []
        
        for timestamp in sorted(endpoint_telemetry.keys()):
            data_point = endpoint_telemetry[timestamp]
            
            feature_vector = [
                # Process features
                data_point.get('process_creations', 0),
                data_point.get('process_terminations', 0),
                data_point.get('process_injections', 0),
                
                # Network features
                data_point.get('network_connections', 0),
                data_point.get('dns_queries', 0),
                data_point.get('http_requests', 0),
                
                # File system features
                data_point.get('file_creations', 0),
                data_point.get('file_modifications', 0),
                data_point.get('file_deletions', 0),
                
                # Registry features
                data_point.get('registry_reads', 0),
                data_point.get('registry_writes', 0),
                data_point.get('registry_deletions', 0),
                
                # Memory features
                data_point.get('memory_allocations', 0),
                data_point.get('memory_protections', 0),
                data_point.get('dll_loads', 0),
                
                # Additional behavioral features...
            ]
            
            # Pad vector to feature_dim length
            while len(feature_vector) < self.feature_dim:
                feature_vector.append(0.0)
            
            time_series_data.append(feature_vector[:self.feature_dim])
        
        # Ensure sequence length
        if len(time_series_data) < self.sequence_length:
            # Pad sequence
            padding_needed = self.sequence_length - len(time_series_data)
            time_series_data = [[0.0] * self.feature_dim] * padding_needed + time_series_data
        elif len(time_series_data) > self.sequence_length:
            # Take latest sequence_length data points
            time_series_data = time_series_data[-self.sequence_length:]
        
        return np.array([time_series_data])  # Batch dimension
    
    def predict_threats(self, endpoint_telemetry):
        """
        Predict threats using trained deep learning model
        """
        preprocessed_data = self.preprocess_endpoint_data(endpoint_telemetry)
        
        # Get predictions
        predictions = self.model.predict(preprocessed_data)
        malware_prob, attack_types, severity = predictions
        
        # Process results
        is_malware = malware_prob[0][0] > 0.5
        attack_type_idx = np.argmax(attack_types[0])
        severity_score = severity[0][0]
        
        attack_types_mapping = [
            'Ransomware', 'Banking Trojan', 'Spyware', 'Adware',
            'Rootkit', 'Worm', 'APT', 'Cryptominer', 'Botnet', 'Other'
        ]
        
        return ThreatPrediction(
            is_malware=is_malware,
            malware_confidence=float(malware_prob[0][0]),
            attack_type=attack_types_mapping[attack_type_idx],
            attack_confidence=float(attack_types[0][attack_type_idx]),
            severity_score=float(severity_score),
            recommendations=self.generate_response_recommendations(
                is_malware, attack_type_idx, severity_score
            )
        )
```

### **📊 Behavioral Baseline Development (12 мин)**

**User и Entity Behavior Analytics (UEBA):**

**Behavioral Baseline Framework:**
```
UEBA Behavioral Analysis Framework:
├── User Behavior Profiling
│   ├── Login Patterns
│   │   ├── Login times и frequency
│   │   ├── Geographic locations
│   │   ├── Device fingerprints
│   │   └── Authentication methods
│   ├── Application Usage
│   │   ├── Application access patterns
│   │   ├── Resource consumption
│   │   ├── Data access patterns
│   │   └── Collaboration behaviors
│   ├── Network Behavior
│   │   ├── Internal resource access
│   │   ├── External communications
│   │   ├── Data transfer volumes
│   │   └── Protocol usage patterns
│   └── File System Behavior
│       ├── File access patterns
│       ├── Creation и modification rates
│       ├── Share access behaviors
│       └── Backup и archival activities
├── Entity Behavior Profiling
│   ├── Endpoint Behavior
│   │   ├── Process execution patterns
│   │   ├── Service startup behaviors
│   │   ├── Network communication patterns
│   │   └── Resource utilization trends
│   ├── Application Behavior
│   │   ├── API call patterns
│   │   ├── Database interaction behaviors
│   │   ├── External service communications
│   │   └── Error rate patterns
│   └── Infrastructure Behavior
│       ├── Server performance patterns
│       ├── Network traffic flows
│       ├── Storage utilization trends
│       └── Service dependency behaviors
└── Anomaly Detection Methods
    ├── Statistical Analysis
    │   ├── Standard deviation analysis
    │   ├── Percentile-based detection
    │   ├── Time series analysis
    │   └── Correlation analysis
    ├── Machine Learning
    │   ├── Clustering algorithms
    │   ├── Classification models
    │   ├── Neural networks
    │   └── Ensemble methods
    └── Rule-Based Detection
        ├── Threshold-based rules
        ├── Pattern matching rules
        ├── Temporal correlation rules
        └── Multi-dimensional rules
```

**Practical UEBA Implementation:**
```python
# User and Entity Behavior Analytics Implementation
class UEBAAnalysisEngine:
    def __init__(self):
        self.user_profiles = {}
        self.entity_profiles = {}
        self.anomaly_detectors = {}
        self.risk_scoring_model = RiskScoringModel()
        
    def build_user_profile(self, user_id, historical_data):
        """
        Build comprehensive user behavioral profile
        """
        profile = UserBehaviorProfile(user_id)
        
        # Analyze login patterns
        login_data = historical_data.get_login_data(user_id)
        profile.login_patterns = self.analyze_login_patterns(login_data)
        
        # Analyze application usage
        app_data = historical_data.get_application_usage(user_id)
        profile.application_patterns = self.analyze_application_usage(app_data)
        
        # Analyze network behavior
        network_data = historical_data.get_network_activity(user_id)
        profile.network_patterns = self.analyze_network_behavior(network_data)
        
        # Analyze file system behavior
        file_data = historical_data.get_file_activity(user_id)
        profile.file_patterns = self.analyze_file_behavior(file_data)
        
        # Calculate baseline metrics
        profile.baseline_metrics = self.calculate_baseline_metrics(profile)
        
        self.user_profiles[user_id] = profile
        return profile
    
    def analyze_login_patterns(self, login_data):
        """
        Analyze user login behavioral patterns
        """
        login_patterns = LoginPatterns()
        
        # Time-based analysis
        login_times = [entry.timestamp.hour for entry in login_data]
        login_patterns.typical_hours = self.calculate_percentile_range(login_times, 5, 95)
        login_patterns.peak_hours = self.find_peak_hours(login_times)
        
        # Day-of-week analysis
        login_days = [entry.timestamp.weekday() for entry in login_data]
        login_patterns.typical_days = self.analyze_day_patterns(login_days)
        
        # Geographic analysis
        locations = [entry.location for entry in login_data]
        login_patterns.typical_locations = self.analyze_location_patterns(locations)
        login_patterns.location_diversity = len(set(locations))
        
        # Device analysis
        devices = [entry.device_fingerprint for entry in login_data]
        login_patterns.typical_devices = self.analyze_device_patterns(devices)
        login_patterns.device_diversity = len(set(devices))
        
        # Frequency analysis
        daily_logins = self.group_by_day(login_data)
        login_patterns.daily_frequency = {
            'mean': np.mean([len(day_logins) for day_logins in daily_logins.values()]),
            'std': np.std([len(day_logins) for day_logins in daily_logins.values()]),
            'max': max([len(day_logins) for day_logins in daily_logins.values()])
        }
        
        return login_patterns
    
    def detect_user_anomalies(self, user_id, current_activity):
        """
        Detect anomalies in current user activity
        """
        profile = self.user_profiles.get(user_id)
        if not profile:
            return AnomalyResult(score=0.5, reason="No profile available")
        
        anomalies = []
        total_risk_score = 0
        
        # Login pattern anomalies
        login_anomaly = self.check_login_anomalies(current_activity.login_data, profile.login_patterns)
        if login_anomaly.is_anomalous:
            anomalies.append(login_anomaly)
            total_risk_score += login_anomaly.risk_score * 0.3
        
        # Application usage anomalies
        app_anomaly = self.check_application_anomalies(current_activity.app_usage, profile.application_patterns)
        if app_anomaly.is_anomalous:
            anomalies.append(app_anomaly)
            total_risk_score += app_anomaly.risk_score * 0.25
        
        # Network behavior anomalies
        network_anomaly = self.check_network_anomalies(current_activity.network_data, profile.network_patterns)
        if network_anomaly.is_anomalous:
            anomalies.append(network_anomaly)
            total_risk_score += network_anomaly.risk_score * 0.25
        
        # File system anomalies
        file_anomaly = self.check_file_anomalies(current_activity.file_data, profile.file_patterns)
        if file_anomaly.is_anomalous:
            anomalies.append(file_anomaly)
            total_risk_score += file_anomaly.risk_score * 0.2
        
        return UserAnomalyResult(
            user_id=user_id,
            total_risk_score=min(total_risk_score, 1.0),
            anomalies=anomalies,
            confidence=self.calculate_detection_confidence(anomalies),
            recommendations=self.generate_user_recommendations(anomalies)
        )
    
    def check_login_anomalies(self, current_login, login_patterns):
        """
        Check for login-related anomalies
        """
        anomaly_score = 0
        anomaly_reasons = []
        
        # Time-based anomaly
        current_hour = current_login.timestamp.hour
        if current_hour < login_patterns.typical_hours[0] or current_hour > login_patterns.typical_hours[1]:
            anomaly_score += 0.4
            anomaly_reasons.append(f"Login at unusual hour: {current_hour}")
        
        # Location-based anomaly
        if current_login.location not in login_patterns.typical_locations:
            # Check distance from typical locations
            min_distance = min([self.calculate_distance(current_login.location, loc) 
                              for loc in login_patterns.typical_locations])
            if min_distance > 100:  # 100km threshold
                anomaly_score += 0.6
                anomaly_reasons.append(f"Login from unusual location: {current_login.location}")
        
        # Device-based anomaly
        if current_login.device_fingerprint not in login_patterns.typical_devices:
            anomaly_score += 0.3
            anomaly_reasons.append("Login from new/unusual device")
        
        # Multiple rapid logins
        recent_logins = self.get_recent_logins(current_login.user_id, minutes=15)
        if len(recent_logins) > 5:
            anomaly_score += 0.5
            anomaly_reasons.append(f"Rapid multiple logins: {len(recent_logins)} in 15 minutes")
        
        return LoginAnomalyResult(
            is_anomalous=anomaly_score > 0.3,
            risk_score=min(anomaly_score, 1.0),
            reasons=anomaly_reasons,
            confidence=self.calculate_login_confidence(current_login, login_patterns)
        )
```

### **🎯 Advanced Pattern Recognition (13 мин)**

**Attack Pattern Recognition:**

**MITRE ATT&CK Integration:**
```
MITRE ATT&CK Behavioral Detection:
├── Initial Access Patterns
│   ├── Spearphishing Detection
│   │   ├── Email attachment analysis
│   │   ├── Link click behavior
│   │   ├── Macro execution patterns
│   │   └── Document exploitation indicators
│   ├── Valid Account Abuse
│   │   ├── Credential stuffing detection
│   │   ├── Brute force identification
│   │   ├── Off-hours access patterns
│   │   └── Geographic anomalies
│   └── External Remote Services
│       ├── VPN access anomalies
│       ├── RDP connection patterns
│       ├── SSH access behaviors
│       └── Cloud service abuse
├── Execution Patterns
│   ├── PowerShell Abuse
│   │   ├── Obfuscated command detection
│   │   ├── Download cradle identification
│   │   ├── Encoded command analysis
│   │   └── Suspicious script execution
│   ├── Command Line Interface
│   │   ├── Living-off-the-land binaries
│   │   ├── Administrative tool abuse
│   │   ├── Batch file execution
│   │   └── Scripting host usage
│   └── Windows Management Instrumentation
│       ├── WMI event subscriptions
│       ├── Remote WMI execution
│       ├── WMI persistence mechanisms
│       └── WMI lateral movement
├── Persistence Patterns
│   ├── Registry Manipulation
│   │   ├── Autorun key modifications
│   │   ├── Service installation
│   │   ├── DLL hijacking setup
│   │   └── Startup folder usage
│   ├── Scheduled Tasks
│   │   ├── Task creation patterns
│   │   ├── Unusual execution times
│   │   ├── Hidden task attributes
│   │   └── Privilege escalation tasks
│   └── Account Manipulation
│       ├── Account creation patterns
│       ├── Privilege modifications
│       ├── Group membership changes
│       └── Password policy bypasses
└── Lateral Movement Patterns
    ├── Remote Services
    │   ├── SMB/Admin$ usage
    │   ├── WinRM connections
    │   ├── DCOM exploitation
    │   └── Service execution
    ├── Application Deployment Software
    │   ├── SCCM abuse
    │   ├── GPO modifications
    │   ├── Software deployment tools
    │   └── Management frameworks
    └── Remote File Copy
        ├── Administrative shares
        ├── FTP transfers
        ├── SCP/SFTP usage
        └── Cloud storage abuse
```

**Advanced Pattern Recognition Implementation:**
```python
# Advanced Attack Pattern Recognition Engine
class AttackPatternRecognizer:
    def __init__(self):
        self.mitre_attack_patterns = self.load_mitre_patterns()
        self.temporal_analyzers = {}
        self.behavioral_models = {}
        self.threat_intelligence = ThreatIntelligenceService()
        
    def analyze_attack_patterns(self, endpoint_events):
        """
        Analyze endpoint events for attack patterns
        """
        # Group events by time windows
        time_windows = self.create_time_windows(endpoint_events, window_size=300)  # 5-minute windows
        
        detected_patterns = []
        attack_chain_score = 0
        
        for window in time_windows:
            # Analyze individual techniques
            techniques = self.identify_mitre_techniques(window.events)
            
            # Look for technique combinations
            combinations = self.analyze_technique_combinations(techniques)
            
            # Calculate attack progression score
            progression_score = self.calculate_progression_score(techniques, combinations)
            
            if progression_score > 0.6:
                pattern = AttackPattern(
                    window=window,
                    techniques=techniques,
                    combinations=combinations,
                    confidence=progression_score,
                    attack_stage=self.identify_attack_stage(techniques)
                )
                detected_patterns.append(pattern)
                attack_chain_score += progression_score
        
        # Analyze cross-window patterns
        if len(detected_patterns) > 1:
            attack_chain = self.analyze_attack_chain(detected_patterns)
            if attack_chain.confidence > 0.7:
                return AttackAnalysisResult(
                    attack_detected=True,
                    attack_chain=attack_chain,
                    confidence=attack_chain.confidence,
                    severity=self.calculate_attack_severity(attack_chain),
                    recommendations=self.generate_attack_recommendations(attack_chain)
                )
        
        return AttackAnalysisResult(
            attack_detected=False,
            individual_patterns=detected_patterns,
            overall_risk=min(attack_chain_score / len(time_windows), 1.0)
        )
    
    def identify_mitre_techniques(self, events):
        """
        Identify MITRE ATT&CK techniques from events
        """
        identified_techniques = []
        
        for event in events:
            # Process creation analysis
            if event.type == 'process_creation':
                techniques.extend(self.analyze_process_creation(event))
            
            # File system analysis
            elif event.type == 'file_operation':
                techniques.extend(self.analyze_file_operation(event))
            
            # Network analysis
            elif event.type == 'network_connection':
                techniques.extend(self.analyze_network_connection(event))
            
            # Registry analysis
            elif event.type == 'registry_operation':
                techniques.extend(self.analyze_registry_operation(event))
            
            # PowerShell analysis
            elif event.type == 'powershell_execution':
                techniques.extend(self.analyze_powershell_execution(event))
        
        return self.deduplicate_techniques(identified_techniques)
    
    def analyze_process_creation(self, event):
        """
        Analyze process creation for attack techniques
        """
        techniques = []
        process_name = event.process_name.lower()
        command_line = event.command_line.lower()
        parent_process = event.parent_process.lower()
        
        # T1059.001 - PowerShell
        if process_name == 'powershell.exe' or process_name == 'pwsh.exe':
            confidence = 0.3  # Base confidence
            
            # Check for suspicious PowerShell usage
            if any(indicator in command_line for indicator in 
                   ['-encodedcommand', '-enc', 'downloadstring', 'invoke-expression']):
                confidence += 0.4
                
            if 'bypass' in command_line or 'unrestricted' in command_line:
                confidence += 0.3
                
            techniques.append(MitreTechnique(
                technique_id='T1059.001',
                name='PowerShell',
                confidence=min(confidence, 1.0),
                evidence=command_line,
                timestamp=event.timestamp
            ))
        
        # T1055 - Process Injection
        if any(indicator in command_line for indicator in 
               ['createremotethread', 'writeprocessmemory', 'virtualalloc']):
            techniques.append(MitreTechnique(
                technique_id='T1055',
                name='Process Injection',
                confidence=0.7,
                evidence=command_line,
                timestamp=event.timestamp
            ))
        
        # T1218 - Signed Binary Proxy Execution
        signed_binaries = ['mshta.exe', 'rundll32.exe', 'regsvr32.exe', 'msiexec.exe']
        if process_name in signed_binaries:
            # Check for suspicious usage
            if parent_process not in ['explorer.exe', 'cmd.exe', 'services.exe']:
                techniques.append(MitreTechnique(
                    technique_id='T1218',
                    name='Signed Binary Proxy Execution',
                    confidence=0.6,
                    evidence=f"{process_name} spawned by {parent_process}",
                    timestamp=event.timestamp
                ))
        
        # T1047 - Windows Management Instrumentation
        if process_name == 'wmic.exe':
            confidence = 0.4
            if any(indicator in command_line for indicator in 
                   ['process call create', '/node:', 'shadowcopy']):
                confidence += 0.4
                
            techniques.append(MitreTechnique(
                technique_id='T1047',
                name='Windows Management Instrumentation',
                confidence=min(confidence, 1.0),
                evidence=command_line,
                timestamp=event.timestamp
            ))
        
        return techniques
    
    def analyze_technique_combinations(self, techniques):
        """
        Analyze technique combinations for attack patterns
        """
        combinations = []
        technique_dict = {t.technique_id: t for t in techniques}
        
        # Initial Access + Execution combinations
        if 'T1566.001' in technique_dict and 'T1059.001' in technique_dict:
            combinations.append(TechniqueCombination(
                primary='T1566.001',  # Spearphishing Attachment
                secondary='T1059.001',  # PowerShell
                pattern_type='Initial Access -> Execution',
                confidence=0.8,
                risk_score=0.9
            ))
        
        # Execution + Persistence combinations
        if 'T1059.001' in technique_dict and 'T1547.001' in technique_dict:
            combinations.append(TechniqueCombination(
                primary='T1059.001',  # PowerShell
                secondary='T1547.001',  # Registry Run Keys
                pattern_type='Execution -> Persistence',
                confidence=0.7,
                risk_score=0.8
            ))
        
        # Lateral Movement combinations
        if 'T1021.001' in technique_dict and 'T1055' in technique_dict:
            combinations.append(TechniqueCombination(
                primary='T1021.001',  # Remote Desktop Protocol
                secondary='T1055',     # Process Injection
                pattern_type='Lateral Movement -> Defense Evasion',
                confidence=0.8,
                risk_score=0.9
            ))
        
        return combinations
    
    def calculate_progression_score(self, techniques, combinations):
        """
        Calculate attack progression score
        """
        if not techniques:
            return 0.0
        
        # Base score from individual techniques
        technique_scores = [t.confidence for t in techniques]
        base_score = np.mean(technique_scores)
        
        # Bonus for technique diversity
        unique_tactics = len(set([self.get_tactic_for_technique(t.technique_id) for t in techniques]))
        diversity_bonus = min(unique_tactics * 0.1, 0.3)
        
        # Bonus for combinations
        combination_bonus = len(combinations) * 0.2
        
        # Temporal clustering bonus
        if self.techniques_temporally_clustered(techniques):
            temporal_bonus = 0.2
        else:
            temporal_bonus = 0.0
        
        total_score = base_score + diversity_bonus + combination_bonus + temporal_bonus
        return min(total_score, 1.0)
```

---

## 🛠️ **ПРАКТИЧЕСКИ УПРАЖНЕНИЯ КОМПОНЕНТ 4.1**

### **🛡️ Упражнение 4.1.1: Modern Endpoint Protection Platform Evaluation (25 мин)**

**Scenario:** Enterprise endpoint protection solution selection

**Evaluation Criteria:**
- EPP vs EDR vs XDR capabilities
- AI/ML detection accuracy
- Zero-day protection effectiveness
- Performance impact assessment
- Integration capabilities

**Evaluation Process:**

**Phase 1: Technical Capability Assessment (8 мин)**
- Compare detection methodologies
- Evaluate AI/ML implementation
- Test zero-day protection
- Measure performance impact

**Phase 2: Practical Testing (10 мин)**
- Deploy test environments
- Execute malware samples
- Measure detection rates
- Assess false positive rates

**Phase 3: Integration Evaluation (7 мин)**
- Test SIEM integration
- Evaluate API capabilities
- Assess management interfaces
- Review reporting capabilities

**Deliverable:** Comprehensive endpoint protection platform evaluation report със recommendations

### **🧠 Упражнение 4.1.2: Machine Learning Detection Model Development (30 мин)**

**Project:** Custom ML model за endpoint threat detection

**Model Requirements:**
- Behavioral analysis capabilities
- Real-time detection performance
- Low false positive rates
- Explainable AI decisions
- Continuous learning adaptation

**Development Process:**

**Phase 1: Data Preparation (10 мин)**
- Collect endpoint telemetry data
- Extract behavioral features
- Create training datasets
- Implement data preprocessing

**Phase 2: Model Development (12 мин)**
- Design ML architecture
- Train classification models
- Implement ensemble methods
- Optimize hyperparameters

**Phase 3: Validation и Testing (8 мин)**
- Test detection accuracy
- Measure false positive rates
- Validate real-time performance
- Implement explainability features

**Deliverable:** Production-ready ML detection model със comprehensive evaluation metrics

### **🎯 Упражнение 4.1.3: UEBA Implementation Workshop (20 мин)**

**Objective:** Implement comprehensive user behavior analytics

**UEBA Components:**
- User baseline profiling
- Anomaly detection algorithms
- Risk scoring framework
- Automated alert generation
- Investigation workflows

**Implementation Tasks:**

**Phase 1: Baseline Development (8 мин)**
- Collect historical user data
- Build behavioral profiles
- Establish normal patterns
- Define anomaly thresholds

**Phase 2: Detection Implementation (7 мин)**
- Implement anomaly algorithms
- Configure risk scoring
- Set up alert workflows
- Test detection accuracy

**Phase 3: Investigation Tools (5 мин)**
- Build investigation dashboards
- Create forensic timelines
- Implement drill-down capabilities
- Test incident workflows

**Deliverable:** Complete UEBA solution със operational dashboards и investigation tools

### **🔍 Упражнение 4.1.4: Attack Pattern Recognition Engine (25 мин)**

**Challenge:** Advanced attack pattern detection system

**Detection Capabilities:**
- MITRE ATT&CK technique identification
- Attack chain reconstruction
- Multi-stage attack correlation
- Threat intelligence integration
- Automated response triggers

**Development Process:**

**Phase 1: Pattern Recognition (10 мин)**
- Implement MITRE ATT&CK mapping
- Create technique detection rules
- Build pattern correlation logic
- Test individual technique detection

**Phase 2: Attack Chain Analysis (8 мин)**
- Implement temporal correlation
- Build attack progression models
- Create chain confidence scoring
- Test multi-stage detection

**Phase 3: Response Integration (7 мин)**
- Configure automated responses
- Implement escalation workflows
- Create investigation playbooks
- Test end-to-end scenarios

**Deliverable:** Advanced attack pattern recognition engine със automated response capabilities

---

## 📊 **ОЦЕНЯВАНЕ КОМПОНЕНТ 4.1**

### **📝 Формативно оценяване:**

**🧪 Тест 4.1: Advanced Endpoint Protection (20 мин, 30 точки)**

**Практически задачи (18 точки):**
1. Configure EPP/EDR/XDR platform за enterprise environment (8 точки)
2. Implement machine learning detection model (6 точки)
3. Design UEBA behavioral baseline framework (4 точки)

**Теоретични въпроси (12 точки):**
1. Compare EPP vs EDR vs XDR capabilities и use cases (4 точки)
2. Explain behavioral analysis techniques и machine learning integration (4 точки)
3. Describe MITRE ATT&CK framework integration в endpoint security (4 точки)

### **🎯 Практическо оценяване:**

**💼 Проект 4.1: Enterprise Endpoint Protection Architecture (35 точки)**

**Рубрика за оценяване:**
- **Platform Integration (12 точки):** EPP/EDR/XDR deployment, configuration, optimization
- **ML Detection Implementation (10 точки):** Custom models, behavioral analysis, accuracy
- **UEBA Framework (8 точки):** User profiling, anomaly detection, risk scoring
- **Attack Pattern Recognition (5 точки):** MITRE mapping, chain analysis, automation

**Критерии за успех:**
- **Отличен (31-35):** Enterprise-grade endpoint protection със innovation
- **Много добър (26-30):** Strong technical implementation с minor optimization
- **Добър (19-25):** Functional solution с improvement opportunities
- **Среден (12-18):** Basic understanding requiring significant development

### **🏠 Домашна работа:**

**📋 Assignment 4.1: "Next-Generation Endpoint Security Strategy" (Due next week)**

**Task:** Design comprehensive endpoint security strategy за large enterprise

**Requirements:**
1. **Technology Assessment (40%)**
   - EPP/EDR/XDR platform comparison
   - AI/ML integration strategy
   - Zero-day protection approach
   - Performance optimization plan

2. **Implementation Planning (35%)**
   - Deployment architecture design
   - Integration roadmap
   - Training program development
   - Success metrics definition

3. **Operational Framework (25%)**
   - UEBA implementation strategy
   - Attack pattern detection
   - Incident response integration
   - Continuous improvement process

**Format:** Technical strategy document (12-15 pages) с architecture diagrams, implementation timeline, и ROI analysis

---
