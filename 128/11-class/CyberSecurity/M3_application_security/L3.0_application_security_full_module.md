**Теоретични въпроси (12 точки):**
1. Compare OWASP API Security Top 10 с traditional web vulnerabilities (4 точки)
2. Explain OAuth 2.0 vs. OpenID Connect differences и use cases (4 точки)
3. Design zero-trust microservices security architecture (4 точки)

### **🎯 Практическо оценяване:**

**💼 Проект 3.4: Complete API Security Ecosystem (40 точки)**

**Рубрика за оценяване:**
- **REST API Security (12 точки):** OWASP API Top 10 coverage, input validation, authorization
- **OAuth 2.0/OIDC Implementation (10 точки):** Complete authentication flows, PKCE, security
- **Microservices Security (10 точки):** Service mesh integration, mTLS, zero-trust design
- **Monitoring & Response (8 точки):** Real-time threat detection, automated response

**Критерии за успех:**
- **Отличен (36-40):** Enterprise-grade API security implementation
- **Много добър (30-35):** Strong security architecture с minor enhancements needed
- **Добър (23-29):** Functional security implementation с improvement opportunities
- **Среден (16-22):** Basic understanding requiring significant security hardening

### **🏠 Домашна работа:**

**📋 Assignment 3.4: "Modern API Security Architecture Design" (Due next week)**

**Task:** Design comprehensive API security architecture за large-scale application

**Requirements:**
1. **Architecture Design (45%)**
   - Complete API security blueprint
   - OAuth 2.0/OIDC integration design
   - Microservices security patterns
   - Zero-trust implementation strategy

2. **Security Implementation (35%)**
   - Detailed security control specifications
   - Authentication и authorization workflows
   - Rate limiting и DDoS protection
   - Monitoring и incident response procedures

3. **Compliance Documentation (20%)**
   - GDPR compliance measures
   - API security standards adherence
   - Risk assessment documentation
   - Security testing strategy

**Format:** Technical architecture document (10-12 pages) с diagrams, code examples, и implementation guidelines

---

### **🛡️ Упражнение 3.5.3: DevSecOps Culture Transformation Program (20 мин)**

**Organizational Challenge:** Implement security-first development culture

**Transformation Scope:**
- Security champions program
- Developer security training
- Security awareness campaigns
- Gamified security education
- Security metrics и recognition

**Program Development:**

**Phase 1: Security Champions Initiative (8 мин)**
- Design security champions program
- Create nomination и selection process
- Develop training curriculum
- Set up mentorship framework

**Phase 2: Education и Awareness (7 мин)**
- Create security training modules
- Design gamified security challenges
- Implement continuous education program
- Set up knowledge sharing platform

**Phase 3: Culture Measurement (5 мин)**
- Define culture transformation metrics
- Implement feedback collection system
- Create recognition programs
- Design continuous improvement process

**Deliverable:** Complete DevSecOps culture transformation program с implementation roadmap

### **📊 Упражнение 3.5.4: Security Automation и Orchestration Platform (25 мин)**

**Platform Objective:** Comprehensive security automation ecosystem

**Automation Components:**
- Security tool orchestration
- Incident response automation
- Compliance automation
- Security metrics automation
- Threat intelligence integration

**Development Process:**

**Phase 1: Security Tool Integration (10 мин)**
- Design tool orchestration architecture
- Implement automated workflows
- Configure tool communication
- Set up data aggregation

**Phase 2: Automation Workflows (8 мин)**
- Create incident response automation
- Implement compliance checking
- Set up security monitoring
- Configure automated remediation

**Phase 3: Intelligence Integration (7 мин)**
- Integrate threat intelligence feeds
- Implement automated threat hunting
- Set up predictive analytics
- Configure automated response

**Deliverable:** Production-ready security automation platform с comprehensive orchestration

---

## 📊 **ОЦЕНЯВАНЕ КОМПОНЕНТ 3.5**

### **📝 Формативно оценяване:**

**🧪 Тест 3.5: DevSecOps и Automation (15 мин, 25 точки)**

**Практически задачи (15 точки):**
1. Design CI/CD security pipeline с automated gates (7 точки)
2. Configure Infrastructure as Code security scanning (4 точки)
3. Implement security metrics collection system (4 точки)

**Теоретични въпроси (10 точки):**
1. Explain DevSecOps culture transformation strategies (3 точки)
2. Compare security automation tools и their integration (4 точки)
3. Design continuous security education program (3 точки)

### **🎯 Практическо оценяване:**

**💼 Проект 3.5: Complete DevSecOps Transformation (35 точки)**

**Рубрика за оценяване:**
- **CI/CD Security Integration (12 точки):** Comprehensive pipeline security, automated gates, tools integration
- **Infrastructure Security Automation (10 точки):** IaC security, policy validation, compliance automation
- **Culture Transformation (8 точки):** Security champions, training programs, awareness initiatives
- **Security Automation Platform (5 точки):** Tool orchestration, workflow automation, intelligence integration

**Критерии за успех:**
- **Отличен (31-35):** Industry-leading DevSecOps implementation с innovation
- **Много добър (26-30):** Strong DevSecOps practices с minor enhancements needed
- **Добър (19-25):** Functional DevSecOps approach с optimization opportunities
- **Среден (12-18):** Basic understanding requiring significant development

### **🏠 Домашна работа:**

**📋 Assignment 3.5: "Enterprise DevSecOps Strategy" (Due next week)**

**Task:** Develop comprehensive DevSecOps transformation strategy за large organization

**Requirements:**
1. **Strategic Planning (40%)**
   - DevSecOps maturity assessment
   - Transformation roadmap development
   - Culture change management strategy
   - Success metrics definition

2. **Technical Implementation (35%)**
   - Complete CI/CD security architecture
   - Infrastructure automation framework
   - Security tool integration design
   - Automation workflow specifications

3. **Organizational Change (25%)**
   - Security champions program design
   - Training и education strategy
   - Change management approach
   - Continuous improvement framework

**Format:** Executive strategy document (15-20 pages) с technical appendices, implementation timeline, и budget projections

---

### **📈 ФИНАЛНИ СТАТИСТИКИ НА МОДУЛ 3:**

**✅ ВСИЧКИ КОМПОНЕНТИ ЗАВЪРШЕНИ:**

### **🌟 ПРОФЕСИОНАЛНИ УМЕНИЯ ПРИДОБИТИ:**


---

### **⚙️ КОМПОНЕНТ 3.5: DEVSECOPS И AUTOMATED SECURITY INTEGRATION (2 часа)**

#### **🎯 СПЕЦИФИЧНИ ЦЕЛИ НА КОМПОНЕНТА:**

**ЗНАНИЯ:**
- Разбиране на DevSecOps principles и security-first development culture
- Познаване на CI/CD security integration patterns и automated testing strategies
- Владеене на Infrastructure as Code security practices и configuration management
- Знание за security automation tools и pipeline orchestration techniques
- Разбиране на continuous security monitoring и production observability approaches

**УМЕНИЯ:**
- Интегриране на security testing в CI/CD pipelines със automated quality gates
- Конфигуриране на Infrastructure as Code security scanning и compliance validation
- Имплементиране на security automation workflows и orchestration systems
- Управление на security incident response automation и remediation workflows
- Провеждане на DevSecOps culture transformation и team collaboration improvement

**КОМПЕТЕНТНОСТИ:**
- Strategic thinking за organizational DevSecOps transformation
- Leadership capabilities за cross-functional security team coordination
- Innovation mindset за security automation opportunity identification
- Continuous improvement approach за DevSecOps process optimization
- Business acumen за security investment justification и ROI demonstration

---

## 📚 **ЧАС 1: CI/CD SECURITY PIPELINE INTEGRATION (40 мин)**

### **⚙️ DevSecOps Pipeline Architecture (15 мин)**

**Complete DevSecOps Pipeline Design:**

```
Comprehensive DevSecOps Pipeline:
┌─────────────────────────────────────────────────────────────┐
│                    Source Code Management                   │
│  ├── Branch protection rules                               │
│  ├── Commit signing requirements                           │
│  ├── Security-focused code review                          │
│  └── Secrets detection в commits                           │
├─────────────────────────────────────────────────────────────┤
│                   Build & Security Scanning                │
│  ├── SAST - Static Application Security Testing           │
│  ├── SCA - Software Composition Analysis                  │
│  ├── Secret scanning и key detection                      │
│  ├── License compliance checking                          │
│  └── Build artifact signing                               │
├─────────────────────────────────────────────────────────────┤
│                    Test & Quality Assurance                │
│  ├── Unit test security validation                        │
│  ├── Integration test security scenarios                  │
│  ├── DAST - Dynamic Application Security Testing          │
│  ├── IAST - Interactive Application Security Testing      │
│  └── Performance security testing                         │
├─────────────────────────────────────────────────────────────┤
│                  Infrastructure Deployment                 │
│  ├── Infrastructure as Code scanning                      │
│  ├── Container security scanning                          │
│  ├── Kubernetes security validation                       │
│  ├── Cloud security posture assessment                    │
│  └── Network security configuration                       │
├─────────────────────────────────────────────────────────────┤
│                Production Security Monitoring              │
│  ├── Runtime application protection                       │
│  ├── Container runtime security                           │
│  ├── Network traffic analysis                             │
│  ├── Log security analysis                                │
│  └── Incident response automation                         │
└─────────────────────────────────────────────────────────────┘
```

**GitHub Actions DevSecOps Pipeline:**
```yaml
name: DevSecOps Security Pipeline
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # Security scanning jobs
  secret-scanning:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    
    - name: Run TruffleHog Secret Scanner
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        base: main
        head: HEAD
        extra_args: --debug --only-verified
    
    - name: Run GitLeaks Secret Detection
      uses: gitleaks/gitleaks-action@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  sast-analysis:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Run SonarQube Analysis
      uses: sonarqube-quality-gate-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        scanMetadataReportFile: target/sonar/report-task.txt
    
    - name: Run Semgrep SAST
      uses: returntocorp/semgrep-action@v1
      with:
        config: auto
        generateSarif: "1"
    
    - name: Upload SARIF results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: semgrep.sarif

  dependency-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Run OWASP Dependency Check
      uses: dependency-check/Dependency-Check_Action@main
      env:
        JAVA_HOME: /opt/jdk
      with:
        project: 'secure-app'
        path: '.'
        format: 'ALL'
        args: >
          --enableRetired
          --enableExperimental
          --failOnCVSS 7
    
    - name: Upload Dependency Check Results
      uses: actions/upload-artifact@v3
      with:
        name: dependency-check-report
        path: reports/

  build-and-test:
    needs: [secret-scanning, sast-analysis, dependency-check]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Build Application
      run: |
        ./mvnw clean compile
        ./mvnw package -DskipTests
    
    - name: Run Security Unit Tests
      run: ./mvnw test -Dtest=**/*SecurityTest
    
    - name: Build Docker Image
      run: |
        docker build -t secure-app:${{ github.sha }} .
        docker tag secure-app:${{ github.sha }} secure-app:latest

  container-security:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Run Trivy Container Scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'secure-app:latest'
        format: 'sarif'
        output: 'trivy-results.sarif'
    
    - name: Run Anchore Container Scan
      uses: anchore/scan-action@v3
      with:
        image: "secure-app:latest"
        fail-build: true
        severity-cutoff: high
    
    - name: Upload Trivy Results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: 'trivy-results.sarif'

  dast-testing:
    needs: build-and-test
    runs-on: ubuntu-latest
    services:
      app:
        image: secure-app:latest
        ports:
          - 8080:8080
    steps:
    - name: Wait for Application Startup
      run: |
        timeout 60 bash -c 'until curl -f http://localhost:8080/actuator/health; do sleep 2; done'
    
    - name: Run OWASP ZAP Baseline Scan
      uses: zaproxy/action-baseline@v0.7.0
      with:
        target: 'http://localhost:8080'
        rules_file_name: '.zap/rules.tsv'
        cmd_options: '-a'
    
    - name: Run OWASP ZAP Full Scan
      uses: zaproxy/action-full-scan@v0.4.0
      with:
        target: 'http://localhost:8080'
        rules_file_name: '.zap/rules.tsv'
        cmd_options: '-a -j'

  infrastructure-security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Run Terraform Security Scan
      uses: aquasecurity/tfsec-action@v1.0.0
      with:
        soft_fail: false
    
    - name: Run Checkov IaC Scanner
      uses: bridgecrewio/checkov-action@master
      with:
        directory: ./infrastructure
        framework: terraform,kubernetes,dockerfile
        output_format: sarif
        output_file_path: checkov-results.sarif
    
    - name: Upload Checkov Results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: checkov-results.sarif

  security-gate:
    needs: [container-security, dast-testing, infrastructure-security]
    runs-on: ubuntu-latest
    steps:
    - name: Security Quality Gate
      run: |
        # Aggregate security results и determine if deployment should proceed
        echo "Evaluating security quality gate..."
        
        # Check for critical vulnerabilities
        CRITICAL_VULNS=$(cat security-results.json | jq '.critical_count')
        HIGH_VULNS=$(cat security-results.json | jq '.high_count')
        
        if [ "$CRITICAL_VULNS" -gt 0 ]; then
          echo "❌ Security gate failed: $CRITICAL_VULNS critical vulnerabilities found"
          exit 1
        fi
        
        if [ "$HIGH_VULNS" -gt 5 ]; then
          echo "❌ Security gate failed: $HIGH_VULNS high-severity vulnerabilities (max 5 allowed)"
          exit 1
        fi
        
        echo "✅ Security gate passed: No critical vulnerabilities found"

  deploy:
    needs: security-gate
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - name: Deploy to Production
      run: |
        echo "Deploying secure application to production..."
        # Deployment logic here
```

### **🔧 Security Automation Tools Integration (12 мин)**

**Comprehensive Security Tools Orchestration:**

**Jenkins Security Pipeline:**
```groovy
pipeline {
    agent any
    
    environment {
        DOCKER_REGISTRY = 'secure-registry.company.com'
        SONAR_TOKEN = credentials('sonar-token')
        SECURITY_GATEWAY_URL = 'https://security-gateway.company.com'
    }
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
                script {
                    env.GIT_COMMIT_SHORT = sh(
                        script: "git rev-parse --short HEAD",
                        returnStdout: true
                    ).trim()
                }
            }
        }
        
        stage('Security Pre-Flight') {
            parallel {
                stage('Secret Scanning') {
                    steps {
                        script {
                            def secretScanResults = sh(
                                script: 'trufflehog filesystem . --json',
                                returnStdout: true
                            )
                            
                            if (secretScanResults.contains('"verified": true')) {
                                error "❌ Verified secrets found в repository!"
                            }
                        }
                    }
                }
                
                stage('License Compliance') {
                    steps {
                        sh '''
                            # FOSSA license scanning
                            fossa analyze
                            fossa test --timeout 300
                        '''
                    }
                }
                
                stage('Branch Protection Validation') {
                    steps {
                        script {
                            if (env.BRANCH_NAME == 'main') {
                                def prNumber = env.CHANGE_ID
                                if (!prNumber) {
                                    error "❌ Direct push to main branch не е allowed!"
                                }
                            }
                        }
                    }
                }
            }
        }
        
        stage('Build & SAST Analysis') {
            parallel {
                stage('Application Build') {
                    steps {
                        sh '''
                            ./mvnw clean compile
                            ./mvnw package -DskipTests
                        '''
                    }
                }
                
                stage('Static Security Analysis') {
                    steps {
                        // SonarQube security analysis
                        withSonarQubeEnv('SonarQube') {
                            sh '''
                                ./mvnw sonar:sonar \
                                  -Dsonar.projectKey=secure-app \
                                  -Dsonar.qualitygate.wait=true \
                                  -Dsonar.security-hotspots.analysis=true
                            '''
                        }
                        
                        // CodeQL analysis
                        sh '''
                            codeql database create codeql-db --language=java
                            codeql database analyze codeql-db \
                                --format=sarif-latest \
                                --output=security-results.sarif \
                                --sarif-category=security
                        '''
                        
                        // Semgrep analysis
                        sh '''
                            semgrep --config=auto \
                                --sarif \
                                --output=semgrep-results.sarif \
                                --severity=ERROR \
                                .
                        '''
                    }
                }
                
                stage('Dependency Vulnerability Scan') {
                    steps {
                        sh '''
                            # OWASP Dependency Check
                            dependency-check.sh \
                                --project "secure-app" \
                                --scan . \
                                --format ALL \
                                --failOnCVSS 7 \
                                --enableExperimental
                            
                            # Snyk dependency scanning
                            snyk test --severity-threshold=high
                            snyk monitor
                        '''
                    }
                }
            }
        }
        
        stage('Security Quality Gate') {
            steps {
                script {
                    // Aggregate security findings
                    def securityReport = [:]
                    
                    // Parse SonarQube results
                    def sonarResults = readJSON file: 'sonar-results.json'
                    securityReport.sonar = [
                        securityHotspots: sonarResults.issues.findAll { it.type == 'SECURITY_HOTSPOT' }.size(),
                        vulnerabilities: sonarResults.issues.findAll { it.type == 'VULNERABILITY' }.size()
                    ]
                    
                    // Parse dependency check results
                    def depCheckResults = readJSON file: 'dependency-check-report.json'
                    securityReport.dependencies = [
                        critical: depCheckResults.dependencies.sum { it.vulnerabilities?.findAll { v -> v.cvssv3?.baseScore >= 9.0 }?.size() ?: 0 },
                        high: depCheckResults.dependencies.sum { it.vulnerabilities?.findAll { v -> v.cvssv3?.baseScore >= 7.0 && v.cvssv3?.baseScore < 9.0 }?.size() ?: 0 }
                    ]
                    
                    // Security gate decision logic
                    if (securityReport.dependencies.critical > 0) {
                        error "❌ Security Gate Failed: ${securityReport.dependencies.critical} critical vulnerabilities в dependencies"
                    }
                    
                    if (securityReport.sonar.vulnerabilities > 0) {
                        error "❌ Security Gate Failed: ${securityReport.sonar.vulnerabilities} code vulnerabilities found"
                    }
                    
                    if (securityReport.dependencies.high > 10) {
                        currentBuild.result = 'UNSTABLE'
                        echo "⚠️ Security Gate Warning: ${securityReport.dependencies.high} high-severity vulnerabilities (threshold: 10)"
                    }
                    
                    echo "✅ Security Gate Passed: Application meets security requirements"
                }
            }
        }
        
        stage('Container Security') {
            when {
                expression { currentBuild.result != 'FAILURE' }
            }
            steps {
                script {
                    // Build container image
                    def image = docker.build("${DOCKER_REGISTRY}/secure-app:${env.GIT_COMMIT_SHORT}")
                    
                    // Container security scanning
                    sh """
                        # Trivy container scan
                        trivy image \
                            --severity CRITICAL,HIGH \
                            --format json \
                            --output trivy-results.json \
                            ${DOCKER_REGISTRY}/secure-app:${env.GIT_COMMIT_SHORT}
                        
                        # Anchore container scan
                        anchore-cli image add ${DOCKER_REGISTRY}/secure-app:${env.GIT_COMMIT_SHORT}
                        anchore-cli image wait ${DOCKER_REGISTRY}/secure-app:${env.GIT_COMMIT_SHORT}
                        anchore-cli image vuln ${DOCKER_REGISTRY}/secure-app:${env.GIT_COMMIT_SHORT} all
                        
                        # Docker Bench security
                        docker run --rm \
                            -v /var/run/docker.sock:/var/run/docker.sock \
                            -v /etc:/host/etc:ro \
                            docker/docker-bench-security
                    """
                    
                    // Validate container security results
                    def trivyResults = readJSON file: 'trivy-results.json'
                    def criticalCount = trivyResults.Results?.sum { it.Vulnerabilities?.findAll { v -> v.Severity == 'CRITICAL' }?.size() ?: 0 } ?: 0
                    
                    if (criticalCount > 0) {
                        error "❌ Container Security Failed: ${criticalCount} critical vulnerabilities в container image"
                    }
                    
                    // Push secure image
                    image.push()
                    image.push('latest')
                }
            }
        }
        
        stage('Infrastructure Security Validation') {
            steps {
                sh '''
                    # Terraform security validation
                    tfsec ./infrastructure --format json --out tfsec-results.json
                    
                    # Checkov Infrastructure as Code scanning
                    checkov -d ./infrastructure \
                        --framework terraform,kubernetes \
                        --output json \
                        --output-file checkov-results.json
                    
                    # OPA policy validation
                    opa test ./policies/ --verbose
                    opa fmt ./policies/ --diff
                '''
                
                script {
                    def tfsecResults = readJSON file: 'tfsec-results.json'
                    def highSeverityIssues = tfsecResults.results?.findAll { it.severity == 'HIGH' || it.severity == 'CRITICAL' }?.size() ?: 0
                    
                    if (highSeverityIssues > 0) {
                        currentBuild.result = 'UNSTABLE'
                        echo "⚠️ Infrastructure Security Warning: ${highSeverityIssues} high/critical issues found"
                    }
                }
            }
        }
    }
    
    post {
        always {
            // Archive security reports
            archiveArtifacts artifacts: '*-results.json,*-results.sarif', allowEmptyArchive: true
            
            // Publish security test results
            publishHTML([
                allowMissing: false,
                alwaysLinkToLastBuild: true,
                keepAll: true,
                reportDir: 'reports',
                reportFiles: 'security-dashboard.html',
                reportName: 'Security Report'
            ])
            
            // Send security notifications
            script {
                def securitySummary = generateSecuritySummary()
                
                if (currentBuild.result == 'FAILURE') {
                    slackSend(
                        channel: '#security-alerts',
                        color: 'danger',
                        message: "🚨 Security Pipeline Failed: ${env.JOB_NAME} - ${env.BUILD_NUMBER}\n${securitySummary}"
                    )
                } else if (currentBuild.result == 'UNSTABLE') {
                    slackSend(
                        channel: '#security-warnings',
                        color: 'warning',
                        message: "⚠️ Security Pipeline Warning: ${env.JOB_NAME} - ${env.BUILD_NUMBER}\n${securitySummary}"
                    )
                }
            }
        }
        
        success {
            echo "✅ DevSecOps Pipeline completed successfully!"
        }
        
        failure {
            echo "❌ DevSecOps Pipeline failed due to security issues"
        }
    }
}

def generateSecuritySummary() {
    // Generate comprehensive security summary
    return """
    Security Pipeline Summary:
    • SAST Analysis: Completed
    • Dependency Check: Completed  
    • Container Scan: Completed
    • Infrastructure Validation: Completed
    • Security Gate: ${currentBuild.result ?: 'PASSED'}
    
    Build: ${env.BUILD_URL}
    """
}
```

### **📊 Security Metrics и Continuous Monitoring (13 мин)**

**DevSecOps Metrics Dashboard:**

**Security KPI Tracking Service:**
```java
// DevSecOps Security Metrics Service
@Service
public class DevSecOpsMetricsService {
    
    private final MeterRegistry meterRegistry;
    private final SecurityDataRepository securityDataRepository;
    
    @EventListener
    public void handlePipelineEvent(PipelineSecurityEvent event) {
        // Track pipeline security metrics
        recordPipelineMetrics(event);
        
        // Update security dashboard
        updateSecurityDashboard(event);
        
        // Check for security trend anomalies
        analyzeSecurityTrends(event);
    }
    
    private void recordPipelineMetrics(PipelineSecurityEvent event) {
        // Pipeline success/failure rates
        meterRegistry.counter("devsecops.pipeline.executions",
            "result", event.getResult(),
            "stage", event.getStage(),
            "project", event.getProjectName())
            .increment();
        
        // Security finding metrics
        meterRegistry.gauge("devsecops.vulnerabilities.critical",
            Tags.of("project", event.getProjectName()),
            event.getCriticalVulnerabilities());
        
        meterRegistry.gauge("devsecops.vulnerabilities.high",
            Tags.of("project", event.getProjectName()),
            event.getHighVulnerabilities());
        
        // Time to remediation tracking
        if (event.getRemediationTime() != null) {
            meterRegistry.timer("devsecops.remediation.time",
                "severity", event.getSeverity(),
                "project", event.getProjectName())
                .record(event.getRemediationTime(), TimeUnit.SECONDS);
        }
        
        // Security tool effectiveness
        meterRegistry.counter("devsecops.tools.findings",
            "tool", event.getSecurityTool(),
            "type", event.getFindingType())
            .increment();
        
        // Deployment frequency with security validation
        if (event.isDeploymentEvent()) {
            meterRegistry.counter("devsecops.deployments.total",
                "security_validated", String.valueOf(event.isSecurityValidated()))
                .increment();
        }
    }
    
    @Scheduled(fixedRate = 300000) // Every 5 minutes
    public void generateSecurityMetrics() {
        // Calculate rolling security metrics
        SecurityMetrics metrics = calculateSecurityMetrics();
        
        // Mean Time to Detection (MTTD)
        double mttd = calculateMeanTimeToDetection();
        meterRegistry.gauge("devsecops.mttd.hours", mttd);
        
        // Mean Time to Remediation (MTTR)
        double mttr = calculateMeanTimeToRemediation();
        meterRegistry.gauge("devsecops.mttr.hours", mttr);
        
        // Security debt accumulation
        double securityDebt = calculateSecurityDebt();
        meterRegistry.gauge("devsecops.security_debt.score", securityDebt);
        
        // Pipeline security coverage
        double securityCoverage = calculateSecurityCoverage();
        meterRegistry.gauge("devsecops.security_coverage.percentage", securityCoverage);
        
        // Compliance score
        double complianceScore = calculateComplianceScore();
        meterRegistry.gauge("devsecops.compliance.score", complianceScore);
    }
    
    private SecurityMetrics calculateSecurityMetrics() {
        LocalDateTime now = LocalDateTime.now();
        LocalDateTime weekAgo = now.minusWeeks(1);
        
        List<SecurityFinding> recentFindings = securityDataRepository
            .findByCreatedAtBetween(weekAgo, now);
        
        return SecurityMetrics.builder()
            .totalFindings(recentFindings.size())
            .criticalFindings(recentFindings.stream()
                .mapToInt(f -> f.getSeverity() == Severity.CRITICAL ? 1 : 0)
                .sum())
            .resolvedFindings(recentFindings.stream()
                .mapToInt(f -> f.getStatus() == Status.RESOLVED ? 1 : 0)
                .sum())
            .averageResolutionTime(calculateAverageResolutionTime(recentFindings))
            .build();
    }
    
    @EventListener
    public void handleSecurityIncident(SecurityIncidentEvent incident) {
        // Track security incident metrics
        meterRegistry.counter("devsecops.incidents.total",
            "severity", incident.getSeverity().toString(),
            "source", incident.getSource(),
            "type", incident.getIncidentType())
            .increment();
        
        // Alert on critical incidents
        if (incident.getSeverity() == IncidentSeverity.CRITICAL) {
            alertingService.sendCriticalAlert(
                "Critical Security Incident Detected",
                String.format("Incident: %s, Source: %s, Time: %s", 
                    incident.getDescription(), 
                    incident.getSource(), 
                    incident.getTimestamp())
            );
        }
        
        // Track incident response time
        if (incident.getResponseTime() != null) {
            meterRegistry.timer("devsecops.incident.response_time",
                "severity", incident.getSeverity().toString())
                .record(incident.getResponseTime(), TimeUnit.MINUTES);
        }
    }
}
```

**Security Observability Dashboard:**
```java
// Security Observability Controller
@RestController
@RequestMapping("/api/security/observability")
@PreAuthorize("hasRole('SECURITY_ENGINEER')")
public class SecurityObservabilityController {
    
    @Autowired
    private SecurityMetricsService metricsService;
    
    @Autowired
    private SecurityTrendService trendService;
    
    @GetMapping("/dashboard")
    public ResponseEntity<SecurityDashboard> getSecurityDashboard(
            @RequestParam(defaultValue = "7d") String timeRange) {
        
        Duration duration = parseDuration(timeRange);
        
        SecurityDashboard dashboard = SecurityDashboard.builder()
            .metrics(metricsService.getMetrics(duration))
            .trends(trendService.getSecurityTrends(duration))
            .alerts(metricsService.getActiveAlerts())
            .pipelineStatus(metricsService.getPipelineSecurityStatus())
            .complianceStatus(metricsService.getComplianceStatus())
            .build();
        
        return ResponseEntity.ok(dashboard);
    }
    
    @GetMapping("/metrics/vulnerabilities")
    public ResponseEntity<VulnerabilityMetrics> getVulnerabilityMetrics(
            @RequestParam(defaultValue = "30d") String timeRange,
            @RequestParam(required = false) String project) {
        
        Duration duration = parseDuration(timeRange);
        VulnerabilityMetrics metrics;
        
        if (project != null) {
            metrics = metricsService.getProjectVulnerabilityMetrics(project, duration);
        } else {
            metrics = metricsService.getOrganizationVulnerabilityMetrics(duration);
        }
        
        return ResponseEntity.ok(metrics);
    }
    
    @GetMapping("/metrics/pipeline")
    public ResponseEntity<PipelineSecurityMetrics> getPipelineMetrics() {
        PipelineSecurityMetrics metrics = metricsService.getPipelineSecurityMetrics();
        return ResponseEntity.ok(metrics);
    }
    
    @GetMapping("/trends/security-debt")
    public ResponseEntity<List<SecurityDebtTrend>> getSecurityDebtTrends(
            @RequestParam(defaultValue = "90d") String timeRange) {
        
        Duration duration = parseDuration(timeRange);
        List<SecurityDebtTrend> trends = trendService.getSecurityDebtTrends(duration);
        
        return ResponseEntity.ok(trends);
    }
    
    @GetMapping("/compliance/status")
    public ResponseEntity<ComplianceStatus> getComplianceStatus() {
        ComplianceStatus status = ComplianceStatus.builder()
            .gdprCompliance(metricsService.getGDPRComplianceScore())
            .pciDssCompliance(metricsService.getPCIDSSComplianceScore())
            .iso27001Compliance(metricsService.getISO27001ComplianceScore())
            .nistFramework(metricsService.getNISTFrameworkScore())
            .lastAssessment(metricsService.getLastComplianceAssessment())
            .nextAssessment(metricsService.getNextComplianceAssessment())
            .build();
        
        return ResponseEntity.ok(status);
    }
    
    @PostMapping("/alerts/acknowledge")
    public ResponseEntity<Void> acknowledgeAlert(@RequestBody AlertAcknowledgement ack) {
        metricsService.acknowledgeAlert(ack.getAlertId(), ack.getAcknowledgedBy(), ack.getNotes());
        return ResponseEntity.ok().build();
    }
}
```

---

## 📚 **ЧАС 2: INFRASTRUCTURE AS CODE SECURITY И DEVSECOPS CULTURE (40 мин)**

### **🔧 Infrastructure as Code Security Automation (20 мин)**

**Terraform Security Scanning Integration:**

**Secure Terraform Module Example:**
```hcl
# Secure AWS Infrastructure Module
terraform {
  required_version = ">= 1.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# Security-hardened VPC configuration
resource "aws_vpc" "secure_vpc" {
  cidr_block           = var.vpc_cidr
  enable_dns_hostnames = true
  enable_dns_support   = true
  
  # Security: Enable VPC flow logs
  tags = {
    Name        = "${var.environment}-secure-vpc"
    Environment = var.environment
    Security    = "compliant"
  }
}

# VPC Flow Logs за security monitoring
resource "aws_flow_log" "vpc_flow_log" {
  iam_role_arn    = aws_iam_role.flow_log_role.arn
  log_destination = aws_cloudwatch_log_group.vpc_flow_log.arn
  traffic_type    = "ALL"
  vpc_id          = aws_vpc.secure_vpc.id
}

# Security-focused security group
resource "aws_security_group" "secure_app_sg" {
  name_prefix = "${var.environment}-secure-app-"
  vpc_id      = aws_vpc.secure_vpc.id
  
  # Restrict inbound traffic
  ingress {
    description = "HTTPS from ALB only"
    from_port   = 443
    to_port     = 443
    protocol    = "tcp"
    security_groups = [aws_security_group.alb_sg.id]
  }
  
  # Allow outbound HTTPS only
  egress {
    description = "HTTPS outbound"
    from_port   = 443
    to_port     = 443
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }
  
  # Security: No SSH access в production
  dynamic "ingress" {
    for_each = var.environment == "production" ? [] : [1]
    content {
      description = "SSH для development only"
      from_port   = 22
      to_port     = 22
      protocol    = "tcp"
      cidr_blocks = var.admin_cidr_blocks
    }
  }
  
  tags = {
    Name        = "${var.environment}-secure-app-sg"
    Environment = var.environment
  }
}

# WAF за application protection
resource "aws_wafv2_web_acl" "secure_app_waf" {
  name  = "${var.environment}-secure-app-waf"
  scope = "REGIONAL"
  
  default_action {
    allow {}
  }
  
  # OWASP Top 10 protection rules
  rule {
    name     = "AWSManagedRulesCommonRuleSet"
    priority = 1
    
    override_action {
      none {}
    }
    
    statement {
      managed_rule_group_statement {
        name        = "AWSManagedRulesCommonRuleSet"
        vendor_name = "AWS"
      }
    }
    
    visibility_config {
      cloudwatch_metrics_enabled = true
      metric_name                 = "CommonRuleSetMetric"
      sampled_requests_enabled    = true
    }
  }
  
  # SQL injection protection
  rule {
    name     = "AWSManagedRulesSQLiRuleSet"
    priority = 2
    
    override_action {
      none {}
    }
    
    statement {
      managed_rule_group_statement {
        name        = "AWSManagedRulesSQLiRuleSet"
        vendor_name = "AWS"
      }
    }
    
    visibility_config {
      cloudwatch_metrics_enabled = true
      metric_name                 = "SQLiRuleSetMetric"
      sampled_requests_enabled    = true
    }
  }
  
  tags = {
    Environment = var.environment
    Security    = "waf-enabled"
  }
}

# Secure S3 bucket configuration
resource "aws_s3_bucket" "secure_app_bucket" {
  bucket = "${var.environment}-secure-app-${random_id.bucket_suffix.hex}"
  
  tags = {
    Environment = var.environment
    Security    = "encrypted"
  }
}

# S3 bucket encryption
resource "aws_s3_bucket_server_side_encryption_configuration" "secure_app_bucket_encryption" {
  bucket = aws_s3_bucket.secure_app_bucket.id
  
  rule {
    apply_server_side_encryption_by_default {
      kms_master_key_id = aws_kms_key.s3_key.arn
      sse_algorithm     = "aws:kms"
    }
    bucket_key_enabled = true
  }
}

# S3 bucket public access block
resource "aws_s3_bucket_public_access_block" "secure_app_bucket_pab" {
  bucket = aws_s3_bucket.secure_app_bucket.id
  
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# Security validation using tfsec
# tfsec:ignore:aws-s3-encryption-customer-key
resource "aws_s3_bucket_versioning" "secure_app_bucket_versioning" {
  bucket = aws_s3_bucket.secure_app_bucket.id
  versioning_configuration {
    status = "Enabled"
  }
}

# CloudTrail за audit logging
resource "aws_cloudtrail" "security_audit_trail" {
  name           = "${var.environment}-security-audit-trail"
  s3_bucket_name = aws_s3_bucket.audit_logs_bucket.bucket
  
  enable_logging                = true
  enable_log_file_validation    = true
  include_global_service_events = true
  is_multi_region_trail         = true
  
  event_selector {
    read_write_type                 = "All"
    include_management_events       = true
    exclude_management_event_sources = []
    
    data_resource {
      type   = "AWS::S3::Object"
      values = ["${aws_s3_bucket.secure_app_bucket.arn}/*"]
    }
  }
  
  tags = {
    Environment = var.environment
    Security    = "audit-enabled"
  }
}
```

**Terraform Security Validation Pipeline:**
```yaml
# Terraform Security Pipeline
name: Infrastructure Security Validation
on:
  push:
    paths:
      - 'infrastructure/**'
  pull_request:
    paths:
      - 'infrastructure/**'

jobs:
  terraform-security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: 1.5.0
    
    - name: Terraform Format Check
      run: terraform fmt -check -recursive infrastructure/
    
    - name: Terraform Init
      run: |
        cd infrastructure/
        terraform init -backend=false
    
    - name: Terraform Validate
      run: |
        cd infrastructure/
        terraform validate
    
    - name: Run tfsec Security Scan
      uses: aquasecurity/tfsec-action@v1.0.0
      with:
        soft_fail: false
        tfsec_args: --minimum-severity MEDIUM
    
    - name: Run Checkov IaC Security Scan
      uses: bridgecrewio/checkov-action@master
      with:
        directory: infrastructure/
        framework: terraform
        output_format: sarif
        output_file_path: checkov-results.sarif
        soft_fail: false
    
    - name: Run Terrascan Security Scan
      uses: tenable/terrascan-action@main
      with:
        iac_type: terraform
        iac_version: v14
        policy_type: aws
        only_warn: false
        sarif_upload: true
    
    - name: OPA Policy Validation
      run: |
        # Install OPA
        curl -L -o opa https://github.com/open-policy-agent/opa/releases/download/v0.55.0/opa_linux_amd64_static
        chmod +x opa
        
        # Run policy tests
        ./opa test policies/ --verbose
        
        # Validate infrastructure against policies
        ./opa eval -d policies/ -i infrastructure/main.tf \
          'data.terraform.security.violations[_]' \
          --format pretty
    
    - name: Terraform Security Compliance Check
      run: |
        # Custom compliance validation script
        python3 scripts/terraform-compliance-check.py \
          --config compliance-config.yaml \
          --terraform-dir infrastructure/ \
          --output compliance-report.json
    
    - name: Upload Security Results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: checkov-results.sarif
    
    - name: Generate Security Summary
      run: |
        # Aggregate security findings
        python3 scripts/aggregate-security-findings.py \
          --tfsec-results tfsec-results.json \
          --checkov-results checkov-results.sarif \
          --terrascan-results terrascan-results.json \
          --output security-summary.json
    
    - name: Security Gate Evaluation
      run: |
        # Evaluate security findings against thresholds
        CRITICAL_ISSUES=$(jq '.critical_count' security-summary.json)
        HIGH_ISSUES=$(jq '.high_count' security-summary.json)
        
        if [ "$CRITICAL_ISSUES" -gt 0 ]; then
          echo "❌ Security gate failed: $CRITICAL_ISSUES critical issues found"
          exit 1
        fi
        
        if [ "$HIGH_ISSUES" -gt 5 ]; then
          echo "❌ Security gate failed: $HIGH_ISSUES high-severity issues (max 5 allowed)"
          exit 1
        fi
        
        echo "✅ Infrastructure security validation passed"
```

### **🛡️ DevSecOps Culture и Best Practices (20 мин)**

**DevSecOps Culture Transformation Framework:**

**Security Champions Program:**
```java
// Security Champions Program Management
@Service
public class SecurityChampionsService {
    
    @Autowired
    private UserService userService;
    
    @Autowired
    private TrainingService trainingService;
    
    @Autowired
    private SecurityMetricsService metricsService;
    
    public void nominateSecurityChampion(SecurityChampionNomination nomination) {
        // Validate nomination criteria
        if (!isEligibleForChampionRole(nomination.getNomineeId())) {
            throw new IllegalArgumentException("Nominee does not meet eligibility criteria");
        }
        
        User nominee = userService.findById(nomination.getNomineeId());
        
        // Create security champion profile
        SecurityChampion champion = SecurityChampion.builder()
            .userId(nominee.getId())
            .nominator(nomination.getNominator())
            .teamId(nomination.getTeamId())
            .specializations(nomination.getSpecializations())
            .nominationDate(LocalDateTime.now())
            .status(ChampionStatus.PENDING_APPROVAL)
            .trainingProgress(initializeTrainingProgress())
            .build();
        
        securityChampionRepository.save(champion);
        
        // Start onboarding process
        initiateChampionOnboarding(champion);
        
        // Notify security team
        securityTeamNotificationService.notifyNewChampionNomination(champion);
    }
    
    public ChampionTrainingPlan createTrainingPlan(Long championId) {
        SecurityChampion champion = securityChampionRepository.findById(championId)
            .orElseThrow(() -> new EntityNotFoundException("Security champion not found"));
        
        return ChampionTrainingPlan.builder()
            .championId(championId)
            .coreSecurityModules(List.of(
                "OWASP Top 10 Deep Dive",
                "Secure Coding Practices",
                "Security Testing Methodologies",
                "Incident Response Basics",
                "DevSecOps Pipeline Integration"
            ))
            .specializationModules(getSpecializationModules(champion.getSpecializations()))
            .hands_onLabs(List.of(
                "Vulnerability Assessment Lab",
                "Penetration Testing Workshop",
                "Security Code Review Practice",
                "Incident Simulation Exercise"
            ))
            .certificationTargets(List.of(
                "OWASP Security Champion Certification",
                "DevSecOps Foundation Certificate"
            ))
            .milestones(createTrainingMilestones())
            .estimatedDuration(Duration.ofWeeks(12))
            .build();
    }
    
    @Scheduled(fixedRate = 604800000) // Weekly
    public void generateChampionMetrics() {
        List<SecurityChampion> activeChampions = securityChampionRepository
            .findByStatus(ChampionStatus.ACTIVE);
        
        ChampionProgramMetrics metrics = ChampionProgramMetrics.builder()
            .totalChampions(activeChampions.size())
            .championsByTeam(groupChampionsByTeam(activeChampions))
            .trainingCompletionRate(calculateTrainingCompletionRate(activeChampions))
            .vulnerabilityDetectionRate(calculateVulnerabilityDetectionByChampions())
            .securityImprovementMetrics(calculateSecurityImprovements())
            .build();
        
        metricsService.recordChampionMetrics(metrics);
        
        // Generate monthly champion recognition report
        generateChampionRecognitionReport(activeChampions);
    }
    
    private SecurityImprovementMetrics calculateSecurityImprovements() {
        LocalDateTime lastMonth = LocalDateTime.now().minusMonths(1);
        
        return SecurityImprovementMetrics.builder()
            .vulnerabilitiesFound(countVulnerabilitiesFoundByChampions(lastMonth))
            .securityPracticesAdopted(countSecurityPracticesAdopted(lastMonth))
            .securityTrainingCompleted(countSecurityTrainingCompleted(lastMonth))
            .codeReviewsWithSecurityFocus(countSecurityFocusedCodeReviews(lastMonth))
            .securityIncidentsPrevented(estimatePreventedIncidents(lastMonth))
            .build();
    }
}
```

**Security-First Development Practices:**
```java
// Security-First Development Guidelines Service
@Service
public class SecurityFirstDevelopmentService {
    
    public DevelopmentSecurityGuidelines generateProjectGuidelines(ProjectType projectType, 
                                                                  SecurityRequirements requirements) {
        DevelopmentSecurityGuidelines.Builder guidelines = DevelopmentSecurityGuidelines.builder();
        
        // Core security practices
        guidelines.coreSecurityPractices(List.of(
            SecurityPractice.builder()
                .practice("Threat Modeling")
                .description("Conduct threat modeling for all new features")
                .implementation("Use STRIDE methodology, document threats в project wiki")
                .validationCriteria("Threat model reviewed by security champion")
                .tools(List.of("Microsoft Threat Modeling Tool", "OWASP Threat Dragon"))
                .build(),
            
            SecurityPractice.builder()
                .practice("Secure Code Review")
                .description("Security-focused code review for all pull requests")
                .implementation("Use security code review checklist, require security champion approval")
                .validationCriteria("Security checklist completed, no high-severity findings")
                .tools(List.of("SonarQube", "GitHub Security Tab", "Checkmarx"))
                .build(),
            
            SecurityPractice.builder()
                .practice("Dependency Security")
                .description("Automated dependency vulnerability scanning")
                .implementation("Integrate OWASP Dependency Check в CI/CD pipeline")
                .validationCriteria("No critical/high vulnerabilities в dependencies")
                .tools(List.of("OWASP Dependency Check", "Snyk", "GitHub Dependabot"))
                .build()
        ));
        
        // Project-specific security requirements
        if (projectType == ProjectType.WEB_APPLICATION) {
            guidelines.webApplicationSecurity(WebAppSecurityGuidelines.builder()
                .inputValidation("Implement comprehensive input validation using whitelist approach")
                .outputEncoding("Use context-appropriate output encoding (HTML, JavaScript, URL)")
                .authenticationRequirements("Implement multi-factor authentication for sensitive operations")
                .sessionManagement("Use secure session management с proper timeout и invalidation")
                .csrfProtection("Implement CSRF tokens for all state-changing operations")
                .xssProtection("Implement Content Security Policy и XSS protection headers")
                .build());
        }
        
        if (projectType == ProjectType.API) {
            guidelines.apiSecurity(APISecurityGuidelines.builder()
                .authenticationMechanism("Implement OAuth 2.0 с JWT tokens")
                .authorizationModel("Use role-based access control (RBAC)")
                .rateLimiting("Implement rate limiting (100 requests/minute per client)")
                .inputValidation("Validate all input using JSON schema validation")
                .errorHandling("Return generic error messages, log detailed errors securely")
                .apiDocumentation("Document security requirements в OpenAPI specification")
                .build());
        }
        
        // Security testing requirements
        guidelines.securityTesting(SecurityTestingGuidelines.builder()
            .unitSecurityTests("Write security-focused unit tests for authentication/authorization")
            .integrationSecurityTests("Test security controls в integration test scenarios")
            .sastRequirements("SAST must pass с no critical/high findings")
            .dastRequirements("DAST must pass OWASP Top 10 validation")
            .penetrationTesting("Annual penetration testing by external security firm")
            .build());
        
        return guidelines.build();
    }
    
    public SecurityTrainingPlan createDeveloperTrainingPlan(Developer developer) {
        DeveloperSkillAssessment assessment = assessDeveloperSecuritySkills(developer);
        
        return SecurityTrainingPlan.builder()
            .developerId(developer.getId())
            .currentSkillLevel(assessment.getOverallSecurityLevel())
            .targetSkillLevel(SecuritySkillLevel.PROFICIENT)
            .trainingModules(selectTrainingModules(assessment))
            .hands_onExercises(selectPracticalExercises(assessment))
            .mentorship(assignSecurityMentor(developer))
            .evaluationMilestones(createEvaluationMilestones())
            .estimatedCompletion(calculateTrainingDuration(assessment))
            .build();
    }
    
    private List<TrainingModule> selectTrainingModules(DeveloperSkillAssessment assessment) {
        List<TrainingModule> modules = new ArrayList<>();
        
        // Core security fundamentals
        if (assessment.getSecurityFundamentals() < SecuritySkillLevel.COMPETENT) {
            modules.add(TrainingModule.builder()
                .name("Security Fundamentals")
                .description("Basic security principles и concepts")
                .duration(Duration.ofHours(8))
                .prerequisites(List.of())
                .learningObjectives(List.of(
                    "Understand CIA triad",
                    "Recognize common attack vectors",
                    "Apply defense-in-depth principles"
                ))
                .assessmentCriteria("Pass security fundamentals quiz с 80% score")
                .build());
        }
        
        // Secure coding practices
        if (assessment.getSecureCoding() < SecuritySkillLevel.COMPETENT) {
            modules.add(TrainingModule.builder()
                .name("Secure Coding Practices")
                .description("Language-specific secure coding techniques")
                .duration(Duration.ofHours(12))
                .prerequisites(List.of("Security Fundamentals"))
                .learningObjectives(List.of(
                    "Implement input validation",
                    "Prevent injection vulnerabilities",
                    "Handle authentication securely",
                    "Manage sessions properly"
                ))
                .assessmentCriteria("Complete secure coding lab exercises")
                .build());
        }
        
        // Application security testing
        if (assessment.getSecurityTesting() < SecuritySkillLevel.COMPETENT) {
            modules.add(TrainingModule.builder()
                .name("Application Security Testing")
                .description("Security testing methodologies и tools")
                .duration(Duration.ofHours(10))
                .prerequisites(List.of("Secure Coding Practices"))
                .learningObjectives(List.of(
                    "Perform security code reviews",
                    "Use SAST/DAST tools effectively",
                    "Design security test cases",
                    "Interpret security test results"
                ))
                .assessmentCriteria("Successfully identify и fix vulnerabilities в test application")
                .build());
        }
        
        return modules;
    }
}
```

**Continuous Security Education Program:**
```java
// Continuous Security Education Service
@Service
public class ContinuousSecurityEducationService {
    
    @Scheduled(cron = "0 0 9 * * MON") // Every Monday at 9 AM
    public void deliverWeeklySecurityTip() {
        SecurityTip weeklyTip = generateWeeklySecurityTip();
        
        // Send to all developers
        List<Developer> developers = userService.getAllDevelopers();
        developers.forEach(developer -> {
            SecurityTipDelivery delivery = SecurityTipDelivery.builder()
                .developerId(developer.getId())
                .securityTip(weeklyTip)
                .deliveryDate(LocalDateTime.now())
                .channel(selectOptimalDeliveryChannel(developer))
                .build();
            
            deliverSecurityTip(delivery);
        });
    }
    
    @EventListener
    public void handleSecurityIncident(SecurityIncidentEvent incident) {
        // Generate learning material from security incidents
        if (incident.getSeverity().ordinal() >= IncidentSeverity.MEDIUM.ordinal()) {
            SecurityLearningCase learningCase = SecurityLearningCase.builder()
                .incidentId(incident.getIncidentId())
                .title("Learning from Incident: " + incident.getType())
                .description(sanitizeIncidentForLearning(incident))
                .lessonLearned(extractLessonsLearned(incident))
                .preventionMeasures(identifyPreventionMeasures(incident))
                .codeExamples(generateSecureCodeExamples(incident))
                .relatedVulnerabilities(identifyRelatedVulnerabilities(incident))
                .build();
            
            // Share с relevant teams
            shareSecurityLearningCase(learningCase, incident.getAffectedTeams());
        }
    }
    
    public GameifiedSecurityChallenge createMonthlySecurityChallenge() {
        SecurityChallengeTheme theme = selectChallengeTheme();
        
        return GameifiedSecurityChallenge.builder()
            .title("Monthly Security Challenge: " + theme.getName())
            .description(theme.getDescription())
            .difficulty(theme.getDifficulty())
            .challengeType(theme.getChallengeType())
            .startDate(LocalDateTime.now().with(TemporalAdjusters.firstDayOfNextMonth()))
            .endDate(LocalDateTime.now().with(TemporalAdjusters.lastDayOfMonth().plusMonths(1)))
            .objectives(theme.getLearningObjectives())
            .tasks(generateChallengeTasks(theme))
            .scoringCriteria(createScoringCriteria(theme))
            .rewards(defineRewards(theme.getDifficulty()))
            .leaderboard(initializeLeaderboard())
            .build();
    }
    
    private SecurityTip generateWeeklySecurityTip() {
        // Rotate through different security topics
        SecurityTopic topic = selectWeeklyTopic();
        
        return SecurityTip.builder()
            .topic(topic)
            .title(topic.getTipTitle())
            .content(topic.getTipContent())
            .codeExample(topic.getCodeExample())
            .additionalResources(topic.getAdditionalResources())
            .difficulty(topic.getDifficulty())
            .estimatedReadTime(topic.getEstimatedReadTime())
            .relatedTools(topic.getRelatedTools())
            .actionableSteps(topic.getActionableSteps())
            .build();
    }
    
    @Scheduled(cron = "0 0 10 1 * ?") // First day of every month at 10 AM
    public void generateSecurityEducationReport() {
        SecurityEducationReport report = SecurityEducationReport.builder()
            .reportPeriod(LocalDateTime.now().minusMonths(1))
            .participationMetrics(calculateParticipationMetrics())
            .skillImprovementMetrics(calculateSkillImprovements())
            .vulnerabilityReductionMetrics(calculateVulnerabilityReduction())
            .programEffectiveness(assessProgramEffectiveness())
            .recommendations(generateProgramRecommendations())
            .build();
        
        // Share с security leadership
        securityLeadershipService.shareEducationReport(report);
        
        // Update training programs based на findings
        updateTrainingPrograms(report.getRecommendations());
    }
}
```

---

## 🛠️ **ПРАКТИЧЕСКИ УПРАЖНЕНИЯ КОМПОНЕНТ 3.5**

### **⚙️ Упражнение 3.5.1: Complete DevSecOps Pipeline Implementation (30 мин)**

**Project:** End-to-end secure CI/CD pipeline design

**Pipeline Requirements:**
- Multi-stage security testing
- Infrastructure as Code security
- Container security scanning
- Automated security gates
- Security metrics collection

**Implementation Tasks:**

**Phase 1: CI/CD Security Integration (12 мин)**
- Configure SAST/DAST/SCA integration
- Implement security quality gates
- Set up automated security testing
- Configure security notification system

**Phase 2: Infrastructure Security Automation (10 мин)**
- Integrate Terraform security scanning
- Set up OPA policy validation
- Configure compliance checking
- Implement security drift detection

**Phase 3: Monitoring и Metrics (8 мин)**
- Implement security metrics collection
- Set up security dashboards
- Configure automated alerting
- Create security reporting automation

**Deliverable:** Production-ready DevSecOps pipeline със comprehensive security automation

### **🔧 Упражнение 3.5.2: Infrastructure as Code Security Framework (25 мин)**

**Scenario:** Multi-cloud infrastructure security implementation

**Infrastructure Components:**
- AWS VPC и networking
- Kubernetes cluster deployment
- Database security configuration
- Monitoring и logging setup
- Backup и disaster recovery

**Development Process:**

**Phase 1: Secure Infrastructure Design (10 мин)**
- Design security-hardened infrastructure
- Implement security best practices
- Configure encryption и access controls
- Set up audit logging

**Phase 2: Security Scanning Integration (8 мин)**
- Configure tfsec/Checkov scanning
- Implement OPA policy validation
- Set up compliance checking
- Add security testing automation

**Phase 3: Deployment Security (7 мин)**
- Configure secure deployment pipeline
- Implement security monitoring
- Set up incident response
- Test security controls

**Deliverable**

---

### **🔗 КОМПОНЕНТ 3.4: API SECURITY И MICROSERVICES PROTECTION (3 часа)**

#### **🎯 СПЕЦИФИЧНИ ЦЕЛИ НА КОМПОНЕНТА:**

**ЗНАНИЯ:**
- Разбиране на REST API security principles и OWASP API Security Top 10
- Познаване на OAuth 2.0, OpenID Connect, и JWT token security mechanisms
- Владеене на microservices security patterns и service mesh architectures
- Знание за API gateway security capabilities и centralized enforcement strategies
- Разбиране на container security и orchestration platform protection measures

**УМЕНИЯ:**
- Проектиране и имплементиране на secure REST API endpoints със comprehensive protection
- Конфигуриране на OAuth 2.0/OIDC authentication flows и JWT token validation
- Изпълнение на microservices security implementations със service mesh integration
- Управление на API gateway security policies и rate limiting mechanisms
- Провеждане на API security testing и vulnerability assessments

**КОМПЕТЕНТНОСТИ:**
- Modern security architecture thinking за distributed system protection
- DevSecOps mindset за automated API security integration
- Risk assessment capabilities за microservices attack surface analysis
- Technical leadership в API security program establishment
- Continuous monitoring approach за API threat detection и response

---

## 📚 **ЧАС 1: REST API SECURITY DESIGN И IMPLEMENTATION (40 мин)**

### **🔗 REST API Security Fundamentals (15 мин)**

**OWASP API Security Top 10 2023:**

```
OWASP API Security Top 10 2023 Critical Risks:
┌─────┬─────────────────────────────────┬───────────────┬──────────────────┐
│ Rank│ API Security Risk               │ Prevalence    │ Business Impact  │
├─────┼─────────────────────────────────┼───────────────┼──────────────────┤
│ API1│ Broken Object Level Auth       │ Very High     │ Critical        │
│ API2│ Broken Authentication          │ High          │ Critical        │
│ API3│ Broken Object Property Level   │ High          │ High            │
│ API4│ Unrestricted Resource Consumption│ Medium      │ High            │
│ API5│ Broken Function Level Auth     │ High          │ High            │
│ API6│ Unrestricted Access to Sensitive│ Medium       │ High            │
│ API7│ Server Side Request Forgery    │ Medium        │ High            │
│ API8│ Security Misconfiguration      │ High          │ Medium          │
│ API9│ Improper Inventory Management  │ High          │ Medium          │
│ API10│ Unsafe Consumption of APIs    │ Medium        │ Medium          │
└─────┴─────────────────────────────────┴───────────────┴──────────────────┘
```

**API1: Broken Object Level Authorization Deep Dive:**

**Attack Scenario Example:**
```
Vulnerable API Endpoint:
GET /api/users/{userId}/orders/{orderId}

Attack Vector:
1. Legitimate request: GET /api/users/123/orders/456
2. Attacker modifies: GET /api/users/123/orders/789
3. Server fails to verify order ownership
4. Attacker accesses other users' order data

Business Impact:
├── Data privacy violations
├── Regulatory compliance breaches
├── Customer trust erosion
├── Competitive intelligence exposure
└── Legal liability risks
```

**Secure Object Level Authorization Implementation:**
```java
// Secure API Controller с Object-Level Authorization
@RestController
@RequestMapping("/api")
@PreAuthorize("hasRole('USER')")
public class SecureOrderController {
    
    @Autowired
    private OrderService orderService;
    
    @Autowired
    private AuthorizationService authService;
    
    @GetMapping("/users/{userId}/orders/{orderId}")
    public ResponseEntity<OrderResponse> getUserOrder(
            @PathVariable Long userId,
            @PathVariable Long orderId,
            Authentication authentication) {
        
        // Step 1: Verify user identity
        String currentUsername = authentication.getName();
        if (!authService.isUserAuthorized(currentUsername, userId)) {
            throw new AccessDeniedException("Cannot access other users' data");
        }
        
        // Step 2: Verify object ownership
        Order order = orderService.findById(orderId);
        if (order == null) {
            throw new ResourceNotFoundException("Order not found");
        }
        
        if (!order.getUserId().equals(userId)) {
            throw new AccessDeniedException("Order does not belong to user");
        }
        
        // Step 3: Apply data filtering based on permissions
        OrderResponse response = authService.filterOrderData(order, authentication);
        
        // Step 4: Log access attempt
        auditService.logDataAccess(currentUsername, "ORDER_ACCESS", orderId);
        
        return ResponseEntity.ok(response);
    }
    
    // Bulk operations требват additional security
    @GetMapping("/users/{userId}/orders")
    @PreAuthorize("@authService.canAccessUserOrders(#userId, authentication)")
    public ResponseEntity<List<OrderResponse>> getUserOrders(
            @PathVariable Long userId,
            @RequestParam(defaultValue = "0") int page,
            @RequestParam(defaultValue = "10") int size,
            Authentication authentication) {
        
        // Implement pagination limits to prevent data dumping
        if (size > 100) {
            throw new IllegalArgumentException("Page size cannot exceed 100");
        }
        
        Pageable pageable = PageRequest.of(page, size);
        Page<Order> orders = orderService.findByUserId(userId, pageable);
        
        List<OrderResponse> responses = orders.getContent().stream()
            .map(order -> authService.filterOrderData(order, authentication))
            .collect(Collectors.toList());
        
        return ResponseEntity.ok(responses);
    }
}
```

**API Rate Limiting и Resource Protection:**
```java
// Advanced Rate Limiting Implementation
@Component
public class APIRateLimitingFilter implements Filter {
    
    private final RedisTemplate<String, String> redisTemplate;
    private final RateLimitingConfig config;
    
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, 
                        FilterChain chain) throws IOException, ServletException {
        
        HttpServletRequest httpRequest = (HttpServletRequest) request;
        HttpServletResponse httpResponse = (HttpServletResponse) response;
        
        String clientId = extractClientIdentifier(httpRequest);
        String endpoint = httpRequest.getRequestURI();
        
        // Apply endpoint-specific rate limits
        RateLimitRule rule = config.getRulеForEndpoint(endpoint);
        
        if (!checkRateLimit(clientId, endpoint, rule)) {
            httpResponse.setStatus(429); // Too Many Requests
            httpResponse.setHeader("Retry-After", String.valueOf(rule.getWindowSeconds()));
            httpResponse.setHeader("X-RateLimit-Limit", String.valueOf(rule.getRequestsPerWindow()));
            httpResponse.setHeader("X-RateLimit-Remaining", "0");
            
            // Return rate limit exceeded response
            ObjectMapper mapper = new ObjectMapper();
            ErrorResponse error = new ErrorResponse(
                "RATE_LIMIT_EXCEEDED",
                "API rate limit exceeded. Please retry after " + rule.getWindowSeconds() + " seconds"
            );
            httpResponse.getWriter().write(mapper.writeValueAsString(error));
            return;
        }
        
        chain.doFilter(request, response);
    }
    
    private boolean checkRateLimit(String clientId, String endpoint, RateLimitRule rule) {
        String key = String.format("rate_limit:%s:%s", clientId, endpoint);
        long currentWindow = System.currentTimeMillis() / (rule.getWindowSeconds() * 1000);
        String windowKey = key + ":" + currentWindow;
        
        // Sliding window rate limiting using Redis
        Long currentCount = redisTemplate.opsForValue().increment(windowKey);
        
        if (currentCount == 1) {
            // Set expiration for the window
            redisTemplate.expire(windowKey, Duration.ofSeconds(rule.getWindowSeconds()));
        }
        
        return currentCount <= rule.getRequestsPerWindow();
    }
    
    private String extractClientIdentifier(HttpServletRequest request) {
        // Priority: API Key > JWT Subject > IP Address
        String apiKey = request.getHeader("X-API-Key");
        if (apiKey != null) {
            return "api_key:" + apiKey;
        }
        
        String authHeader = request.getHeader("Authorization");
        if (authHeader != null && authHeader.startsWith("Bearer ")) {
            try {
                String token = authHeader.substring(7);
                Claims claims = jwtService.validateToken(token);
                return "user:" + claims.getSubject();
            } catch (Exception e) {
                // Fall back to IP-based limiting
            }
        }
        
        return "ip:" + getClientIpAddress(request);
    }
}
```

### **🔐 API Input Validation и Security Headers (12 мин)**

**Comprehensive API Input Validation Framework:**

**Request Validation Pipeline:**
```java
// API Request Validation Framework
@Component
public class APIValidationService {
    
    // JSON Schema Validation
    public void validateJsonPayload(String jsonPayload, String schemaName) {
        try {
            JsonSchema schema = loadJsonSchema(schemaName);
            JsonNode jsonNode = objectMapper.readTree(jsonPayload);
            
            Set<ValidationMessage> violations = schema.validate(jsonNode);
            if (!violations.isEmpty()) {
                throw new ValidationException(
                    "JSON payload validation failed: " + violations.toString()
                );
            }
        } catch (Exception e) {
            throw new ValidationException("Invalid JSON payload", e);
        }
    }
    
    // Content Type Validation
    public void validateContentType(String contentType, List<String> allowedTypes) {
        if (contentType == null || !allowedTypes.contains(contentType.toLowerCase())) {
            throw new UnsupportedMediaTypeException(
                "Content type must be one of: " + String.join(", ", allowedTypes)
            );
        }
    }
    
    // Request Size Validation
    public void validateRequestSize(long contentLength, long maxSize) {
        if (contentLength > maxSize) {
            throw new PayloadTooLargeException(
                String.format("Request size %d exceeds maximum allowed size %d", 
                             contentLength, maxSize)
            );
        }
    }
    
    // Parameter Injection Prevention
    public Map<String, Object> sanitizeParameters(Map<String, Object> parameters) {
        Map<String, Object> sanitized = new HashMap<>();
        
        for (Map.Entry<String, Object> entry : parameters.entrySet()) {
            String key = entry.getKey();
            Object value = entry.getValue();
            
            // Validate parameter names (prevent parameter pollution)
            if (!isValidParameterName(key)) {
                throw new ValidationException("Invalid parameter name: " + key);
            }
            
            // Sanitize parameter values based on expected type
            Object sanitizedValue = sanitizeParameterValue(value);
            sanitized.put(key, sanitizedValue);
        }
        
        return sanitized;
    }
    
    private boolean isValidParameterName(String name) {
        // Allow only alphanumeric characters и underscores
        return name.matches("^[a-zA-Z0-9_]+$") && name.length() <= 50;
    }
    
    private Object sanitizeParameterValue(Object value) {
        if (value instanceof String) {
            String strValue = (String) value;
            
            // Remove potentially dangerous characters
            strValue = strValue.replaceAll("[<>\"'&]", "");
            
            // Limit string length
            if (strValue.length() > 1000) {
                strValue = strValue.substring(0, 1000);
            }
            
            return strValue;
        }
        
        return value; // Numbers, booleans, etc. pass through
    }
}
```

**API Security Headers Configuration:**
```java
// Comprehensive API Security Headers
@Configuration
public class APISecurityHeadersConfig {
    
    @Bean
    public FilterRegistrationBean<APISecurityHeadersFilter> securityHeadersFilter() {
        FilterRegistrationBean<APISecurityHeadersFilter> registration = 
            new FilterRegistrationBean<>();
        registration.setFilter(new APISecurityHeadersFilter());
        registration.addUrlPatterns("/api/*");
        registration.setOrder(1);
        return registration;
    }
}

@Component
public class APISecurityHeadersFilter implements Filter {
    
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, 
                        FilterChain chain) throws IOException, ServletException {
        
        HttpServletResponse httpResponse = (HttpServletResponse) response;
        
        // API-specific security headers
        httpResponse.setHeader("X-Content-Type-Options", "nosniff");
        httpResponse.setHeader("X-Frame-Options", "DENY");
        httpResponse.setHeader("X-XSS-Protection", "1; mode=block");
        httpResponse.setHeader("Referrer-Policy", "strict-origin-when-cross-origin");
        
        // API versioning и deprecation headers
        httpResponse.setHeader("API-Version", "v1.2.0");
        httpResponse.setHeader("API-Deprecation", "false");
        
        // Rate limiting headers
        httpResponse.setHeader("X-RateLimit-Limit", "1000");
        httpResponse.setHeader("X-RateLimit-Window", "3600");
        
        // CORS headers for API access
        httpResponse.setHeader("Access-Control-Allow-Origin", "https://trusted-domain.com");
        httpResponse.setHeader("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS");
        httpResponse.setHeader("Access-Control-Allow-Headers", 
            "Authorization, Content-Type, X-API-Key, X-Requested-With");
        httpResponse.setHeader("Access-Control-Max-Age", "86400");
        
        // Security policy headers
        httpResponse.setHeader("Strict-Transport-Security", 
            "max-age=31536000; includeSubDomains; preload");
        
        chain.doFilter(request, response);
    }
}
```

### **🛡️ API Error Handling и Information Disclosure Prevention (13 мин)**

**Secure API Error Handling Framework:**

**Production-Safe Error Responses:**
```java
// Secure API Error Handling
@ControllerAdvice
@RestController
public class APIErrorHandler {
    
    private static final Logger logger = LoggerFactory.getLogger(APIErrorHandler.class);
    
    // Generic error response structure
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResponse> handleGenericException(Exception e, 
                                                              HttpServletRequest request) {
        // Log detailed error для debugging (не expose to client)
        String errorId = UUID.randomUUID().toString();
        logger.error("API Error [{}]: {} at {}", errorId, e.getMessage(), 
                    request.getRequestURI(), e);
        
        // Return generic error message
        ErrorResponse response = new ErrorResponse(
            "INTERNAL_SERVER_ERROR",
            "An internal error occurred. Please contact support with error ID: " + errorId,
            errorId
        );
        
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(response);
    }
    
    // Validation errors (safe to expose)
    @ExceptionHandler(ValidationException.class)
    public ResponseEntity<ErrorResponse> handleValidationException(ValidationException e) {
        ErrorResponse response = new ErrorResponse(
            "VALIDATION_ERROR",
            "Request validation failed",
            sanitizeValidationErrors(e.getErrors())
        );
        
        return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(response);
    }
    
    // Authentication errors
    @ExceptionHandler(AuthenticationException.class)
    public ResponseEntity<ErrorResponse> handleAuthenticationException(AuthenticationException e) {
        // Don't reveal authentication details
        ErrorResponse response = new ErrorResponse(
            "AUTHENTICATION_REQUIRED",
            "Valid authentication credentials are required"
        );
        
        return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(response);
    }
    
    // Authorization errors
    @ExceptionHandler(AccessDeniedException.class)
    public ResponseEntity<ErrorResponse> handleAccessDeniedException(AccessDeniedException e) {
        // Don't reveal what resources exist
        ErrorResponse response = new ErrorResponse(
            "ACCESS_DENIED",
            "You don't have permission to access this resource"
        );
        
        return ResponseEntity.status(HttpStatus.FORBIDDEN).body(response);
    }
    
    // Resource not found
    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<ErrorResponse> handleResourceNotFoundException(ResourceNotFoundException e) {
        ErrorResponse response = new ErrorResponse(
            "RESOURCE_NOT_FOUND",
            "The requested resource was not found"
        );
        
        return ResponseEntity.status(HttpStatus.NOT_FOUND).body(response);
    }
    
    // Rate limiting errors
    @ExceptionHandler(RateLimitExceededException.class)
    public ResponseEntity<ErrorResponse> handleRateLimitException(RateLimitExceededException e) {
        ErrorResponse response = new ErrorResponse(
            "RATE_LIMIT_EXCEEDED",
            "API rate limit exceeded. Please retry after the specified time.",
            Map.of("retryAfter", e.getRetryAfterSeconds())
        );
        
        HttpHeaders headers = new HttpHeaders();
        headers.add("Retry-After", String.valueOf(e.getRetryAfterSeconds()));
        
        return ResponseEntity.status(HttpStatus.TOO_MANY_REQUESTS)
            .headers(headers)
            .body(response);
    }
    
    private List<String> sanitizeValidationErrors(List<String> errors) {
        // Remove sensitive information от validation errors
        return errors.stream()
            .map(error -> error.replaceAll("\\b\\d{4,}\\b", "[REDACTED]")) // Remove numbers
            .map(error -> error.replaceAll("\\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Z|a-z]{2,}\\b", "[EMAIL]")) // Remove emails
            .collect(Collectors.toList());
    }
}

// Secure Error Response Model
public class ErrorResponse {
    private String code;
    private String message;
    private String errorId;
    private Map<String, Object> details;
    private Instant timestamp;
    
    public ErrorResponse(String code, String message) {
        this.code = code;
        this.message = message;
        this.timestamp = Instant.now();
    }
    
    public ErrorResponse(String code, String message, String errorId) {
        this(code, message);
        this.errorId = errorId;
    }
    
    public ErrorResponse(String code, String message, Map<String, Object> details) {
        this(code, message);
        this.details = details;
    }
    
    // Getters и setters...
}
```

**API Logging и Monitoring Security:**
```java
// Secure API Audit Logging
@Component
public class APIAuditLogger {
    
    private static final Logger auditLogger = LoggerFactory.getLogger("API_AUDIT");
    private final ObjectMapper objectMapper = new ObjectMapper();
    
    public void logAPICall(HttpServletRequest request, HttpServletResponse response, 
                          String username, long processingTime) {
        try {
            APIAuditEvent event = APIAuditEvent.builder()
                .timestamp(Instant.now())
                .username(username)
                .method(request.getMethod())
                .endpoint(request.getRequestURI())
                .queryParams(sanitizeQueryParams(request.getParameterMap()))
                .userAgent(request.getHeader("User-Agent"))
                .clientIp(getClientIpAddress(request))
                .responseStatus(response.getStatus())
                .processingTimeMs(processingTime)
                .requestId(MDC.get("requestId"))
                .build();
            
            auditLogger.info(objectMapper.writeValueAsString(event));
            
            // Send to SIEM или security monitoring system
            securityMonitoringService.processAuditEvent(event);
            
        } catch (Exception e) {
            // Don't fail the request if logging fails
            logger.error("Failed to log API audit event", e);
        }
    }
    
    private Map<String, String> sanitizeQueryParams(Map<String, String[]> parameterMap) {
        Map<String, String> sanitized = new HashMap<>();
        
        for (Map.Entry<String, String[]> entry : parameterMap.entrySet()) {
            String key = entry.getKey();
            String[] values = entry.getValue();
            
            // Don't log sensitive parameters
            if (isSensitiveParameter(key)) {
                sanitized.put(key, "[REDACTED]");
            } else {
                sanitized.put(key, String.join(",", values));
            }
        }
        
        return sanitized;
    }
    
    private boolean isSensitiveParameter(String paramName) {
        List<String> sensitiveParams = Arrays.asList(
            "password", "token", "secret", "key", "auth", "credential"
        );
        
        return sensitiveParams.stream()
            .anyMatch(sensitive -> paramName.toLowerCase().contains(sensitive));
    }
}
```

---

## 📚 **ЧАС 2: OAUTH 2.0, JWT И MODERN AUTHENTICATION PROTOCOLS (40 мин)**

### **🎟️ OAuth 2.0 Implementation и Security Best Practices (18 мин)**

**OAuth 2.0 Secure Authorization Server:**

**Complete OAuth 2.0 Authorization Server Implementation:**
```java
// Secure OAuth 2.0 Authorization Server Configuration
@Configuration
@EnableAuthorizationServer
public class OAuth2AuthorizationServerConfig {
    
    @Autowired
    private BCryptPasswordEncoder passwordEncoder;
    
    @Autowired
    private UserDetailsService userDetailsService;
    
    @Bean
    @Primary
    public DefaultTokenServices tokenServices() {
        DefaultTokenServices tokenServices = new DefaultTokenServices();
        tokenServices.setSupportRefreshToken(true);
        tokenServices.setTokenStore(tokenStore());
        tokenServices.setClientDetailsService(clientDetailsService());
        tokenServices.setAuthenticationManager(authenticationManager);
        
        // Security configurations
        tokenServices.setAccessTokenValiditySeconds(15 * 60); // 15 minutes
        tokenServices.setRefreshTokenValiditySeconds(7 * 24 * 60 * 60); // 7 days
        
        return tokenServices;
    }
    
    @Bean
    public TokenStore tokenStore() {
        // Use Redis для distributed token storage
        return new RedisTokenStore(jedisConnectionFactory());
    }
    
    @Bean
    public ClientDetailsService clientDetailsService() {
        return new JdbcClientDetailsService(dataSource()) {
            @Override
            public ClientDetails loadClientByClientId(String clientId) 
                    throws ClientRegistrationException {
                
                ClientDetails client = super.loadClientByClientId(clientId);
                
                // Additional security validations
                validateClientSecurity(client);
                
                return client;
            }
        };
    }
    
    @Override
    public void configure(AuthorizationServerEndpointsConfigurer endpoints) {
        endpoints
            .tokenStore(tokenStore())
            .tokenServices(tokenServices())
            .userDetailsService(userDetailsService)
            .authenticationManager(authenticationManager)
            .tokenEnhancer(tokenEnhancerChain())
            .exceptionTranslator(oauth2ResponseExceptionTranslator());
    }
    
    @Override
    public void configure(AuthorizationServerSecurityConfigurer security) {
        security
            .tokenKeyAccess("hasAuthority('ROLE_TRUSTED_CLIENT')")
            .checkTokenAccess("hasAuthority('ROLE_TRUSTED_CLIENT')")
            .allowFormAuthenticationForClients()
            .passwordEncoder(passwordEncoder)
            .sslOnly() // Require HTTPS
            .realm("oauth2/client");
    }
    
    private void validateClientSecurity(ClientDetails client) {
        // Validate redirect URIs
        for (String redirectUri : client.getRegisteredRedirectUri()) {
            if (!isSecureRedirectUri(redirectUri)) {
                throw new InvalidClientException("Insecure redirect URI: " + redirectUri);
            }
        }
        
        // Validate grant types
        if (client.getAuthorizedGrantTypes().contains("password")) {
            // Password grant should only be used by trusted first-party clients
            if (!client.getAuthorities().stream()
                    .anyMatch(auth -> auth.getAuthority().equals("ROLE_TRUSTED_CLIENT"))) {
                throw new InvalidClientException("Password grant not allowed for this client");
            }
        }
    }
    
    private boolean isSecureRedirectUri(String redirectUri) {
        // Must use HTTPS except for localhost development
        return redirectUri.startsWith("https://") || 
               redirectUri.startsWith("http://localhost") ||
               redirectUri.startsWith("http://127.0.0.1");
    }
}
```

**PKCE (Proof Key for Code Exchange) Implementation:**
```java
// PKCE Support για Enhanced Security
@Service
public class PKCEService {
    
    private final SecureRandom secureRandom = new SecureRandom();
    
    public PKCEChallenge generatePKCEChallenge() {
        // Generate code verifier (43-128 characters)
        String codeVerifier = generateCodeVerifier();
        
        // Generate code challenge using SHA256
        String codeChallenge = generateCodeChallenge(codeVerifier);
        
        return new PKCEChallenge(codeVerifier, codeChallenge, "S256");
    }
    
    public boolean validatePKCEChallenge(String codeVerifier, String codeChallenge, 
                                        String challengeMethod) {
        if (!"S256".equals(challengeMethod)) {
            throw new InvalidGrantException("Unsupported PKCE challenge method");
        }
        
        String expectedChallenge = generateCodeChallenge(codeVerifier);
        return MessageDigest.isEqual(
            expectedChallenge.getBytes(StandardCharsets.UTF_8),
            codeChallenge.getBytes(StandardCharsets.UTF_8)
        );
    }
    
    private String generateCodeVerifier() {
        byte[] bytes = new byte[32]; // 256 bits
        secureRandom.nextBytes(bytes);
        return Base64.getUrlEncoder().withoutPadding().encodeToString(bytes);
    }
    
    private String generateCodeChallenge(String codeVerifier) {
        try {
            MessageDigest digest = MessageDigest.getInstance("SHA-256");
            byte[] hash = digest.digest(codeVerifier.getBytes(StandardCharsets.UTF_8));
            return Base64.getUrlEncoder().withoutPadding().encodeToString(hash);
        } catch (NoSuchAlgorithmException e) {
            throw new RuntimeException("SHA-256 not available", e);
        }
    }
}

// Enhanced Authorization Code Grant with PKCE
@RestController
@RequestMapping("/oauth")
public class PKCEAuthorizationController {
    
    @Autowired
    private PKCEService pkceService;
    
    @Autowired
    private AuthorizationCodeService authorizationCodeService;
    
    @PostMapping("/authorize")
    public ResponseEntity<Map<String, String>> authorize(
            @RequestParam String client_id,
            @RequestParam String redirect_uri,
            @RequestParam String scope,
            @RequestParam String state,
            @RequestParam String code_challenge,
            @RequestParam String code_challenge_method,
            Authentication authentication) {
        
        // Validate PKCE parameters
        if (!"S256".equals(code_challenge_method)) {
            throw new InvalidRequestException("code_challenge_method must be S256");
        }
        
        if (code_challenge == null || code_challenge.length() < 43) {
            throw new InvalidRequestException("Invalid code_challenge");
        }
        
        // Validate client и redirect URI
        ClientDetails client = clientDetailsService.loadClientByClientId(client_id);
        if (!client.getRegisteredRedirectUri().contains(redirect_uri)) {
            throw new InvalidRequestException("Invalid redirect_uri");
        }
        
        // Generate authorization code
        String authorizationCode = authorizationCodeService.createAuthorizationCode(
            client_id, authentication.getName(), scope, redirect_uri, 
            code_challenge, code_challenge_method);
        
        // Build redirect URL
        String redirectUrl = String.format("%s?code=%s&state=%s", 
                                          redirect_uri, authorizationCode, state);
        
        return ResponseEntity.ok(Map.of("redirect_url", redirectUrl));
    }
}
```

### **🔒 JWT Security Implementation и Best Practices (12 мин)**

**Secure JWT Token Service:**

**Production-Grade JWT Implementation:**
```java
// Secure JWT Token Service
@Service
public class SecureJWTService {
    
    private final String ISSUER = "MySecureAPI";
    private final Key signingKey;
    private final Key verificationKey;
    
    @Value("${jwt.access-token.expiry:900}") // 15 minutes
    private int accessTokenExpiry;
    
    @Value("${jwt.refresh-token.expiry:604800}") // 7 days
    private int refreshTokenExpiry;
    
    public SecureJWTService(@Value("${jwt.secret}") String secretKey) {
        // Use HMAC-SHA512 for symmetric key
        this.signingKey = new SecretKeySpec(
            secretKey.getBytes(StandardCharsets.UTF_8), 
            SignatureAlgorithm.HS512.getJcaName()
        );
        this.verificationKey = signingKey;
    }
    
    public TokenPair generateTokens(UserPrincipal userPrincipal) {
        Instant now = Instant.now();
        
        // Create access token with minimal claims
        String accessToken = Jwts.builder()
            .setIssuer(ISSUER)
            .setSubject(userPrincipal.getUsername())
            .setAudience("api")
            .setIssuedAt(Date.from(now))
            .setExpiration(Date.from(now.plusSeconds(accessTokenExpiry)))
            .setNotBefore(Date.from(now))
            .setId(UUID.randomUUID().toString()) // Unique token ID
            .claim("scope", userPrincipal.getAuthorities().stream()
                .map(GrantedAuthority::getAuthority)
                .collect(Collectors.toList()))
            .claim("type", "access")
            .claim("uid", userPrincipal.getUserId()) // User ID for faster lookups
            .signWith(signingKey, SignatureAlgorithm.HS512)
            .compact();
        
        // Create refresh token with different expiry
        String refreshToken = Jwts.builder()
            .setIssuer(ISSUER)
            .setSubject(userPrincipal.getUsername())
            .setAudience("refresh")
            .setIssuedAt(Date.from(now))
            .setExpiration(Date.from(now.plusSeconds(refreshTokenExpiry)))
            .setId(UUID.randomUUID().toString())
            .claim("type", "refresh")
            .claim("uid", userPrincipal.getUserId())
            .signWith(signingKey, SignatureAlgorithm.HS512)
            .compact();
        
        // Store refresh token для revocation capability
        storeRefreshToken(refreshToken, userPrincipal.getUsername());
        
        return new TokenPair(accessToken, refreshToken);
    }
    
    public Claims validateToken(String token) {
        try {
            Claims claims = Jwts.parserBuilder()
                .setSigningKey(verificationKey)
                .requireIssuer(ISSUER)
                .build()
                .parseClaimsJws(token)
                .getBody();
            
            // Additional security validations
            validateTokenSecurity(claims, token);
            
            return claims;
            
        } catch (JwtException e) {
            throw new InvalidTokenException("Invalid JWT token", e);
        }
    }
    
    private void validateTokenSecurity(Claims claims, String token) {
        // Check token blacklist
        if (isTokenBlacklisted(claims.getId())) {
            throw new InvalidTokenException("Token has been revoked");
        }
        
        // Validate token type
        String tokenType = claims.get("type", String.class);
        if (!"access".equals(tokenType) && !"refresh".equals(tokenType)) {
            throw new InvalidTokenException("Invalid token type");
        }
        
        // Check for token reuse (optional)
        if (detectTokenReuse(token, claims)) {
            // Log security incident
            securityEventService.logTokenReuseAttempt(claims.getSubject(), token);
            throw new InvalidTokenException("Potential token reuse detected");
        }
        
        // Validate token binding (optional)
        if (!validateTokenBinding(token, claims)) {
            throw new InvalidTokenException("Token binding validation failed");
        }
    }
    
    public TokenPair refreshTokens(String refreshToken) {
        Claims claims = validateToken(refreshToken);
        
        // Verify this is a refresh token
        if (!"refresh".equals(claims.get("type"))) {
            throw new InvalidTokenException("Invalid refresh token");
        }
        
        // Check if refresh token is still valid в database
        if (!isRefreshTokenValid(claims.getId(), claims.getSubject())) {
            throw new InvalidTokenException("Refresh token is no longer valid");
        }
        
        // Revoke old refresh token (refresh token rotation)
        revokeRefreshToken(claims.getId());
        
        // Load fresh user details
        UserPrincipal userPrincipal = userService.loadUserByUsername(claims.getSubject());
        
        // Generate new token pair
        return generateTokens(userPrincipal);
    }
    
    // Token revocation for logout
    public void revokeTokens(String refreshToken) {
        try {
            Claims claims = validateToken(refreshToken);
            String tokenId = claims.getId();
            
            // Add refresh token to blacklist
            revokeRefreshToken(tokenId);
            
            // Optionally blacklist associated access tokens
            blacklistUserTokens(claims.getSubject());
            
        } catch (Exception e) {
            // Log but don't fail - token might already be invalid
            logger.warn("Failed to revoke token", e);
        }
    }
}
```

### **🔗 OpenID Connect Integration (10 мин)**

**OpenID Connect Provider Implementation:**
```java
// OpenID Connect Provider Configuration
@Configuration
@EnableResourceServer
public class OpenIDConnectConfig {
    
    @Bean
    public JWKSource<SecurityContext> jwkSource() {
        // Generate RSA key pair за signing
        RSAKey rsaKey = generateRSAKey();
        JWKSet jwkSet = new JWKSet(rsaKey);
        return new ImmutableJWKSet<>(jwkSet);
    }
    
    @Bean
    public JwtDecoder jwtDecoder(JWKSource<SecurityContext> jwkSource) {
        return NimbusJwtDecoder.withJwkSetUri("/.well-known/jwks.json").build();
    }
    
    @Bean
    public JwtEncoder jwtEncoder(JWKSource<SecurityContext> jwkSource) {
        return new NimbusJwtEncoder(jwkSource);
    }
    
    private RSAKey generateRSAKey() {
        try {
            KeyPairGenerator keyPairGenerator = KeyPairGenerator.getInstance("RSA");
            keyPairGenerator.initialize(2048);
            KeyPair keyPair = keyPairGenerator.generateKeyPair();
            
            RSAPublicKey publicKey = (RSAPublicKey) keyPair.getPublic();
            RSAPrivateKey privateKey = (RSAPrivateKey) keyPair.getPrivate();
            
            return new RSAKey.Builder(publicKey)
                .privateKey(privateKey)
                .keyID(UUID.randomUUID().toString())
                .build();
        } catch (Exception e) {
            throw new IllegalStateException("Unable to generate RSA key pair", e);
        }
    }
}

// OpenID Connect ID Token Service
@Service
public class IDTokenService {
    
    @Autowired
    private JwtEncoder jwtEncoder;
    
    public String generateIDToken(UserPrincipal userPrincipal, String clientId, String nonce) {
        Instant now = Instant.now();
        
        JwtClaimsSet.Builder claimsBuilder = JwtClaimsSet.builder()
            .issuer("https://your-auth-server.com")
            .subject(userPrincipal.getUsername())
            .audience(List.of(clientId))
            .issuedAt(now)
            .expiresAt(now.plus(Duration.ofMinutes(60)))
            .claim("auth_time", now.getEpochSecond())
            .claim("azp", clientId); // Authorized party
        
        // Add nonce if provided (OIDC requirement)
        if (nonce != null) {
            claimsBuilder.claim("nonce", nonce);
        }
        
        // Add standard claims
        claimsBuilder
            .claim("name", userPrincipal.getFullName())
            .claim("given_name", userPrincipal.getFirstName())
            .claim("family_name", userPrincipal.getLastName())
            .claim("email", userPrincipal.getEmail())
            .claim("email_verified", userPrincipal.isEmailVerified())
            .claim("picture", userPrincipal.getProfilePictureUrl());
        
        JwtClaimsSet claims = claimsBuilder.build();
        
        return jwtEncoder.encode(JwtEncoderParameters.from(claims)).getTokenValue();
    }
}

// OpenID Connect Discovery Endpoint
@RestController
@RequestMapping("/.well-known")
public class OpenIDConnectDiscoveryController {
    
    @GetMapping("/openid_configuration")
    public ResponseEntity<Map<String, Object>> openidConfiguration() {
        Map<String, Object> configuration = new HashMap<>();
        
        String baseUrl = "https://your-auth-server.com";
        
        configuration.put("issuer", baseUrl);
        configuration.put("authorization_endpoint", baseUrl + "/oauth/authorize");
        configuration.put("token_endpoint", baseUrl + "/oauth/token");
        configuration.put("userinfo_endpoint", baseUrl + "/oauth/userinfo");
        configuration.put("jwks_uri", baseUrl + "/.well-known/jwks.json");
        configuration.put("registration_endpoint", baseUrl + "/oauth/register");
        configuration.put("revocation_endpoint", baseUrl + "/oauth/revoke");
        
        // Supported response types
        configuration.put("response_types_supported", 
            List.of("code", "id_token", "token id_token", "code id_token"));
        
        // Supported grant types
        configuration.put("grant_types_supported", 
            List.of("authorization_code", "refresh_token", "client_credentials"));
        
        // Supported scopes
        configuration.put("scopes_supported", 
            List.of("openid", "profile", "email", "phone", "address"));
        
        // Supported claims
        configuration.put("claims_supported", 
            List.of("sub", "name", "given_name", "family_name", "email", 
                   "email_verified", "picture", "locale"));
        
        // Security features
        configuration.put("code_challenge_methods_supported", List.of("S256"));
        configuration.put("token_endpoint_auth_methods_supported", 
            List.of("client_secret_basic", "client_secret_post", "private_key_jwt"));
        
        return ResponseEntity.ok(configuration);
    }
    
    @GetMapping("/jwks.json")
    public ResponseEntity<Map<String, Object>> jwks() {
        // Return public keys for token verification
        JWKSet jwkSet = loadPublicJWKSet();
        return ResponseEntity.ok(jwkSet.toJSONObject());
    }
}
```

---

## 📚 **ЧАС 3: MICROSERVICES SECURITY И API GATEWAY PROTECTION (40 мин)**

### **🏗️ Microservices Security Architecture (15 мин)**

**Zero-Trust Microservices Security:**

```
Zero-Trust Microservices Architecture:
┌─────────────────────────────────────────────────────────────┐
│                      API Gateway Layer                     │
│  ├── Authentication & Authorization                        │
│  ├── Rate limiting & DDoS protection                      │
│  ├── Request validation & filtering                       │
│  └── SSL termination & certificate management             │
├─────────────────────────────────────────────────────────────┤
│                     Service Mesh Layer                     │
│  ├── mTLS между all services                              │
│  ├── Service identity & verification                      │
│  ├── Encrypted inter-service communication                │
│  └── Traffic policy enforcement                           │
├─────────────────────────────────────────────────────────────┤
│                   Microservices Layer                      │
│  ├── Service-level authorization                          │
│  ├── Input validation & sanitization                      │
│  ├── Business logic protection                            │
│  └── Data access control                                  │
├─────────────────────────────────────────────────────────────┤
│                     Data Layer Security                    │
│  ├── Database-level access control                        │
│  ├── Data encryption at rest                              │
│  ├── Connection encryption (TLS)                          │
│  └── Audit logging & monitoring                           │
└─────────────────────────────────────────────────────────────┘
```

**Service Mesh Security с Istio:**
```yaml
# Istio Security Policy Configuration
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: production
spec:
  mtls:
    mode: STRICT  # Require mTLS for all communications

---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: order-service-authz
  namespace: production
spec:
  selector:
    matchLabels:
      app: order-service
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/production/sa/api-gateway"]
    - source:
        principals: ["cluster.local/ns/production/sa/user-service"]
    to:
    - operation:
        methods: ["GET", "POST"]
        paths: ["/api/orders/*"]
    when:
    - key: source.labels[version]
      values: ["v1", "v2"]
  - from:
    - source:
        principals: ["cluster.local/ns/production/sa/admin-service"]
    to:
    - operation:
        methods: ["GET", "POST", "PUT", "DELETE"]
        paths: ["/api/orders/*", "/api/admin/orders/*"]

---
apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: jwt-auth
  namespace: production
spec:
  selector:
    matchLabels:
      app: order-service
  jwtRules:
  - issuer: "https://auth.company.com"
    jwksUri: "https://auth.company.com/.well-known/jwks.json"
    audiences:
    - "order-service"
    forwardOriginalToken: true
```

**Service-to-Service Authentication:**
```java
// Inter-Service Authentication Service
@Service
public class ServiceAuthenticationService {
    
    @Value("${service.name}")
    private String serviceName;
    
    @Value("${service.secret}")
    private String serviceSecret;
    
    private final RestTemplate restTemplate;
    
    public ServiceAuthenticationService() {
        this.restTemplate = createSecureRestTemplate();
    }
    
    public <T> ResponseEntity<T> callSecureService(String serviceUrl, String endpoint, 
                                                  Class<T> responseType, Object request) {
        // Generate service-to-service JWT
        String serviceToken = generateServiceToken();
        
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        headers.setBearerAuth(serviceToken);
        headers.set("X-Service-Name", serviceName);
        headers.set("X-Request-ID", generateRequestId());
        
        HttpEntity<Object> entity = new HttpEntity<>(request, headers);
        
        try {
            return restTemplate.exchange(
                serviceUrl + endpoint, 
                HttpMethod.POST, 
                entity, 
                responseType
            );
        } catch (Exception e) {
            logger.error("Service call failed: {}", serviceUrl + endpoint, e);
            throw new ServiceCommunicationException("Failed to call service", e);
        }
    }
    
    private String generateServiceToken() {
        Instant now = Instant.now();
        
        return Jwts.builder()
            .setIssuer(serviceName)
            .setSubject("service")
            .setAudience("internal-services")
            .setIssuedAt(Date.from(now))
            .setExpiration(Date.from(now.plusSeconds(300))) // 5 minutes
            .claim("service_name", serviceName)
            .claim("type", "service_token")
            .signWith(Keys.hmacShaKeyFor(serviceSecret.getBytes()), SignatureAlgorithm.HS512)
            .compact();
    }
    
    private RestTemplate createSecureRestTemplate() {
        // Create SSL context със certificate validation
        SSLContext sslContext = createSSLContext();
        
        // Configure connection timeouts
        RequestConfig requestConfig = RequestConfig.custom()
            .setConnectionRequestTimeout(5000)
            .setConnectTimeout(5000)
            .setSocketTimeout(10000)
            .build();
        
        CloseableHttpClient httpClient = HttpClients.custom()
            .setSSLContext(sslContext)
            .setDefaultRequestConfig(requestConfig)
            .setMaxConnTotal(100)
            .setMaxConnPerRoute(20)
            .build();
        
        HttpComponentsClientHttpRequestFactory factory = 
            new HttpComponentsClientHttpRequestFactory(httpClient);
        
        RestTemplate restTemplate = new RestTemplate(factory);
        
        // Add error handling
        restTemplate.setErrorHandler(new ServiceErrorHandler());
        
        return restTemplate;
    }
    
    public boolean validateServiceToken(String token) {
        try {
            Claims claims = Jwts.parserBuilder()
                .setSigningKey(Keys.hmacShaKeyFor(serviceSecret.getBytes()))
                .build()
                .parseClaimsJws(token)
                .getBody();
            
            // Verify token type
            if (!"service_token".equals(claims.get("type"))) {
                return false;
            }
            
            // Verify service name
            String tokenServiceName = claims.get("service_name", String.class);
            if (!isAllowedService(tokenServiceName)) {
                return false;
            }
            
            return true;
            
        } catch (JwtException e) {
            logger.warn("Invalid service token: {}", e.getMessage());
            return false;
        }
    }
}
```

### **🔐 API Gateway Security Implementation (15 мин)**

**Comprehensive API Gateway Security:**
```java
// API Gateway Security Configuration
@Configuration
@EnableWebFluxSecurity
public class GatewaySecurityConfig {
    
    @Bean
    public SecurityWebFilterChain springSecurityFilterChain(ServerHttpSecurity http) {
        return http
            .csrf().disable()
            .cors(cors -> cors.configurationSource(corsConfigurationSource()))
            .authorizeExchange(exchanges -> exchanges
                // Public endpoints
                .pathMatchers("/actuator/health", "/actuator/info").permitAll()
                .pathMatchers("/.well-known/**").permitAll()
                .pathMatchers("/auth/**").permitAll()
                
                // API endpoints require authentication
                .pathMatchers("/api/v1/public/**").permitAll()
                .pathMatchers("/api/v1/users/**").hasRole("USER")
                .pathMatchers("/api/v1/admin/**").hasRole("ADMIN")
                .pathMatchers("/api/**").authenticated()
                
                .anyExchange().denyAll()
            )
            .oauth2ResourceServer(oauth2 -> oauth2
                .jwt(jwt -> jwt
                    .jwtDecoder(jwtDecoder())
                    .jwtAuthenticationConverter(jwtAuthenticationConverter())
                )
            )
            .exceptionHandling(exceptions -> exceptions
                .authenticationEntryPoint(customAuthenticationEntryPoint())
                .accessDeniedHandler(customAccessDeniedHandler())
            )
            .build();
    }
    
    @Bean
    public ReactiveJwtDecoder jwtDecoder() {
        String jwkSetUri = "https://auth.company.com/.well-known/jwks.json";
        
        NimbusReactiveJwtDecoder jwtDecoder = 
            NimbusReactiveJwtDecoder.withJwkSetUri(jwkSetUri)
                .cache(Duration.ofMinutes(5))
                .build();
        
        // Add custom JWT validation
        jwtDecoder.setJwtValidator(jwtValidator());
        
        return jwtDecoder;
    }
    
    @Bean
    public Converter<Jwt, Mono<AbstractAuthenticationToken>> jwtAuthenticationConverter() {
        JwtAuthenticationConverter converter = new JwtAuthenticationConverter();
        converter.setJwtGrantedAuthoritiesConverter(jwt -> {
            // Extract authorities from JWT claims
            List<String> scopes = jwt.getClaimAsStringList("scope");
            return scopes.stream()
                .map(scope -> new SimpleGrantedAuthority("SCOPE_" + scope))
                .collect(Collectors.toList());
        });
        
        return new ReactiveJwtAuthenticationConverterAdapter(converter);
    }
}

// API Gateway Rate Limiting
@Component
public class RateLimitingGatewayFilter implements GlobalFilter, Ordered {
    
    private final ReactiveRedisTemplate<String, String> redisTemplate;
    private final RateLimitingProperties rateLimitingProperties;
    
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        ServerHttpRequest request = exchange.getRequest();
        String clientId = extractClientId(request);
        String endpoint = request.getPath().value();
        
        return checkRateLimit(clientId, endpoint)
            .flatMap(allowed -> {
                if (allowed) {
                    return chain.filter(exchange);
                } else {
                    return handleRateLimitExceeded(exchange);
                }
            });
    }
    
    private Mono<Boolean> checkRateLimit(String clientId, String endpoint) {
        String key = String.format("rate_limit:%s:%s", clientId, endpoint);
        long window = System.currentTimeMillis() / 60000; // 1-minute windows
        String windowKey = key + ":" + window;
        
        return redisTemplate.opsForValue().increment(windowKey)
            .flatMap(count -> {
                if (count == 1) {
                    // Set expiration for new window
                    return redisTemplate.expire(windowKey, Duration.ofMinutes(1))
                        .then(Mono.just(true));
                }
                
                RateLimitRule rule = rateLimitingProperties.getRuleForEndpoint(endpoint);
                return Mono.just(count <= rule.getRequestsPerMinute());
            });
    }
    
    private Mono<Void> handleRateLimitExceeded(ServerWebExchange exchange) {
        ServerHttpResponse response = exchange.getResponse();
        response.setStatusCode(HttpStatus.TOO_MANY_REQUESTS);
        response.getHeaders().add("Retry-After", "60");
        response.getHeaders().add("X-RateLimit-Limit", "100");
        response.getHeaders().add("X-RateLimit-Remaining", "0");
        
        String errorMessage = "{\"error\":\"Rate limit exceeded\",\"message\":\"Too many requests\"}";
        DataBuffer buffer = response.bufferFactory().wrap(errorMessage.getBytes());
        
        return response.writeWith(Mono.just(buffer));
    }
    
    @Override
    public int getOrder() {
        return -1; // Execute before other filters
    }
}
```

### **📊 API Monitoring и Threat Detection (10 мин)**

**Real-time API Security Monitoring:**
```java
// API Security Monitoring Service
@Service
public class APISecurityMonitoringService {
    
    private final MeterRegistry meterRegistry;
    private final SecurityEventPublisher eventPublisher;
    
    @EventListener
    public void handleAPICall(APICallEvent event) {
        // Collect metrics
        Timer.Sample sample = Timer.start(meterRegistry);
        sample.stop(Timer.builder("api.request.duration")
            .tag("endpoint", event.getEndpoint())
            .tag("method", event.getMethod())
            .tag("status", String.valueOf(event.getResponseStatus()))
            .register(meterRegistry));
        
        // Increment counters
        meterRegistry.counter("api.requests.total",
            "endpoint", event.getEndpoint(),
            "status", String.valueOf(event.getResponseStatus()))
            .increment();
        
        // Detect anomalies
        detectSecurityAnomalies(event);
    }
    
    private void detectSecurityAnomalies(APICallEvent event) {
        // High error rate detection
        if (isHighErrorRate(event.getClientId(), event.getEndpoint())) {
            SecurityEvent securityEvent = SecurityEvent.builder()
                .type(SecurityEventType.HIGH_ERROR_RATE)
                .severity(SecuritySeverity.MEDIUM)
                .clientId(event.getClientId())
                .endpoint(event.getEndpoint())
                .description("High error rate detected")
                .timestamp(Instant.now())
                .build();
            
            eventPublisher.publish(securityEvent);
        }
        
        // Suspicious user agent detection
        if (isSuspiciousUserAgent(event.getUserAgent())) {
            SecurityEvent securityEvent = SecurityEvent.builder()
                .type(SecurityEventType.SUSPICIOUS_USER_AGENT)
                .severity(SecuritySeverity.LOW)
                .clientId(event.getClientId())
                .userAgent(event.getUserAgent())
                .description("Suspicious user agent detected")
                .timestamp(Instant.now())
                .build();
            
            eventPublisher.publish(securityEvent);
        }
        
        // Geographic anomaly detection
        if (isGeographicAnomaly(event.getClientId(), event.getIpAddress())) {
            SecurityEvent securityEvent = SecurityEvent.builder()
                .type(SecurityEventType.GEOGRAPHIC_ANOMALY)
                .severity(SecuritySeverity.MEDIUM)
                .clientId(event.getClientId())
                .ipAddress(event.getIpAddress())
                .description("Unusual geographic location detected")
                .timestamp(Instant.now())
                .build();
            
            eventPublisher.publish(securityEvent);
        }
        
        // Rapid consecutive requests (potential abuse)
        if (isRapidConsecutiveRequests(event.getClientId())) {
            SecurityEvent securityEvent = SecurityEvent.builder()
                .type(SecurityEventType.RAPID_REQUESTS)
                .severity(SecuritySeverity.HIGH)
                .clientId(event.getClientId())
                .description("Rapid consecutive requests detected")
                .timestamp(Instant.now())
                .build();
            
            eventPublisher.publish(securityEvent);
        }
    }
    
    @Scheduled(fixedRate = 60000) // Every minute
    public void generateSecurityMetrics() {
        // Calculate security metrics
        double errorRate = calculateErrorRate();
        double averageResponseTime = calculateAverageResponseTime();
        int activeThreats = countActiveThreats();
        
        // Update Grafana dashboards
        meterRegistry.gauge("api.security.error_rate", errorRate);
        meterRegistry.gauge("api.security.avg_response_time", averageResponseTime);
        meterRegistry.gauge("api.security.active_threats", activeThreats);
        
        // Check thresholds
        if (errorRate > 0.05) { // 5% error rate threshold
            alertService.sendAlert("High API error rate: " + errorRate);
        }
        
        if (averageResponseTime > 2000) { // 2 second threshold
            alertService.sendAlert("Slow API response time: " + averageResponseTime + "ms");
        }
    }
}

// API Security Dashboard Controller
@RestController
@RequestMapping("/api/security")
@PreAuthorize("hasRole('SECURITY_ADMIN')")
public class SecurityDashboardController {
    
    @Autowired
    private SecurityMetricsService metricsService;
    
    @GetMapping("/metrics")
    public ResponseEntity<SecurityMetrics> getSecurityMetrics(
            @RequestParam(defaultValue = "1h") String timeRange) {
        
        Duration duration = parseDuration(timeRange);
        SecurityMetrics metrics = metricsService.getMetrics(duration);
        
        return ResponseEntity.ok(metrics);
    }
    
    @GetMapping("/threats")
    public ResponseEntity<List<SecurityThreat>> getActiveThreats() {
        List<SecurityThreat> threats = metricsService.getActiveThreats();
        return ResponseEntity.ok(threats);
    }
    
    @PostMapping("/threats/{threatId}/mitigate")
    public ResponseEntity<Void> mitigateThreat(@PathVariable String threatId,
                                             @RequestBody ThreatMitigationRequest request) {
        
        securityResponseService.mitigateThreat(threatId, request);
        return ResponseEntity.ok().build();
    }
    
    @GetMapping("/reports/compliance")
    public ResponseEntity<ComplianceReport> getComplianceReport(
            @RequestParam String startDate,
            @RequestParam String endDate) {
        
        LocalDateTime start = LocalDateTime.parse(startDate);
        LocalDateTime end = LocalDateTime.parse(endDate);
        
        ComplianceReport report = metricsService.generateComplianceReport(start, end);
        return ResponseEntity.ok(report);
    }
}
```

---

## 🛠️ **ПРАКТИЧЕСКИ УПРАЖНЕНИЯ КОМПОНЕНТ 3.4**

### **🔗 Упражнение 3.4.1: Secure REST API Development (25 мин)**

**Project:** Complete e-commerce API security implementation

**API Requirements:**
- User management endpoints
- Product catalog API
- Order processing endpoints
- Payment integration API
- Administrative functions

**Implementation Tasks:**

**Phase 1: OWASP API Security Top 10 Mitigation (10 мин)**
- Implement object-level authorization
- Add comprehensive input validation
- Configure rate limiting по endpoint
- Set up API security headers

**Phase 2: JWT Authentication Integration (8 мин)**
- Configure JWT token validation
- Implement token refresh mechanism
- Add scope-based authorization
- Set up secure token storage

**Phase 3: API Security Testing (7 мин)**
- Test authorization bypass attempts
- Validate rate limiting effectiveness
- Verify JWT token security
- Perform API fuzzing tests

**Deliverable:** Production-ready secure REST API с comprehensive security measures

### **🎟️ Упражнение 3.4.2: OAuth 2.0 и OpenID Connect Implementation (20 мин)**

**Scenario:** Multi-tenant SaaS application authentication

**Requirements:**
- OAuth 2.0 authorization server
- PKCE support for mobile clients
- OpenID Connect ID tokens
- Multi-tenant isolation
- Social login integration

**Development Process:**

**Phase 1: OAuth 2.0 Server Setup (8 мин)**
- Configure authorization server
- Implement PKCE validation
- Set up client management
- Add security validations

**Phase 2: OpenID Connect Integration (7 мин)**
- Implement ID token generation
- Configure discovery endpoints
- Set up JWK endpoint
- Add userinfo endpoint

**Phase 3: Security Testing (5 мин)**
- Test authorization flows
- Validate PKCE implementation
- Verify token security
- Test multi-tenant isolation

**Deliverable:** Complete OAuth 2.0/OIDC implementation със security best practices

### **🏗️ Упражнение 3.4.3: Microservices Security Architecture (25 мин)**

**Architecture Challenge:** Secure microservices ecosystem design

**System Components:**
- API Gateway
- Authentication service
- User service
- Order service
- Payment service
- Notification service

**Implementation Tasks:**

**Phase 1: Service Mesh Security (10 мин)**
- Configure mTLS между services
- Set up service identity management
- Implement authorization policies
- Design zero-trust networking

**Phase 2: API Gateway Security (8 мин)**
- Configure centralized authentication
- Implement rate limiting
- Set up request validation
- Add monitoring и logging

**Phase 3: Inter-Service Communication (7 мин)**
- Implement service-to-service JWT
- Add circuit breaker patterns
- Configure retry policies
- Set up security monitoring

**Deliverable:** Complete microservices security architecture с documentation

### **📊 Упражнение 3.4.4: API Security Monitoring System (20 мин)**

**Monitoring Objective:** Real-time API threat detection system

**Monitoring Requirements:**
- Real-time threat detection
- Anomaly identification
- Performance monitoring
- Compliance reporting
- Automated response triggers

**Development Process:**

**Phase 1: Metrics Collection (7 мин)**
- Implement API call tracking
- Set up performance metrics
- Configure error rate monitoring
- Add security event logging

**Phase 2: Anomaly Detection (8 мин)**
- Implement rate anomaly detection
- Add geographic anomaly detection
- Configure behavioral analysis
- Set up threat scoring

**Phase 3: Alerting и Response (5 мин)**
- Configure real-time alerts
- Implement automated responses
- Set up dashboard visualization
- Test incident response workflows

**Deliverable:** Complete API security monitoring solution с automated threat response

---

## 📊 **ОЦЕНЯВАНЕ КОМПОНЕНТ 3.4**

### **📝 Формативно оценяване:**

**🧪 Тест 3.4: API Security и Microservices (20 мин, 30 точки)**

**Практически задачи (18 точки):**
1. Implement OAuth 2.0 authorization flow с PKCE (8 точки)
2. Configure microservices mTLS communication (5 точки)
3. Design API rate limiting strategy (5 точки)

**Теоретични въпроси (12 точки):**
1. Compare# 💻 МОДУЛ 3: "APPLICATION SECURITY И SECURE DEVELOPMENT"

## 🎯 ЗАГЛАВНА ЧАСТ

**Предмет:** Технически методи за защита  
**Клас:** 11  
**Модул:** 3 - Application Security и Secure Development  
**Продължителност:** 20 учебни часа (20 x 40 минути)  
**Тип урок:** Комбинирани уроци за усвояване на нови знания с практически secure coding implementations и penetration testing exercises

---

## 🎓 МОДУЛНИ УЧЕБНИ ЦЕЛИ

### **ГЛАВНА ЦЕЛ:**
Учениците да овладеят comprehensive application security principles, secure development lifecycle methodologies, и practical vulnerability assessment techniques за създаване на security-first software applications.

### **СПЕЦИФИЧНИ ЦЕЛИ:**

**ЗНАНИЯ:**
- Разбиране на Secure Software Development Lifecycle (SSDLC) principles и best practices
- Познаване на OWASP Top 10 vulnerabilities и corresponding mitigation strategies
- Владеене на static/dynamic code analysis techniques и security testing methodologies
- Знание за API security frameworks, authentication и authorization mechanisms
- Разбиране на DevSecOps integration strategies и automated security testing

**УМЕНИЯ:**
- Провеждане на comprehensive security code reviews и vulnerability assessments
- Имплементиране на secure coding practices в various programming languages
- Конфигуриране на automated security testing tools в CI/CD pipelines
- Изпълнение на web application penetration testing и security validation
- Разработка на security-first application architectures и design patterns

**КОМПЕТЕНТНОСТИ:**
- Стратегическо мислене за application security integration в development processes
- Лидерство в secure development team coordination и security culture building
- Способност за risk assessment и security-performance trade-off analysis
- Continuous learning mindset за emerging application security threats и technologies
- Professional communication на security findings и recommendations към development teams

---

## 📋 МОДУЛНА СТРУКТУРА

### **🔒 КОМПОНЕНТ 3.1: SECURE SOFTWARE DEVELOPMENT LIFECYCLE (5 часа)**
- Час 1: SSDLC Principles и Security-by-Design Methodology
- Час 2: Threat Modeling и Risk Assessment Techniques  
- Час 3: Security Requirements Engineering и Architecture Design
- Час 4: Secure Coding Standards и Development Guidelines
- Час 5: Security Testing Integration и Validation Strategies

### **🔍 КОМПОНЕНТ 3.2: CODE REVIEW И VULNERABILITY ANALYSIS (5 часа)**
- Час 1: Static Application Security Testing (SAST) Tools и Techniques
- Час 2: Manual Code Review Methodologies и Best Practices
- Час 3: Dynamic Application Security Testing (DAST) Implementation
- Час 4: Interactive Application Security Testing (IAST) Integration
- Час 5: Vulnerability Management и Remediation Strategies

### **🌐 КОМПОНЕНТ 3.3: WEB APPLICATION SECURITY (5 часа)**
- Час 1: OWASP Top 10 Vulnerabilities Analysis и Mitigation
- Час 2: Input Validation и Output Encoding Security Measures
- Час 3: Authentication и Session Management Security
- Час 4: Authorization и Access Control Implementation
- Час 5: Web Application Penetration Testing Methodologies

### **🔗 КОМПОНЕНТ 3.4: API SECURITY И MICROSERVICES PROTECTION (3 часа)**
- Час 1: REST API Security Design и Implementation
- Час 2: OAuth 2.0, JWT и Modern Authentication Protocols
- Час 3: Microservices Security Patterns и Container Protection

### **⚙️ КОМПОНЕНТ 3.5: DEVSECOPS И AUTOMATED SECURITY INTEGRATION (2 часа)**
- Час 1: CI/CD Security Pipeline Integration и Automation
- Час 2: Infrastructure as Code Security и DevSecOps Culture Implementation

---

## 🎯 **ЗАПОЧВАМЕ С КОМПОНЕНТ 3.1!**

### **🔒 КОМПОНЕНТ 3.1: SECURE SOFTWARE DEVELOPMENT LIFECYCLE (5 часа)**

#### **🎯 СПЕЦИФИЧНИ ЦЕЛИ НА КОМПОНЕНТА:**

**ЗНАНИЯ:**
- Разбиране на Secure SDLC phases и security integration points
- Познаване на threat modeling methodologies и attack vector analysis
- Владеене на security requirements engineering и compliance frameworks
- Знание за secure architecture patterns и design principles
- Разбиране на security testing strategies и validation techniques

**УМЕНИЯ:**
- Провеждане на comprehensive threat modeling sessions
- Разработка на security requirements specifications
- Проектиране на secure application architectures
- Имплементиране на security-first development processes
- Изпълнение на security validation и testing protocols

**КОМПЕТЕНТНОСТИ:**
- Стратегическо мислене за security integration в development lifecycle
- Лидерство в secure development culture establishment
- Способност за cross-functional security collaboration
- Risk-based decision making за security-performance trade-offs
- Continuous improvement mindset за security process optimization

---

## 📚 **ЧАС 1: SSDLC PRINCIPLES И SECURITY-BY-DESIGN METHODOLOGY (40 мин)**

### **🏗️ Secure SDLC Overview и Framework (12 мин)**

**Traditional SDLC vs. Secure SDLC:**

```
Traditional SDLC Approach:
Requirements → Design → Development → Testing → Deployment
                                    ↑
                              Security Added Here
                              (Too Late, Expensive)

Secure SDLC Approach:
Security Requirements → Secure Design → Secure Development → Security Testing → Secure Deployment
    ↓                      ↓              ↓                    ↓                ↓
Security Planning → Threat Modeling → Secure Coding → Penetration Testing → Security Monitoring
```

**Security-by-Design Core Principles:**

**1. Proactive vs. Reactive Security:**
- **Shift-left approach** - integrate security от earliest phases
- **Cost of fixing** - 100x more expensive в production vs. design
- **Security debt** - accumulation на security issues over time
- **Prevention focus** - stop vulnerabilities before they're created

*Real-world example: Microsoft's SDL implementation reduced security vulnerabilities by 50%*

**2. Defense in Depth Strategy:**
- **Multiple security layers** - no single point of failure
- **Redundant protections** - backup security mechanisms
- **Layered validation** - input sanitization + WAF + database permissions
- **Gradual degradation** - system remains functional when layers fail

**3. Least Privilege Principle:**
- **Minimal access rights** - users/processes get only necessary permissions
- **Just-in-time access** - temporary elevated permissions when needed
- **Regular permission audits** - quarterly access reviews
- **Zero-trust architecture** - verify everything, trust nothing

**4. Fail-Safe Defaults:**
- **Secure by default** - applications start в secure state
- **Explicit permissions** - everything denied unless explicitly allowed
- **Error handling** - failures should not expose sensitive information
- **Configuration security** - secure defaults в all system settings

### **📊 SSDLC Phase Integration (15 мин)**

**Phase 1: Security Planning и Requirements (SDLC Planning)**

**Security Requirements Categories:**
```
Functional Security Requirements:
├── Authentication mechanisms
├── Authorization frameworks  
├── Data encryption standards
├── Audit logging capabilities
└── Input validation rules

Non-Functional Security Requirements:
├── Performance under attack
├── Availability during incidents
├── Scalability of security measures
├── Usability of security features
└── Maintainability of security code
```

**Compliance Framework Integration:**
- **GDPR compliance** - data protection by design и by default
- **PCI-DSS requirements** - payment card data protection standards
- **HIPAA security** - healthcare data protection requirements
- **Bulgarian regulations** - local compliance requirements integration

**Phase 2: Secure Architecture и Design (SDLC Design)**

**Secure Architecture Patterns:**

**Layered Security Architecture:**
```
Presentation Layer Security:
├── Input validation и sanitization
├── Output encoding и CSP headers
├── Session management и CSRF protection
└── Client-side security measures

Business Logic Layer Security:
├── Authorization и access control
├── Business rule validation
├── Workflow security enforcement
└── Data integrity verification

Data Access Layer Security:
├── Database connection security
├── SQL injection prevention
├── Data encryption at rest
├── Query parameterization
└── Database access logging
```

**Microservices Security Patterns:**
- **API Gateway security** - centralized authentication и rate limiting
- **Service mesh security** - encrypted inter-service communication
- **Container security** - image scanning и runtime protection
- **Zero-trust networking** - mutual TLS между services

**Phase 3: Secure Development (SDLC Implementation)**

**Secure Coding Standards:**
- **Language-specific guidelines** - Java, C#, Python, JavaScript security
- **Framework security** - Spring Security, ASP.NET Security, Django Security
- **Third-party library management** - vulnerability scanning и updates
- **Code review checklists** - security-focused review criteria

**Phase 4: Security Testing (SDLC Testing)**

**Security Testing Pyramid:**
```
Manual Penetration Testing (Top)
├── Comprehensive security validation
├── Complex attack scenario testing
├── Business logic vulnerability assessment
└── Social engineering и physical security

Automated Security Testing (Middle)
├── Dynamic Application Security Testing (DAST)
├── Interactive Application Security Testing (IAST)
├── Dependency vulnerability scanning
└── Infrastructure security scanning

Static Code Analysis (Base)
├── Static Application Security Testing (SAST)
├── Software Composition Analysis (SCA)
├── Code quality и security metrics
└── Policy compliance validation
```

**Phase 5: Secure Deployment (SDLC Deployment)**

**Production Security Hardening:**
- **Infrastructure security** - server hardening и network security
- **Application configuration** - secure deployment settings
- **Monitoring setup** - security event detection и alerting
- **Incident response** - security incident handling procedures

### **⚡ Security Integration Challenges и Solutions (13 мин)**

**Common SSDLC Implementation Challenges:**

**Challenge 1: Developer Resistance**
- **Problem:** Developers view security as productivity hindrance
- **Solution:** Security champions program и security training
- **Implementation:** Gamified security training и positive reinforcement
- **Metrics:** Developer security knowledge assessment scores

**Challenge 2: Timeline Pressure**
- **Problem:** Business demands faster delivery at expense на security
- **Solution:** Automated security testing integration
- **Implementation:** Security gates в CI/CD pipelines
- **Metrics:** Time-to-fix security issues tracking

**Challenge 3: Security Expertise Gap**
- **Problem:** Limited security knowledge в development teams
- **Solution:** Security champions и embedded security engineers
- **Implementation:** Cross-functional security training programs
- **Metrics:** Security vulnerability detection rates by developers

**SSDLC Success Metrics:**

**Security KPIs:**
```
Vulnerability Metrics:
├── Critical vulnerabilities в production: Target <5 per release
├── Time to fix critical issues: Target <24 hours
├── Security test coverage: Target >90%
└── Security training completion: Target 100% annually

Process Metrics:
├── Security requirements coverage: Target 100%
├── Security review completion: Target 100% critical features
├── Security test automation: Target >80% automated
└── Incident response time: Target <1 hour detection
```

**Cost-Benefit Analysis:**
- **Security investment** - typically 5-15% на development budget
- **Breach cost savings** - average $3.86M per prevented breach
- **Compliance benefits** - reduced regulatory fines и penalties
- **Reputation protection** - maintained customer trust и loyalty

**SSDLC Maturity Model:**
```
Level 1: Initial (Ad-hoc security)
├── Reactive security measures
├── Basic security awareness
└── Limited security testing

Level 2: Repeatable (Basic SSDLC)
├── Security requirements definition
├── Basic threat modeling
└── Regular security testing

Level 3: Defined (Comprehensive SSDLC)
├── Standardized security processes
├── Automated security testing
└── Security metrics tracking

Level 4: Managed (Optimized SSDLC)
├── Continuous security improvement
├── Advanced threat modeling
└── Proactive security measures

Level 5: Optimizing (Innovation SSDLC)
├── Industry-leading security practices
├── Security research integration
└── Continuous innovation
```

---

## 📚 **ЧАС 2: THREAT MODELING И RISK ASSESSMENT TECHNIQUES (40 мин)**

### **🎯 Threat Modeling Fundamentals (15 мин)**

**Threat Modeling Definition и Purpose:**

Threat modeling е systematic approach за identifying, cataloging, и prioritizing potential security threats към application или system. Целта е да се разберат attack vectors преди application deployment.

**Core Threat Modeling Questions:**
1. **What are we building?** - Asset identification и system understanding
2. **What can go wrong?** - Threat identification и attack scenario development  
3. **What should we do about it?** - Mitigation strategy development
4. **Did we do a good job?** - Validation и continuous improvement

**STRIDE Threat Categorization:**

```
S - Spoofing Identity:
├── Impersonating legitimate users
├── Using stolen credentials  
├── Session hijacking attacks
└── Mitigation: Strong authentication, MFA

T - Tampering with Data:
├── Unauthorized data modification
├── Man-in-the-middle attacks
├── Database injection attacks
└── Mitigation: Data integrity checks, encryption

R - Repudiation:
├── Denying performed actions
├── Log tampering attempts
├── Non-repudiation bypass
└── Mitigation: Audit logging, digital signatures

I - Information Disclosure:
├── Unauthorized data access
├── Data leakage scenarios
├── Privacy violations
└── Mitigation: Access controls, encryption

D - Denial of Service:
├── Resource exhaustion attacks
├── Service availability disruption
├── Performance degradation
└── Mitigation: Rate limiting, load balancing

E - Elevation of Privilege:
├── Unauthorized permission escalation
├── Administrative access bypass
├── Privilege abuse scenarios
└── Mitigation: Least privilege, role separation
```

**DREAD Risk Rating System:**

**Risk Calculation Formula:**
```
Risk Score = (Damage + Reproducibility + Exploitability + Affected Users + Discoverability) / 5

Rating Scale (1-10):
├── 1-3: Low Risk
├── 4-6: Medium Risk  
├── 7-8: High Risk
└── 9-10: Critical Risk
```

**Example DREAD Assessment:**
```
SQL Injection Vulnerability:
├── Damage: 9 (Complete database access)
├── Reproducibility: 8 (Easy to reproduce)
├── Exploitability: 7 (Moderate skill required)
├── Affected Users: 9 (All users affected)
├── Discoverability: 6 (Moderate discovery difficulty)
└── Risk Score: 7.8 (High Risk)
```

### **🔍 Threat Modeling Methodologies (12 мин)**

**Microsoft STRIDE-based Approach:**

**Step 1: Create Architecture Overview**
- **Data Flow Diagrams (DFD)** - visual representation на system interactions
- **Trust Boundaries** - security perimeter identification
- **Entry Points** - all possible input vectors
- **Assets** - valuable data и functionality identification

**Step 2: Threat Identification**
- **Apply STRIDE** - systematic threat category analysis
- **Attack Trees** - hierarchical attack scenario decomposition
- **Misuse Cases** - negative use case development
- **Threat Libraries** - reusable threat pattern catalogs

**OWASP Threat Modeling Methodology:**

**Application-Centric Approach:**
```
Phase 1: Preparation
├── Define application scope
├── Identify stakeholders
├── Gather documentation
└── Set threat modeling objectives

Phase 2: Threat Model Creation
├── Application decomposition
├── Threat identification
├── Risk analysis
└── Mitigation planning

Phase 3: Review и Validation
├── Threat model peer review
├── Architecture validation
├── Security control verification
└── Documentation updates
```

**PASTA (Process for Attack Simulation and Threat Analysis):**

**Business-Driven Threat Modeling:**
```
Stage 1: Business Objective Definition
├── Business requirements analysis
├── Compliance requirements mapping
├── Risk tolerance establishment
└── Success criteria definition

Stage 2: Technical Scope Definition  
├── Application boundaries
├── Infrastructure dependencies
├── Technology stack analysis
└── Data classification

Stage 3: Application Decomposition
├── Data flow analysis
├── Component interaction mapping
├── Trust boundary identification
└── Attack surface analysis

Stage 4: Threat Analysis
├── Attack vector identification
├── Threat agent profiling
├── Attack scenario development
└── Vulnerability correlation

Stage 5: Weakness и Vulnerability Analysis
├── Security control gap analysis
├── Known vulnerability research
├── Configuration assessment
└── Code review findings integration

Stage 6: Attack Modeling
├── Attack tree construction
├── Attack path analysis
├── Exploitation probability assessment
└── Impact evaluation

Stage 7: Risk Analysis и Management
├── Risk quantification
├── Mitigation strategy development
├── Cost-benefit analysis
└── Treatment plan creation
```

### **📊 Risk Assessment и Prioritization (13 мин)**

**Quantitative Risk Assessment:**

**Asset Valuation Methods:**
```
Asset Value Calculation:
├── Replacement Cost: Cost to recreate asset
├── Revenue Impact: Lost revenue from asset compromise
├── Regulatory Fines: Potential penalties for breach
├── Reputation Damage: Brand value impact
└── Total Asset Value = Sum of all impacts
```

**Threat Probability Assessment:**
- **Historical data** - past incident frequency analysis
- **Industry statistics** - sector-specific threat intelligence
- **Environmental factors** - current threat landscape assessment
- **Control effectiveness** - existing security measure evaluation

**Risk Calculation Example:**
```
E-commerce Customer Database:
├── Asset Value: $2,000,000
├── Threat: SQL Injection Attack
├── Vulnerability: Inadequate input validation
├── Probability: 30% annually (based on industry data)
├── Impact: 80% (partial data exposure)
└── Annual Risk = $2M × 0.30 × 0.80 = $480,000
```

**Qualitative Risk Assessment:**

**Risk Matrix Approach:**
```
Probability vs. Impact Matrix:
                Low Impact  Medium Impact  High Impact
High Probability    Medium      High       Critical
Med Probability     Low         Medium     High  
Low Probability     Low         Low        Medium
```

**Risk Treatment Strategies:**

**1. Risk Mitigation (Reduce):**
- **Technical controls** - firewalls, encryption, access controls
- **Administrative controls** - policies, procedures, training
- **Physical controls** - locks, cameras, environmental controls
- **Cost consideration** - mitigation cost vs. risk reduction

**2. Risk Transfer (Share):**
- **Cyber insurance** - financial risk transfer
- **Third-party services** - outsource risky operations
- **Contractual liability** - shared responsibility agreements
- **Cloud services** - infrastructure risk transfer

**3. Risk Acceptance (Accept):**
- **Low impact risks** - cost of mitigation exceeds potential loss
- **Residual risk** - remaining risk after mitigation
- **Executive approval** - formal risk acceptance documentation
- **Regular review** - periodic risk reassessment

**4. Risk Avoidance (Eliminate):**
- **Feature removal** - eliminate risky functionality
- **Technology change** - switch to more secure alternatives
- **Process redesign** - eliminate risk-introducing processes
- **Business decision** - avoid risky business activities

**Continuous Risk Monitoring:**

**Risk Register Maintenance:**
```
Risk Register Components:
├── Risk ID и Description
├── Asset Affected
├── Threat Source
├── Vulnerability Details
├── Probability Assessment
├── Impact Assessment
├── Risk Score
├── Treatment Strategy
├── Owner Assignment
├── Review Date
└── Status Updates
```

**Risk Metrics и KPIs:**
- **Risk trend analysis** - risk level changes over time
- **Treatment effectiveness** - mitigation success measurement
- **Cost efficiency** - security spending optimization
- **Compliance coverage** - regulatory requirement adherence

---

## 📚 **ЧАС 3: SECURITY REQUIREMENTS ENGINEERING И ARCHITECTURE DESIGN (40 мин)**

### **📋 Security Requirements Development (15 мин)**

**Security Requirements Categories:**

**Authentication Requirements:**
```
Identity Verification Requirements:
├── Multi-factor authentication mandatory for admin access
├── Password complexity: 12+ characters, mixed case, numbers, symbols
├── Account lockout: 5 failed attempts, 15-minute lockout
├── Session timeout: 30 minutes inactivity for sensitive data
├── Password rotation: 90-day mandatory change for privileged accounts
└── Single sign-on integration with corporate identity provider
```

**Authorization Requirements:**
- **Role-based access control (RBAC)** - permissions based on job functions
- **Attribute-based access control (ABAC)** - dynamic permissions based on context
- **Principle of least privilege** - minimal necessary permissions
- **Segregation of duties** - critical operations require multiple approvals

**Data Protection Requirements:**
```
Encryption Requirements:
├── Data at Rest: AES-256 encryption for all sensitive data
├── Data in Transit: TLS 1.3 for all network communications
├── Key Management: Hardware Security Module (HSM) for key storage
├── Database Encryption: Transparent Data Encryption (TDE)
└── Backup Encryption: Encrypted backups with separate key management

Data Classification Requirements:
├── Public: No special protection required
├── Internal: Access control and audit logging
├── Confidential: Encryption and need-to-know access
├── Restricted: Maximum security controls and monitoring
└── Data Loss Prevention (DLP) controls for all classifications
```

**Audit и Compliance Requirements:**
- **Comprehensive logging** - all security-relevant events
- **Log integrity** - tamper-evident log storage
- **Log retention** - compliance-driven retention periods
- **Real-time monitoring** - immediate alerting для suspicious activities

**Security Requirements Specification Techniques:**

**User Stories with Security Acceptance Criteria:**
```
Example User Story:
"As a customer, I want to log into my account securely, so that my personal information is protected."

Security Acceptance Criteria:
├── GIVEN user enters valid credentials
├── WHEN authentication is successful  
├── THEN session is established with 30-minute timeout
├── AND login event is logged with timestamp и IP address
├── AND failed login attempts are tracked и limited
└── AND MFA is required for sensitive operations
```

**Misuse Cases Development:**
```
Primary Use Case: Customer places online order
Misuse Case: Attacker manipulates order total

Security Requirements:
├── Input validation на all price fields
├── Server-side total recalculation
├── Transaction integrity verification
├── Audit trail for all price changes
└── Anomaly detection for unusual orders
```

### **🏗️ Secure Architecture Patterns (12 мин)**

**Defense in Depth Architecture:**

**Network Security Layers:**
```
Internet → DMZ → Internal Network → Database Layer
    ↓       ↓         ↓               ↓
  WAF → Firewall → Internal FW → DB Encryption
    ↓       ↓         ↓               ↓
  DDoS → IDS/IPS → Host Security → Access Control
```

**Application Security Layers:**
```
Client Layer Security:
├── Input validation и sanitization
├── Client-side security controls
├── Browser security headers
└── Content Security Policy (CSP)

Presentation Layer Security:
├── Authentication и session management
├── Output encoding и XSS prevention
├── CSRF protection mechanisms
└── Rate limiting и throttling

Business Logic Layer Security:
├── Authorization и access control
├── Business rule validation
├── Data integrity verification
└── Workflow security enforcement

Data Access Layer Security:
├── SQL injection prevention
├── Database connection security
├── Data encryption и key management
└── Database activity monitoring
```

**Microservices Security Architecture:**

**Service-to-Service Security:**
```
API Gateway Pattern:
├── Centralized authentication
├── Rate limiting и throttling
├── Request routing и load balancing
├── API versioning и transformation
└── Comprehensive API logging

Service Mesh Security:
├── Mutual TLS (mTLS) между services
├── Service identity verification
├── Traffic encryption и authentication
├── Policy enforcement at network level
└── Zero-trust networking implementation
```

**Security Patterns Implementation:**

**Secure Session Management Pattern:**
```
Session Security Implementation:
├── Secure session ID generation (cryptographically random)
├── Session ID transmission only via HTTPS
├── Session timeout и idle timeout enforcement
├── Session invalidation на logout
├── Concurrent session limiting
└── Session fixation prevention
```

**Input Validation Pattern:**
```
Layered Input Validation:
├── Client-side validation (UX improvement, not security)
├── Server-side validation (primary security control)
├── Data type validation (integer, email, etc.)
├── Range validation (min/max values)
├── Format validation (regex patterns)
├── Business rule validation
└── Encoding/escaping for output contexts
```

### **🔐 Security Architecture Documentation (13 мин)**

**Security Architecture Artifacts:**

**Security Architecture Document Structure:**
```
1. Executive Summary
   ├── Business context и objectives
   ├── Security requirements overview
   ├── Architecture approach
   └── Risk assessment summary

2. Security Requirements
   ├── Functional security requirements
   ├── Non-functional requirements
   ├── Compliance requirements
   └── Threat model summary

3. Security Architecture Design
   ├── High-level security architecture
   ├── Component security design
   ├── Data flow security analysis
   └── Trust boundary definitions

4. Security Controls Mapping
   ├── Control implementation details
   ├── Responsibility matrix
   ├── Control testing procedures
   └── Compliance mapping

5. Risk Analysis
   ├── Residual risk assessment
   ├── Risk mitigation strategies
   ├── Risk acceptance decisions
   └── Monitoring requirements
```

**Security Control Framework Mapping:**

**NIST Cybersecurity Framework Alignment:**
```
Identify (ID):
├── Asset management (ID.AM)
├── Business environment (ID.BE)
├── Governance (ID.GV)
├── Risk assessment (ID.RA)
└── Risk management strategy (ID.RM)

Protect (PR):
├── Identity management и authentication (PR.AC)
├── Awareness и training (PR.AT)
├── Data security (PR.DS)
├── Information protection processes (PR.IP)
├── Maintenance (PR.MA)
└── Protective technology (PR.PT)

Detect (DE):
├── Anomalies и events (DE.AE)
├── Security continuous monitoring (DE.CM)
└── Detection processes (DE.DP)

Respond (RS):
├── Response planning (RS.RP)
├── Communications (RS.CO)
├── Analysis (RS.AN)
├── Mitigation (RS.MI)
└── Improvements (RS.IM)

Recover (RC):
├── Recovery planning (RC.RP)
├── Improvements (RC.IM)
└── Communications (RC.CO)
```

**Security Architecture Review Process:**

**Peer Review Checklist:**
```
Architecture Completeness:
├── ✓ All security requirements addressed
├── ✓ Threat model alignment verified
├── ✓ Control coverage assessment complete
├── ✓ Risk analysis documented
└── ✓ Compliance requirements mapped

Technical Accuracy:
├── ✓ Security controls properly implemented
├── ✓ Integration points secured
├── ✓ Performance impact assessed
├── ✓ Scalability considerations addressed
└── ✓ Maintainability evaluated

Documentation Quality:
├── ✓ Clear и unambiguous language
├── ✓ Sufficient detail for implementation
├── ✓ Consistent notation и terminology
├── ✓ Version control и change tracking
└── ✓ Stakeholder approval obtained
```

**Architecture Validation Techniques:**

**Security Architecture Testing:**
- **Threat model validation** - verify threat coverage completeness
- **Control testing** - validate security control effectiveness
- **Integration testing** - verify secure component interactions
- **Performance testing** - ensure security doesn't degrade performance

**Security Architecture Metrics:**
```
Design Quality Metrics:
├── Requirements coverage: 100% target
├── Threat model completeness: All STRIDE categories
├── Control effectiveness: >95% protection rate
├── Documentation quality: Peer review score >90%
└── Stakeholder satisfaction: >95% approval rate

Implementation Metrics:
├── Architecture compliance: 100% conformance target
├── Security testing coverage: >90% code coverage
├── Vulnerability detection: <5 critical issues
├── Performance impact: <10% degradation
└── Time to implement: Within planned timeline
```

---

## 🛠️ **ПРАКТИЧЕСКИ УПРАЖНЕНИЯ КОМПОНЕНТ 3.1**

### **🎯 Упражнение 3.1.1: SSDLC Integration Planning (25 мин)**

**Scenario:** Your company is transitioning from traditional SDLC към Secure SDLC за new web application development.

**Company Context:**
- 50-person development team
- Agile development methodology
- 2-week sprint cycles
- Limited security expertise
- High customer data sensitivity

**Задача:** Develop comprehensive SSDLC integration plan

**Step 1: Current State Assessment (5 мин)**
- Map existing development process phases
- Identify current security touchpoints
- Document security skill gaps
- Assess tool и resource needs

**Step 2: Security Integration Design (10 мин)**
- Define security requirements для each SDLC phase
- Plan security training и awareness program
- Design security review checkpoints
- Create security metrics и KPIs

**Step 3: Implementation Roadmap (10 мин)**
- Prioritize security integration activities
- Create realistic timeline и milestones
- Identify resource requirements
- Plan change management strategy

**Deliverable:** Complete SSDLC transition plan с budget и timeline

### **🔍 Упражнение 3.1.2: Threat Modeling Workshop (30 мин)**

**Application Scenario:** Online banking application с mobile access

**Given Architecture:**
- Web frontend (Angular)
- REST API backend (Java Spring)
- Database (PostgreSQL)
- Mobile app (React Native)
- Cloud deployment (AWS)

**Threat Modeling Tasks:**

**Phase 1: System Decomposition (8 мин)**
- Create data flow diagram
- Identify trust boundaries
- Map external entities
- Document data stores

**Phase 2: STRIDE Analysis (12 мин)**
- Apply STRIDE категории към each component
- Identify potential attack vectors
- Document threat scenarios
- Assess threat likelihood

**Phase 3: Risk Assessment (10 мин)**
- Apply DREAD scoring към identified threats
- Prioritize threats by risk score
- Recommend mitigation strategies
- Create risk treatment plan

**Deliverable:** Complete threat model с prioritized risk register

### **📋 Упражнение 3.1.3: Security Requirements Specification (20 мин)**

**Project:** Healthcare patient portal development

**Business Requirements:**
- Patient medical record access
- Appointment scheduling
- Secure messaging with doctors
- Prescription management
- Insurance integration

**Compliance Requirements:**
- HIPAA compliance mandatory
- State healthcare regulations
- Data breach notification laws

**Security Requirements Development:**

**Step 1: Requirements Analysis (5 мин)**
- Map business requirements към security needs
- Identify compliance obligations
- Assess risk factors

**Step 2: Security Specification (10 мин)**
- Define authentication requirements
- Specify authorization controls
- Document encryption requirements
- Create audit и logging specifications

**Step 3: Acceptance Criteria (5 мин)**
- Write testable security acceptance criteria
- Define security validation methods
- Create compliance verification steps

**Deliverable:** Complete security requirements specification document

### **🏗️ Упражнение 3.1.4: Secure Architecture Design (25 мин)**

**Challenge:** Design secure architecture за e-commerce platform

**Platform Requirements:**
- Customer registration и login
- Product catalog и search
- Shopping cart и checkout
- Payment processing
- Order management
- Admin portal

**Security Constraints:**
- PCI-DSS compliance required
- High availability (99.9% uptime)
- Support for 10,000 concurrent users
- Multi-tenant architecture

**Architecture Design Tasks:**

**Step 1: Component Architecture (8 мин)**
- Design layered security architecture
- Define component responsibilities
- Plan integration security measures

**Step 2: Security Controls Mapping (10 мин)**
- Map security controls към components
- Design authentication и authorization flow
- Plan data protection measures

**Step 3: Risk Mitigation (7 мин)**
- Identify architecture security risks
- Design risk mitigation strategies
- Plan monitoring и incident response

**Deliverable:** Secure architecture diagram с control specifications

---

## 📊 **ОЦЕНЯВАНЕ КОМПОНЕНТ 3.1**

### **📝 Формативно оценяване:**

**🧪 Тест 3.1: Secure SDLC Fundamentals (15 мин, 25 точки)**

**Множествен избор (10 точки):**
1. Which SSDLC principle reduces vulnerability fixing costs? (2.5)
   a) Defense in depth b) Shift-left c) Fail-safe defaults d) Least privilege

2. What is the primary purpose of threat modeling? (2.5)
   a) Code review b) Risk identification c) Performance testing d) User training

3. Which STRIDE category addresses data tampering? (2.5)
   a) Spoofing b) Tampering c) Information Disclosure d) Elevation

4. What does DREAD methodology evaluate? (2.5)
   a) Code quality b) Risk probability c) Performance metrics d) User satisfaction

**Отворени въпроси (15 точки):**
1. Explain the cost benefits of implementing SSDLC (5 точки)
2. Describe STRIDE threat categorization с examples (5 точки)
3. Compare quantitative vs. qualitative risk assessment (5 точки)

### **🎯 Практическо оценяване:**

**💼 Проект 3.1: SSDLC Implementation Plan (35 точки)**

**Рубрика за оценяване:**
- **SSDLC Design (12 точки):** Comprehensive lifecycle integration
- **Threat Model (10 точки):** Complete STRIDE analysis и DREAD scoring
- **Security Requirements (8 точки):** Detailed и testable specifications
- **Documentation Quality (5 точки):** Professional presentation и clarity

**Критерии за успех:**
- **Отличен (31-35):** Industry-ready SSDLC implementation
- **Много добър (25-30):** Strong technical approach с minor improvements
- **Добър (18-24):** Functional SSDLC с optimization opportunities
- **Среден (11-17):** Basic understanding с significant development needed

### **🏠 Домашна работа:**

**📋 Assignment 3.1: "Real-World Threat Modeling Case Study" (Due next week)**

**Task:** Analyze a recent security breach и develop preventive SSDLC measures

**Requirements:**
1. **Breach Analysis (40%)**
   - Incident timeline и impact assessment
   - Root cause analysis и vulnerability identification
   - Attack vector analysis using STRIDE

2. **SSDLC Prevention (35%)**
   - Identify SSDLC phases where prevention was possible
   - Design security requirements що could prevent the breach
   - Propose architecture changes за improved security

3. **Implementation Roadmap (25%)**
   - Create actionable SSDLC improvement plan
   - Estimate costs и timeline for implementation
   - Define success metrics и validation methods

**Format:** 6-page professional report с executive summary и appendices

---
