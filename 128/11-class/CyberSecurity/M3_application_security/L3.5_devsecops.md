---
Учител: Мариян Йорданов
Училище: 120-мо СУ "Алберт Айнщайн", гр. София
Предмет: Cybersecurity
---

### **⚙️ КОМПОНЕНТ 3.5: DEVSECOPS И AUTOMATED SECURITY INTEGRATION (2 часа)**

#### **🎯 СПЕЦИФИЧНИ ЦЕЛИ НА КОМПОНЕНТА:**

**ЗНАНИЯ:**
- Разбиране на DevSecOps principles и security-first development culture
- Познаване на CI/CD security integration patterns и automated testing strategies
- Владеене на Infrastructure as Code security practices и configuration management
- Знание за security automation tools и pipeline orchestration techniques
- Разбиране на continuous security monitoring и production observability approaches

**УМЕНИЯ:**
- Интегриране на security testing в CI/CD pipelines със automated quality gates
- Конфигуриране на Infrastructure as Code security scanning и compliance validation
- Имплементиране на security automation workflows и orchestration systems
- Управление на security incident response automation и remediation workflows
- Провеждане на DevSecOps culture transformation и team collaboration improvement

**КОМПЕТЕНТНОСТИ:**
- Strategic thinking за organizational DevSecOps transformation
- Leadership capabilities за cross-functional security team coordination
- Innovation mindset за security automation opportunity identification
- Continuous improvement approach за DevSecOps process optimization
- Business acumen за security investment justification и ROI demonstration

---

## 📚 **ЧАС 1: CI/CD SECURITY PIPELINE INTEGRATION (40 мин)**

### **⚙️ DevSecOps Pipeline Architecture (15 мин)**

**Complete DevSecOps Pipeline Design:**

```
Comprehensive DevSecOps Pipeline:
┌─────────────────────────────────────────────────────────────┐
│                    Source Code Management                   │
│  ├── Branch protection rules                               │
│  ├── Commit signing requirements                           │
│  ├── Security-focused code review                          │
│  └── Secrets detection в commits                           │
├─────────────────────────────────────────────────────────────┤
│                   Build & Security Scanning                │
│  ├── SAST - Static Application Security Testing           │
│  ├── SCA - Software Composition Analysis                  │
│  ├── Secret scanning и key detection                      │
│  ├── License compliance checking                          │
│  └── Build artifact signing                               │
├─────────────────────────────────────────────────────────────┤
│                    Test & Quality Assurance                │
│  ├── Unit test security validation                        │
│  ├── Integration test security scenarios                  │
│  ├── DAST - Dynamic Application Security Testing          │
│  ├── IAST - Interactive Application Security Testing      │
│  └── Performance security testing                         │
├─────────────────────────────────────────────────────────────┤
│                  Infrastructure Deployment                 │
│  ├── Infrastructure as Code scanning                      │
│  ├── Container security scanning                          │
│  ├── Kubernetes security validation                       │
│  ├── Cloud security posture assessment                    │
│  └── Network security configuration                       │
├─────────────────────────────────────────────────────────────┤
│                Production Security Monitoring              │
│  ├── Runtime application protection                       │
│  ├── Container runtime security                           │
│  ├── Network traffic analysis                             │
│  ├── Log security analysis                                │
│  └── Incident response automation                         │
└─────────────────────────────────────────────────────────────┘
```

**GitHub Actions DevSecOps Pipeline:**
```yaml
name: DevSecOps Security Pipeline
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # Security scanning jobs
  secret-scanning:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Run TruffleHog Secret Scanner
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        base: main
        head: HEAD
        extra_args: --debug --only-verified

    - name: Run GitLeaks Secret Detection
      uses: gitleaks/gitleaks-action@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  sast-analysis:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Run SonarQube Analysis
      uses: sonarqube-quality-gate-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        scanMetadataReportFile: target/sonar/report-task.txt

    - name: Run Semgrep SAST
      uses: returntocorp/semgrep-action@v1
      with:
        config: auto
        generateSarif: "1"

    - name: Upload SARIF results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: semgrep.sarif

  dependency-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Run OWASP Dependency Check
      uses: dependency-check/Dependency-Check_Action@main
      env:
        JAVA_HOME: /opt/jdk
      with:
        project: 'secure-app'
        path: '.'
        format: 'ALL'
        args: >
          --enableRetired
          --enableExperimental
          --failOnCVSS 7

    - name: Upload Dependency Check Results
      uses: actions/upload-artifact@v3
      with:
        name: dependency-check-report
        path: reports/

  build-and-test:
    needs: [secret-scanning, sast-analysis, dependency-check]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build Application
      run: |
        ./mvnw clean compile
        ./mvnw package -DskipTests

    - name: Run Security Unit Tests
      run: ./mvnw test -Dtest=**/*SecurityTest

    - name: Build Docker Image
      run: |
        docker build -t secure-app:${{ github.sha }} .
        docker tag secure-app:${{ github.sha }} secure-app:latest

  container-security:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Run Trivy Container Scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'secure-app:latest'
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Run Anchore Container Scan
      uses: anchore/scan-action@v3
      with:
        image: "secure-app:latest"
        fail-build: true
        severity-cutoff: high

    - name: Upload Trivy Results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: 'trivy-results.sarif'

  dast-testing:
    needs: build-and-test
    runs-on: ubuntu-latest
    services:
      app:
        image: secure-app:latest
        ports:
          - 8080:8080
    steps:
    - name: Wait for Application Startup
      run: |
        timeout 60 bash -c 'until curl -f http://localhost:8080/actuator/health; do sleep 2; done'

    - name: Run OWASP ZAP Baseline Scan
      uses: zaproxy/action-baseline@v0.7.0
      with:
        target: 'http://localhost:8080'
        rules_file_name: '.zap/rules.tsv'
        cmd_options: '-a'

    - name: Run OWASP ZAP Full Scan
      uses: zaproxy/action-full-scan@v0.4.0
      with:
        target: 'http://localhost:8080'
        rules_file_name: '.zap/rules.tsv'
        cmd_options: '-a -j'

  infrastructure-security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Run Terraform Security Scan
      uses: aquasecurity/tfsec-action@v1.0.0
      with:
        soft_fail: false

    - name: Run Checkov IaC Scanner
      uses: bridgecrewio/checkov-action@master
      with:
        directory: ./infrastructure
        framework: terraform,kubernetes,dockerfile
        output_format: sarif
        output_file_path: checkov-results.sarif

    - name: Upload Checkov Results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: checkov-results.sarif

  security-gate:
    needs: [container-security, dast-testing, infrastructure-security]
    runs-on: ubuntu-latest
    steps:
    - name: Security Quality Gate
      run: |
        # Aggregate security results и determine if deployment should proceed
        echo "Evaluating security quality gate..."

        # Check for critical vulnerabilities
        CRITICAL_VULNS=$(cat security-results.json | jq '.critical_count')
        HIGH_VULNS=$(cat security-results.json | jq '.high_count')

        if [ "$CRITICAL_VULNS" -gt 0 ]; then
          echo "❌ Security gate failed: $CRITICAL_VULNS critical vulnerabilities found"
          exit 1
        fi

        if [ "$HIGH_VULNS" -gt 5 ]; then
          echo "❌ Security gate failed: $HIGH_VULNS high-severity vulnerabilities (max 5 allowed)"
          exit 1
        fi

        echo "✅ Security gate passed: No critical vulnerabilities found"

  deploy:
    needs: security-gate
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - name: Deploy to Production
      run: |
        echo "Deploying secure application to production..."
        # Deployment logic here
```

### **🔧 Security Automation Tools Integration (12 мин)**

**Jenkins Security Pipeline:**
```groovy
pipeline {
    agent any

    environment {
        DOCKER_REGISTRY = 'secure-registry.company.com'
        SONAR_TOKEN = credentials('sonar-token')
        SECURITY_GATEWAY_URL = 'https://security-gateway.company.com'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
                script {
                    env.GIT_COMMIT_SHORT = sh(
                        script: "git rev-parse --short HEAD",
                        returnStdout: true
                    ).trim()
                }
            }
        }

        stage('Security Pre-Flight') {
            parallel {
                stage('Secret Scanning') {
                    steps {
                        script {
                            def secretScanResults = sh(
                                script: 'trufflehog filesystem . --json',
                                returnStdout: true
                            )

                            if (secretScanResults.contains('"verified": true')) {
                                error "❌ Verified secrets found в repository!"
                            }
                        }
                    }
                }

                stage('License Compliance') {
                    steps {
                        sh '''
                            # FOSSA license scanning
                            fossa analyze
                            fossa test --timeout 300
                        '''
                    }
                }

                stage('Branch Protection Validation') {
                    steps {
                        script {
                            if (env.BRANCH_NAME == 'main') {
                                def prNumber = env.CHANGE_ID
                                if (!prNumber) {
                                    error "❌ Direct push to main branch не е allowed!"
                                }
                            }
                        }
                    }
                }
            }
        }

        stage('Build & SAST Analysis') {
            parallel {
                stage('Application Build') {
                    steps {
                        sh '''
                            ./mvnw clean compile
                            ./mvnw package -DskipTests
                        '''
                    }
                }

                stage('Static Security Analysis') {
                    steps {
                        // SonarQube security analysis
                        withSonarQubeEnv('SonarQube') {
                            sh '''
                                ./mvnw sonar:sonar \
                                  -Dsonar.projectKey=secure-app \
                                  -Dsonar.qualitygate.wait=true \
                                  -Dsonar.security-hotspots.analysis=true
                            '''
                        }

                        // CodeQL analysis
                        sh '''
                            codeql database create codeql-db --language=java
                            codeql database analyze codeql-db \
                                --format=sarif-latest \
                                --output=security-results.sarif \
                                --sarif-category=security
                        '''

                        // Semgrep analysis
                        sh '''
                            semgrep --config=auto \
                                --sarif \
                                --output=semgrep-results.sarif \
                                --severity=ERROR \
                                .
                        '''
                    }
                }

                stage('Dependency Vulnerability Scan') {
                    steps {
                        sh '''
                            # OWASP Dependency Check
                            dependency-check.sh \
                                --project "secure-app" \
                                --scan . \
                                --format ALL \
                                --failOnCVSS 7 \
                                --enableExperimental

                            # Snyk dependency scanning
                            snyk test --severity-threshold=high
                            snyk monitor
                        '''
                    }
                }
            }
        }

        stage('Security Quality Gate') {
            steps {
                script {
                    // Aggregate security findings
                    def securityReport = [:]

                    // Parse SonarQube results
                    def sonarResults = readJSON file: 'sonar-results.json'
                    securityReport.sonar = [
                        securityHotspots: sonarResults.issues.findAll { it.type == 'SECURITY_HOTSPOT' }.size(),
                        vulnerabilities: sonarResults.issues.findAll { it.type == 'VULNERABILITY' }.size()
                    ]

                    // Security gate decision logic
                    if (securityReport.dependencies.critical > 0) {
                        error "❌ Security Gate Failed: ${securityReport.dependencies.critical} critical vulnerabilities в dependencies"
                    }

                    if (securityReport.sonar.vulnerabilities > 0) {
                        error "❌ Security Gate Failed: ${securityReport.sonar.vulnerabilities} code vulnerabilities found"
                    }

                    echo "✅ Security Gate Passed: Application meets security requirements"
                }
            }
        }

        stage('Container Security') {
            when {
                expression { currentBuild.result != 'FAILURE' }
            }
            steps {
                script {
                    def image = docker.build("${DOCKER_REGISTRY}/secure-app:${env.GIT_COMMIT_SHORT}")

                    sh """
                        # Trivy container scan
                        trivy image \
                            --severity CRITICAL,HIGH \
                            --format json \
                            --output trivy-results.json \
                            ${DOCKER_REGISTRY}/secure-app:${env.GIT_COMMIT_SHORT}
                    """

                    image.push()
                    image.push('latest')
                }
            }
        }

        stage('Infrastructure Security Validation') {
            steps {
                sh '''
                    # Terraform security validation
                    tfsec ./infrastructure --format json --out tfsec-results.json

                    # Checkov Infrastructure as Code scanning
                    checkov -d ./infrastructure \
                        --framework terraform,kubernetes \
                        --output json \
                        --output-file checkov-results.json

                    # OPA policy validation
                    opa test ./policies/ --verbose
                '''
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: '*-results.json,*-results.sarif', allowEmptyArchive: true
        }

        success {
            echo "✅ DevSecOps Pipeline completed successfully!"
        }

        failure {
            echo "❌ DevSecOps Pipeline failed due to security issues"
        }
    }
}
```

### **📊 Security Metrics и Continuous Monitoring (13 мин)**

**DevSecOps Metrics Dashboard - Key Concepts:**

Security KPIs to track in DevSecOps:
- **Mean Time to Detection (MTTD)** - time from vulnerability introduction to detection
- **Mean Time to Remediation (MTTR)** - time from detection to resolution
- **Security debt** - accumulated unresolved security findings
- **Pipeline security coverage** - percentage of code paths tested for security
- **Compliance score** - adherence to security standards

**Key Metrics Categories:**
```
Pipeline Metrics:
- Security scan pass/fail rates
- Vulnerability counts by severity (Critical/High/Medium/Low)
- Time to remediation by severity
- Security gate failure rates

Code Quality Metrics:
- Security hotspots identified
- Code vulnerabilities found
- Dependency vulnerabilities
- Secret exposure incidents

Deployment Metrics:
- Security-validated deployments
- Deployment frequency
- Change failure rate (security-related)
- Recovery time from security incidents
```

---

## 📚 **ЧАС 2: INFRASTRUCTURE AS CODE SECURITY И DEVSECOPS CULTURE (40 мин)**

### **🔧 Infrastructure as Code Security Automation (20 мин)**

**Terraform Security Scanning Integration:**

**Secure Terraform Module Example:**
```hcl
# Secure AWS Infrastructure Module
terraform {
  required_version = ">= 1.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# Security-hardened VPC configuration
resource "aws_vpc" "secure_vpc" {
  cidr_block           = var.vpc_cidr
  enable_dns_hostnames = true
  enable_dns_support   = true

  tags = {
    Name        = "${var.environment}-secure-vpc"
    Environment = var.environment
    Security    = "compliant"
  }
}

# VPC Flow Logs за security monitoring
resource "aws_flow_log" "vpc_flow_log" {
  iam_role_arn    = aws_iam_role.flow_log_role.arn
  log_destination = aws_cloudwatch_log_group.vpc_flow_log.arn
  traffic_type    = "ALL"
  vpc_id          = aws_vpc.secure_vpc.id
}

# Security-focused security group
resource "aws_security_group" "secure_app_sg" {
  name_prefix = "${var.environment}-secure-app-"
  vpc_id      = aws_vpc.secure_vpc.id

  # Restrict inbound traffic
  ingress {
    description = "HTTPS from ALB only"
    from_port   = 443
    to_port     = 443
    protocol    = "tcp"
    security_groups = [aws_security_group.alb_sg.id]
  }

  # Allow outbound HTTPS only
  egress {
    description = "HTTPS outbound"
    from_port   = 443
    to_port     = 443
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }
}

# Secure S3 bucket configuration
resource "aws_s3_bucket" "secure_app_bucket" {
  bucket = "${var.environment}-secure-app-${random_id.bucket_suffix.hex}"

  tags = {
    Environment = var.environment
    Security    = "encrypted"
  }
}

# S3 bucket encryption
resource "aws_s3_bucket_server_side_encryption_configuration" "secure_app_bucket_encryption" {
  bucket = aws_s3_bucket.secure_app_bucket.id

  rule {
    apply_server_side_encryption_by_default {
      kms_master_key_id = aws_kms_key.s3_key.arn
      sse_algorithm     = "aws:kms"
    }
    bucket_key_enabled = true
  }
}

# S3 bucket public access block
resource "aws_s3_bucket_public_access_block" "secure_app_bucket_pab" {
  bucket = aws_s3_bucket.secure_app_bucket.id

  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# CloudTrail за audit logging
resource "aws_cloudtrail" "security_audit_trail" {
  name           = "${var.environment}-security-audit-trail"
  s3_bucket_name = aws_s3_bucket.audit_logs_bucket.bucket

  enable_logging                = true
  enable_log_file_validation    = true
  include_global_service_events = true
  is_multi_region_trail         = true
}
```

**Terraform Security Validation Pipeline:**
```yaml
name: Infrastructure Security Validation
on:
  push:
    paths:
      - 'infrastructure/**'
  pull_request:
    paths:
      - 'infrastructure/**'

jobs:
  terraform-security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Run tfsec Security Scan
      uses: aquasecurity/tfsec-action@v1.0.0
      with:
        soft_fail: false
        tfsec_args: --minimum-severity MEDIUM

    - name: Run Checkov IaC Security Scan
      uses: bridgecrewio/checkov-action@master
      with:
        directory: infrastructure/
        framework: terraform
        output_format: sarif
        output_file_path: checkov-results.sarif
        soft_fail: false

    - name: Security Gate Evaluation
      run: |
        CRITICAL_ISSUES=$(jq '.critical_count' security-summary.json)
        HIGH_ISSUES=$(jq '.high_count' security-summary.json)

        if [ "$CRITICAL_ISSUES" -gt 0 ]; then
          echo "❌ Security gate failed: $CRITICAL_ISSUES critical issues found"
          exit 1
        fi

        echo "✅ Infrastructure security validation passed"
```

### **🛡️ DevSecOps Culture и Best Practices (20 мин)**

**DevSecOps Culture Transformation Framework:**

**Security Champions Program - Key Elements:**
- **Nomination process** - team members volunteer or are nominated as security champions
- **Training curriculum** - OWASP Top 10, secure coding, security testing methodologies
- **Mentorship framework** - pairing with experienced security professionals
- **Recognition programs** - acknowledging security champions' contributions

**Security-First Development Practices:**
- **Threat Modeling** - use STRIDE methodology for all new features
- **Secure Code Review** - security-focused reviews using checklists
- **Dependency Security** - automated vulnerability scanning for all dependencies
- **Security Testing Requirements** - SAST must pass before deployment

**Developer Security Training Program:**
```
Core Training Modules:
1. Security Fundamentals (8 hours)
   - CIA triad principles
   - Common attack vectors
   - Defense-in-depth

2. Secure Coding Practices (12 hours)
   - Input validation techniques
   - Injection vulnerability prevention
   - Authentication best practices
   - Session management

3. Application Security Testing (10 hours)
   - Security code review methods
   - SAST/DAST tool usage
   - Security test case design
   - Vulnerability interpretation
```

**Continuous Security Education:**
- **Weekly security tips** - rotating topics delivered to all developers
- **Monthly security challenges** - gamified learning с leaderboards
- **Incident-based learning** - anonymized case studies from real incidents
- **Security awareness campaigns** - phishing simulations, awareness events

---

## 🛠️ **ПРАКТИЧЕСКИ УПРАЖНЕНИЯ КОМПОНЕНТ 3.5**

### **⚙️ Упражнение 3.5.1: Complete DevSecOps Pipeline Implementation (30 мин)**

**Project:** End-to-end secure CI/CD pipeline design

**Pipeline Requirements:**
- Multi-stage security testing
- Infrastructure as Code security
- Container security scanning
- Automated security gates
- Security metrics collection

**Implementation Tasks:**

**Phase 1: CI/CD Security Integration (12 мин)**
- Configure SAST/DAST/SCA integration
- Implement security quality gates
- Set up automated security testing
- Configure security notification system

**Phase 2: Infrastructure Security Automation (10 мин)**
- Integrate Terraform security scanning
- Set up OPA policy validation
- Configure compliance checking
- Implement security drift detection

**Phase 3: Monitoring и Metrics (8 мин)**
- Implement security metrics collection
- Set up security dashboards
- Configure automated alerting
- Create security reporting automation

**Deliverable:** Production-ready DevSecOps pipeline със comprehensive security automation

### **🔧 Упражнение 3.5.2: Infrastructure as Code Security Framework (25 мин)**

**Scenario:** Multi-cloud infrastructure security implementation

**Infrastructure Components:**
- AWS VPC и networking
- Kubernetes cluster deployment
- Database security configuration
- Monitoring и logging setup
- Backup и disaster recovery

**Development Process:**

**Phase 1: Secure Infrastructure Design (10 мин)**
- Design security-hardened infrastructure
- Implement security best practices
- Configure encryption и access controls
- Set up audit logging

**Phase 2: Security Scanning Integration (8 мин)**
- Configure tfsec/Checkov scanning
- Implement OPA policy validation
- Set up compliance checking
- Add security testing automation

**Phase 3: Deployment Security (7 мин)**
- Configure secure deployment pipeline
- Implement security monitoring
- Set up incident response
- Test security controls

**Deliverable:** Production-ready IaC security framework с automated scanning

### **🛡️ Упражнение 3.5.3: DevSecOps Culture Transformation Program (20 мин)**

**Organizational Challenge:** Implement security-first development culture

**Transformation Scope:**
- Security champions program
- Developer security training
- Security awareness campaigns
- Gamified security education
- Security metrics и recognition

**Program Development:**

**Phase 1: Security Champions Initiative (8 мин)**
- Design security champions program
- Create nomination и selection process
- Develop training curriculum
- Set up mentorship framework

**Phase 2: Education и Awareness (7 мин)**
- Create security training modules
- Design gamified security challenges
- Implement continuous education program
- Set up knowledge sharing platform

**Phase 3: Culture Measurement (5 мин)**
- Define culture transformation metrics
- Implement feedback collection system
- Create recognition programs
- Design continuous improvement process

**Deliverable:** Complete DevSecOps culture transformation program с implementation roadmap

### **📊 Упражнение 3.5.4: Security Automation и Orchestration Platform (25 мин)**

**Platform Objective:** Comprehensive security automation ecosystem

**Automation Components:**
- Security tool orchestration
- Incident response automation
- Compliance automation
- Security metrics automation
- Threat intelligence integration

**Development Process:**

**Phase 1: Security Tool Integration (10 мин)**
- Design tool orchestration architecture
- Implement automated workflows
- Configure tool communication
- Set up data aggregation

**Phase 2: Automation Workflows (8 мин)**
- Create incident response automation
- Implement compliance checking
- Set up security monitoring
- Configure automated remediation

**Phase 3: Intelligence Integration (7 мин)**
- Integrate threat intelligence feeds
- Implement automated threat hunting
- Set up predictive analytics
- Configure automated response

**Deliverable:** Production-ready security automation platform с comprehensive orchestration

---

## 📊 **ОЦЕНЯВАНЕ КОМПОНЕНТ 3.5**

### **📝 Формативно оценяване:**

**🧪 Тест 3.5: DevSecOps и Automation (15 мин, 25 точки)**

**Практически задачи (15 точки):**
1. Design CI/CD security pipeline с automated gates (7 точки)
2. Configure Infrastructure as Code security scanning (4 точки)
3. Implement security metrics collection system (4 точки)

**Теоретични въпроси (10 точки):**
1. Explain DevSecOps culture transformation strategies (3 точки)
2. Compare security automation tools и their integration (4 точки)
3. Design continuous security education program (3 точки)

### **🎯 Практическо оценяване:**

**💼 Проект 3.5: Complete DevSecOps Transformation (35 точки)**

**Рубрика за оценяване:**
- **CI/CD Security Integration (12 точки):** Comprehensive pipeline security, automated gates, tools integration
- **Infrastructure Security Automation (10 точки):** IaC security, policy validation, compliance automation
- **Culture Transformation (8 точки):** Security champions, training programs, awareness initiatives
- **Security Automation Platform (5 точки):** Tool orchestration, workflow automation, intelligence integration

**Критерии за успех:**
- **Отличен (31-35):** Industry-leading DevSecOps implementation с innovation
- **Много добър (26-30):** Strong DevSecOps practices с minor enhancements needed
- **Добър (19-25):** Functional DevSecOps approach с optimization opportunities
- **Среден (12-18):** Basic understanding requiring significant development

### **🏠 Домашна работа:**

**📋 Assignment 3.5: "Enterprise DevSecOps Strategy" (Due next week)**

**Task:** Develop comprehensive DevSecOps transformation strategy за large organization

**Requirements:**
1. **Strategic Planning (40%)**
   - DevSecOps maturity assessment
   - Transformation roadmap development
   - Culture change management strategy
   - Success metrics definition

2. **Technical Implementation (35%)**
   - Complete CI/CD security architecture
   - Infrastructure automation framework
   - Security tool integration design
   - Automation workflow specifications

3. **Organizational Change (25%)**
   - Security champions program design
   - Training и education strategy
   - Change management approach
   - Continuous improvement framework

**Format:** Executive strategy document (15-20 pages) с technical appendices, implementation timeline, и budget projections

---
